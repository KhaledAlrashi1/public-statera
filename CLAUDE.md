# CLAUDE.md — public-statera

This file is read by Claude Code at the start of every session. Keep it accurate and up to date.

## Project identity
- **Name:** public-statera
- **What it is:** a personal finance dashboard for Kuwait (KWD currency, KW locale)
- **Monorepo:** `apps/web` (Vite + React 19 + TypeScript + Tailwind) and `apps/api` (Hono + TypeScript + Drizzle + MySQL 8)
- **Origin:** ported from a private Flask + PostgreSQL version. This repo is the Node/TypeScript public version.

## Stack

**Frontend (`apps/web`)**
- Vite 6, React 19, TypeScript 5.7, Tailwind v4
- Radix UI, shadcn/ui conventions (CVA, clsx, tailwind-merge), Lucide icons
- TanStack Query v5, React Router v7, Recharts v2, TanStack Virtual v3
- Vitest + Testing Library (unit), Playwright (E2E)

**Backend (`apps/api`)**
- Hono, Node.js LTS, TypeScript
- Drizzle ORM, MySQL 8 (utf8mb4, utf8mb4_0900_ai_ci, DATETIME(3))
- BullMQ + Redis 7 for background jobs
- Postmark for email, Sentry for errors
- openid-client for OAuth (Google as initial provider; provider-agnostic)
- Vitest

**Tooling**
- pnpm workspaces
- Docker Compose for local dev and prod

## Migration status

**Completed modules:**
- 1a Sentry initialization
- 1b Postmark email service (sendEmail + sendEmailBackground only; sendTemplatedEmail deferred to first caller)
- 1c BullMQ worker scaffold (ping job only)
- 2a Categories
- 2b Merchants
- 3a Transactions (CRUD, queries, bulk, import-batch; exports deferred)
- 3b Memorized transactions
- 4a Budgets
- 4b Debt accounts (calculator + routes) — **SUPERSEDED by phase4 SC-1/2 (2026-07-16): the debt-accounts feature was removed. `routes/debt.ts` + `lib/debt-calculator.ts` (and tests) deleted, router unmounted, purge/data-export/R9/R8/R13 references removed. Table dropped in SC-3. History retained.**
- 4c Savings goals (projection lib + routes) — **SUPERSEDED by phase4 SC-1/2 (2026-07-16): the savings-goals feature was removed. `routes/savings-goals.ts` + `lib/savings-goals-lib.ts` (and tests) deleted, router unmounted, milestone-email dispatch + purge/data-export/R9/R13 references removed. Table dropped in SC-3. History retained.**
- 5a-1 Dashboard snapshot lib
- 5a-2 Analytics cache layer (circuit breaker, Redis helpers, withAnalyticsTimeout)
- 5a-3 Dashboard snapshots rebuild job
- 4d Transaction suggestions
- 5b-1 Aggregation pre-work (analytics-helpers, payday-lib, income-lib; cache busts wired to all write routes)
- 5b-2 Pure aggregation routes (R1 spend-by-category, R2 spend-by-month, R5 expense-breakdown, R6 expense-merchant-trend, R7 budget-metrics)
- 5b-3a Cached routes (R3 dashboard-metrics, R4 account-overview)
- 5b-3b Safe-to-spend route (R9) with F1-F5 fixtures
- 5b-3c Weekly-digest (R10) + dashboard-bundle (R8)
- 5c Intelligence/detection routes (algorithmic; fixture-based equivalence tests) — R11 income-pattern, R12 recurring-patterns, R13 snapshot
- 6a Cleanup jobs (cleanup-account-tokens, cleanup-security-data, cleanup-product-events, cleanup-memorized-transactions)
- 6b Product-events lib (recordEvent, recordEventOnce, recordEventDaily, hasEvent, hasEventBetween; consolidates savings-goals/budgets local copies; wires app_opened on dashboard_metrics)
- 6c Budget alerts + email templates (email-templates lib, budget-alerts-lib, check-budget-alerts BullMQ job, send-budget-alert-email job, send-goal-milestone-email job, GET /api/notifications/budget-alerts, POST /api/notifications/budget-alerts/dismiss, R8 budget_alerts.items wired, goal milestone email dispatch in savings-goals deposit) — **PARTIALLY SUPERSEDED by phase4 SC-1/2 (2026-07-16): the `send-goal-milestone-email` job, its handler, the `goal_milestone` email template, and the savings-goals-deposit dispatch were removed with the savings-goals feature (removed from the JobName union + `jobHandlers` registry per C6). The budget-alerts side (`check-budget-alerts`, `send-budget-alert-email`, the `budget_alert` template, notifications routes) is UNCHANGED. History retained.**
- 6d Activation report job (activation-reporting-lib with buildActivationReport, generate-activation-report BullMQ job with atomic write-to-tmp+rename, signup_completed event wired in auth callback, 3 env vars: ACTIVATION_REPORT_INTERVAL_HOURS/DAYS/PATH)
- 7a TOTP enable/disable (totp-lib with otplib/bcryptjs/qrcode, POST /api/auth/2fa/setup + confirm + disable; sv claim added to JWT; sv deny-list revocation via Redis sv_revoked:{userId}:{sv} keys; session cookie re-issued on disable)
- 7b TOTP verify-on-login (statera_pending_2fa short-lived JWT cookie; OIDC callback gates on totpEnabled → redirect /auth/2fa-verify; POST /api/auth/2fa/verify handles TOTP + backup-code; Redis pending_2fa_failures:{userId} counter with 5-min TTL; PENDING_2FA_MAX_FAILURES=3; BACKUP_CODES_LOW warning ≤2 remaining; auditSecurityEvent helper writing to security_events; login.pending_2fa / login.success / login.2fa.failed events)
- 7c Revoke-all sessions + security events (POST /api/auth/sessions/revoke-all: bumps sessionVersion in DB + writes sv_revoked deny-list key with 30-day TTL + re-issues caller cookie with newSv + sessions.revoke_all audit event; GET /api/auth/profile/security-events: offset-based pagination, limit default 20 max 50, filter event_type LIKE 'profile.%' matching Flask exactly, +00:00 timestamp format)
- 7.5 Account deletion (GET /api/auth/delete-reauth → OIDC re-auth with prompt=login; statera_delete_intent JWT cookie Path=/api/account Max-Age=900; DELETE /api/account: verifies intent cookie + isActive, BullMQ async job (delete-account-data, jobId dedup) with sync transaction fallback Sentry-tracked; GET /api/account/deletion-status/:taskToken: encrypted status token; purgeUserAccountRows: tombstone-first then 13 table deletes then soft-delete; is_tombstone column on security_events migration 0003; hashEmail SHA-256 no-salt; 2FA re-verification: if totpEnabled, callback issues statera_pending_2fa with deleteIntent=true → /2fa/verify issues delete-intent cookie instead of session)
- **Module 7 smoke status:** 7a/7b/7c smoke-verified against live Docker dev stack on 2026-05-18. 7.5 still smoke-deferred — same structural deferral as recorded in "Module fix-forwards" (no browser-held session in test harness). Module 8 deployment work for 8e (TLS + Caddy) may proceed: the precondition's intent — verifying Module 7's runtime behaviour — is met for 7a/7b/7c. 7.5 smoke remains an open item; resolution path is the deferred phase-4 design decision (dev-only test endpoint vs alternative harness pattern).

**Module 8 — Deployment (Hetzner CPX31, Helsinki HEL1 — staterafinance.app):**
- 8a Dockerfile hardening + prod Compose (DONE: pinned node:22.11.0-alpine digest, tini PID-1, non-root user node:1000, GIT_SHA build arg + ENV, npm-installed pnpm bypasses corepack key-verification issue, worker service added to prod Compose, one-shot migrate service, mysql:8.0.41 + redis:7.4.2-alpine pinned with digests, /health exposes version)
- 8a-1 Dev compose for local end-to-end testing (DONE: gap discovered during Module 7 smoke test prep 2026-05-16; docker-compose.dev.yml overlay adds source-mounted tsx watch for api + worker + migrate service; profiles: ["docker-app"] removed from base docker-compose.yml so overlay merges cleanly; COMPOSE_FILE shortcut documented in .env.example; first-run sequence added to README.md; tsx watch verified with plain volume mount — :ro dropped after confirming tsx may need to write inode metadata on some platforms)
- 8a-2 API host bind (DONE: env.ts default flipped from 127.0.0.1 to 0.0.0.0; latent bug from phase-2 scaffold 13c709a; surfaced during Module 7 smoke test prep when curl from host returned "Empty reply" while in-container healthcheck succeeded; .env.example updated with explanatory comment; same fix closes a latent prod bug — prod compose also doesn't set API_HOST so prod api would have been unreachable from Caddy in 8e until this fix)
- 8b Server bootstrap (DONE: deploy/bootstrap.sh — 13-section idempotent script: apt packages (chrony explicitly installed, overrides Debian 12's systemd-timesyncd default), deploy user, Hetzner Volume format+mount with device-path pattern validation, Docker CE + Compose (docker-compose-plugin via apt, tracks Docker engine version automatically), vm.overcommit_memory+swappiness sysctl, fail2ban 24h bantime, UFW, GHCR login guidance, unattended-upgrades with explicit 20auto-upgrades APT::Periodic settings, chrony with 5-min stabilisation warning, SSH hardening + AcceptEnv DEPLOY_SHA, deploy-user SSH verification before root lockout with Hetzner Rescue recovery procedure, age/sops §1 install + §14 key dir; deploy/.env.prod.example; docker-compose.prod.yml volumes → Option A bind-mount; Dockerfile corepack comment; rebuild-from-scratch test passed sha256:bb2ba40f; production server bootstrapped end-to-end 2026-05-15 on CPX31 Helsinki HEL1; two post-commit fixes in daec3d1 — see "Module fix-forwards" below; **TODO(module-8b-§13-rewrite):** §13 SSH verification has a known false-negative — `sudo -u deploy ssh deploy@localhost` fails on fresh server because deploy user has no private key, only operator's pubkey in authorized_keys; script aborts safely before root SSH disabled but doesn't prove operator access works; real verification is "operator SSHes in from laptop" done externally; future fix: rewrite §13 to validate authorized_keys content + sshd PubkeyAuthentication config rather than loopback SSH. The 2026-05-22 operator-key-rotation incident reinforces the need for this rewrite: §13's current loopback test would have passed on the 2026-05-15 bootstrap and still left the server in a state where the operator's actual laptop key was the single point of failure. A correct §13 should verify "the operator-supplied pubkey resolves to a key the operator currently holds the private half of" — for example, by signing a server-issued challenge with the operator's private key during bootstrap, or by requiring at least two operator pubkeys at bootstrap time (forcing the operator to think about backup access before going live); **2026-05-16 finding:** §14 (sops key dir) had never executed on this server due to early-exit on Compose URL and git clone failures in pre-daec3d1 runs; directory created manually post-discovery, no code change required; **TODO(module-8b-bootstrap-rerun-strategy): CLOSED 2026-05-23** — the three concrete gaps are resolved: §14 (sops key dir) backfilled 2026-05-16; repo clone done manually 2026-05-22 (see "8d — bootstrap §6 repo clone gap" fix-forward); production deploys operational end-to-end as of 2026-05-23, which implicitly verifies §7–§12 (Docker, AcceptEnv DEPLOY_SHA, fail2ban, UFW, unattended-upgrades all active in production). §13-rewrite remains open as its own TODO.)
- 8c Secrets management (code committed 468696c + runbook fixes 8917c6a + §14 finding 537deb0 + key population 059bf30; sops v3.9.1 + age v1.2.0 in bootstrap §1; §14 key dir; .sops.yaml two-recipient pattern (operator + server age public keys committed in 059bf30); deploy/SECRETS.md; deploy/8c-post-bootstrap.md; .gitattributes sopsdiffer; .gitignore guards (incl. secrets/smoke.sops.yaml); **OPERATIONAL as of 2026-05-16** — runbook §1–§4 executed, smoke test passed (server decrypted clean dotenv output, no metadata leakage); §5 completed 2026-05-22 (commit 5a2abf3) — production secrets file encrypted with 29 variables; operator age key rotated mid-session due to inadvertent private-key disclosure (see fix-forwards); §6 (pre-8d manual deploy chain validation) intentionally deferred to be exercised by 8d's deploy.sh in production)
- 8d CI/CD (code committed 1b53243 + 1bd02f0 fixup — .github/workflows/deploy.yml test→build-push→deploy, concurrency serialized, workflow_dispatch rollback path, all actions SHA-pinned; deploy/deploy.sh — decrypt-once, _rollback() with prev-SHA pull + health check; deploy/deploy-bootstrap.sh — thin wrapper: git fetch + rev-parse + git reset --hard + exec deploy.sh; deploy/DEPLOY.md; bootstrap §12 AcceptEnv DEPLOY_SHA; GHCR namespace typo khaledalrashidi1→khaledalrashi1 fixed in 1bd02f0; closing commits: d3c8e7c (§4 temp-file decrypt), a62a6b6 (URL-safe MYSQL_PASSWORD + registry/repo split), 79e19ba (thin bootstrap), e1decfb (resolve-sha workflow job), b0e413b (stale comment); **OPERATIONAL as of 2026-05-23** — forward deploy (push to b0e413b) and rollback test (workflow_dispatch sha=79e19ba, back to b0e413b) both green end-to-end; **TODO(module-8d-node24-upgrade): CLOSED 2026-05-30** — all six actions bumped to node24 runtimes: checkout v5.0.1 (`93cb6efe`), setup-node v5.0.0 (`a0853c24`), pnpm/action-setup v4.4.0 (`fc06bc12`, corrected from annotated tag-object SHA to commit SHA), build-push-action v7.2.0 (`f9f3042f`), login-action v4.2.0 (`650006c6`), setup-buildx-action v4.1.0 (`d7f5e7f5`); floating `# vN` comments replaced with exact `# vN.N.N`)
- 8e TLS + reverse proxy + frontend delivery (apex architecture; closing commits: 4d45603 (web Dockerfile + Caddyfile), 59d69fe (caddy fmt + redundant header cleanup), e8e6d69 (frontend CI gating to test job, after 1 triage commit (1caa098) fixing 12 pre-existing test failures across 6 files), 52250a7 (CI builds + pushes statera-web image), 27ae763 (compose cutover: nginx → Caddy-based web service, ports 80/443 bound, ACME provisioned, site live), 829f8ef (obsolete nginx + Flask-era config removed); **OPERATIONAL as of 2026-05-24** — staterafinance.app live on HTTPS via Let's Encrypt, Caddy serving SPA at / and reverse-proxying /api/* + /healthz + /readyz to api:3000; **CSP ENFORCING as of 2026-07-10** (was Report-Only 2026-05-24 → 2026-07-10) — see TODO(module-8e-csp-enforcement) below; **TODO(module-8e-csp-enforcement): CLOSED 2026-07-10 — enforcing CSP live and post-flip walk GREEN.** Post-flip verification walk against the enforcing header PASSED 2026-07-10: console silent (zero `Refused to load`), fonts real incl. the design-0 IBM Plex Mono numerals, QR renders, blob export downloads, Recharts draw. CSP is done. Phase A (2026-07-09) established that the original decision criterion — "zero violation reports in CSP report endpoint logs over ≥7 consecutive days" — was **VACUOUS**: the Report-Only header carried NO `report-uri`/`report-to` directive, so nothing was ever collected in the window that opened 2026-05-24. "Zero reports" was zero-by-construction, not a clean policy — the same pass-for-the-wrong-reason class as the 10d-0 rollback-test finding (an observation window that couldn't observe). The browser independently confirmed this during the pre-flip walk ("no report-to; the policy will have no effect"). Evidence for the flip was therefore a **console-verified operator page-walk** instead (proportionate for a one-user app; no `report-uri` infra was built just to restart a multi-week window). **Pre-flip walk GREEN 2026-07-10** — login / legal (/privacy /terms) / dashboard / insights / import-dialog / 2FA-QR / data-export / budgets, hard-reloaded with console open: the ONLY violations were the two Google Fonts hosts the static audit predicted (the `fonts.googleapis.com` stylesheet `<link>` in `index.html` + the `fonts.gstatic.com` woff2 fetches), which the Report-Only policy did not allow; QR (`img-src data:`), blob export (`accountApi.dataExport`), and Recharts inline styles (`style-src 'unsafe-inline'`) all clean. Two expected non-blocking browser notices, recorded: (i) "frame-ancestors is ignored in report-only" (the flip activates it); (ii) the no-report-to notice above. (One unrelated budgets-page UI error was observed and is under separate triage — possibly redesign-branch-only; it does not gate the flip since Report-Only blocks nothing.) The enforcing header adds `https://fonts.googleapis.com` to `style-src` and `https://fonts.gstatic.com` to `font-src`; everything else byte-identical to the Report-Only baseline. **Rollback:** one-line revert of the Caddyfile header (re-add `-Report-Only`) + redeploy — single header line, SHA-tagged image, one deploy cycle. **Standing style-src note retained:** do NOT remove `style-src 'unsafe-inline'` here — that needs separate scoping (inline-style elimination or CSP nonces, both non-trivial). **Inherited-requirement — CLOSED design-5.4e (2026-07-13):** the chosen typefaces (Space Grotesk + IBM Plex Mono) are now self-hosted via `@fontsource` (weights 400/500/600/700; IBM Plex Sans left as a named fallback only, not self-hosted); the Google Fonts `<link>`/preconnects were removed from `index.html` and `style-src`/`font-src` narrowed back to `'self'` in `deploy/Caddyfile` — no more visitor-IP leak to Google on any page incl. the pre-auth legal pages. The **queued `img-src https:` → `'self' data:`** narrowing landed in the same atomic commit (only image sources are `'self'` + the 2FA `data:` QR — enumerated). `style-src 'unsafe-inline'` STAYS (Recharts). Font self-host + `<link>` removal + Caddyfile narrowing are ONE commit, and the CSP change was named in its deploy report per the ride-along rule. **TODO(module-8e-flask-era-artifact-audit): CLOSED 2026-05-26** — ci.yml (Flask-era, failing on every push since Phase 3 port; no required status checks) and pg-backup.sh (Postgres+S3, no active invocations on production server) deleted; RUNBOOK.md, DOCKER_RUNBOOK.md, docs/runbooks/backup-restore.md, and docs/runbooks/incident-response.md deleted as orphaned documentation for the deleted artifacts. Three additional runbooks (docs/runbooks/activation-reporting.md, docs/runbooks/open-banking-provider-onboarding.md, docs/runbooks/key-rotation.md) were missed in 829f8ef; cleared 2026-05-30: first two deleted, key-rotation.md replaced with Hono-era stub pointing to TODO(key-rotation-runbook).; **TODO(module-9-network-mocking):** The unit test suite mocks at the analyticsApi function level (vi.mock the api module, return canned responses). This bypasses the network layer entirely, which means URL drift between frontend and Hono backend is invisible to tests — including the 12-of-13 broken-URL state that was live in production from commit 27ae763 (2026-05-24) until the 9.2 fix. Adding a fetch-level mock layer (likely MSW — Mock Service Worker) would surface URL and response-shape drift at unit-test time rather than via manual smoke. Not scoped for any current Module 9 sub-item; tracked for future consideration.)
- 8f Backups + monitoring + smoke test:
  - 8f-1 Encrypted R2 backups: **DONE (2026-07-08)** — scripts committed (deploy/backup-db.sh, deploy/systemd/statera-backup.{service,timer}); R2 bucket `statera-prod-db-backups` created with daily/14d + weekly/56d + monthly/365d lifecycle rules; rclone v1.74.2 pinned in bootstrap.sh §1; credentials in sops. Backup DB name is **env-sourced** (`--databases "${MYSQL_DATABASE}"`, `backup-db.sh:101`; production value `statera_prod`) — never a hardcoded literal. **Go-live: the timer was committed but never installed (backups dead 37 days — see fix-forward F1); installed AND verified running 2026-07-08.** Restore path proven by the 8f-2 drill (all stages PASS). Backup existence alone was never DR; the drill is what makes this DONE.
  - 8f-2 Restore drill: **DONE (2026-07-08)** — executed end-to-end against `weekly/statera-2026-05-31T21:13:56Z.sql.zst.age`; all stages PASS (Stage 0 deterministic decrypt ×3 identical pulls → Stage 1 21-table-exact + FK=0 + anchor=1 → Stage 2 gate boundary → Stage 3 known-answer re-purge, exactly 1 = userId 1, all owned rows → 0, tombstone survived → teardown). Execution record in `docs/runbooks/backups.md §8f-2`; handoff `docs/recovery/2026-07-08-8f2-restore-drill.md`. **The drill surfaced SIX operator-seam/environment gaps before its first assertion ran — dead backup timer (F1), Node-toolchain runner (F3), sudo escalation/lost password (F2), prefix double-prefix (`765ed6f`), and the mysql:8 init race's two symptoms 2002 + 1045 (`dde1dfb`) — which is the drill working, not the drill failing.** Original requirement retained: **the re-purge step MUST include the tombstone match** (scoped under 10d): after restoring from a backup, match restored users' hashed emails against surviving `security_events` tombstone rows (`is_tombstone=true`) and re-run `purgeUserAccountRows` for every match, so accounts deleted before the restore do not come back. This is now a PUBLISHED policy commitment (Privacy §7 — "we re-apply all account deletions completed before the restore"); the drill is what keeps that promise from being a paper one. **Timestamp rule (10d-0-docs, 2026-07-07; boundary refined to `>=` in the 8f-2 review):** the re-purge must be TIME-gated — re-purge a restored user only when their deletion tombstone timestamp (`security_events.created_at` on the `is_tombstone=true` row) is **at or after (`>=`) the backup being restored**. (At exact equality the restored rows can only be pre-deletion data — a reactivation stamped at/after `T_backup` cannot appear in a snapshot taken at `T_backup` — so equality must re-purge; a tombstone strictly *before* the backup is already reflected in it and must NOT re-purge, else a user who deleted and *later* re-registered would be wrongly purged — a sequence reactivate-as-fresh (10d-0b) newly makes possible.) Tombstone rows already carry `created_at`; this is a drill-procedure rule, not a schema change. **DR precondition #1 (operator age-key offsite escrow) — CLOSED 2026-07-09:** the drill proved the *server* age key decrypts backups today but not that the key survives simultaneous laptop+server loss; escrow is now done — the operator age key is stored in an end-to-end-encrypted locked note (device-passcode mode, syncs off-device), verified by re-open after locking. Paper copy in a physically separate building recorded as the upgrade path (defense-in-depth), not a blocker. A total-server loss is now recoverable (R2 backups + off-machine decryption key). See `docs/recovery/2026-07-08-8f2-restore-drill.md` open-item #1.
  - 8f-3 UptimeRobot + healthcheck ping: **DONE — VERIFIED 2026-07-09** (F1 rationale delivered: a dead-man ping on the backup timer catches a silent timer failure on day 1, not day 37; `backup-db.sh`'s existing ping plumbing wired to a Healthchecks.io dead-man check + two UptimeRobot monitors on `/healthz`+`/readyz` only, never `/health`). **Operational config (facts):** (a) Healthchecks.io dead-man check `statera-db-backup` expects a ping on the backup cron schedule `30 2 * * *` UTC with a 60-minute grace window; `HEALTHCHECK_PING_URL` in prod sops (committed `feb4907`); `backup-db.sh` pings on success. (b) two UptimeRobot HTTP(s) monitors — `statera-healthz` on `/healthz` and (readyz) on `/readyz` — 5-minute interval, 2-fail confirmation before alerting; never `/health` (SPA fallback, see the health-path correction below). **Verification — both induced-absence tests fired real alerts** (this is the 8e/F1 "documented artifact needs an active verification step" lesson applied — activation proven, not assumed): (1) **dead-man DOWN** — a skipped backup ping produced the "statera-db-backup is DOWN" email ~6 min after the missed window; (2) **prober DOWN** — pointing a monitor at `/api/nonexistent` produced the "statera-healthz is down" email. Both checks were then restored and **confirmed re-armed** (UP notifications received). **Positive path confirmed:** a manual `backup-db.sh` run pinged → the HC check went green, and both UR monitors report UP. Verification-window discipline satisfied (the induced tests exercise the alert path itself, not just the happy path — the exact gap that let F1's dead timer go unnoticed for 37 days).
  - 8f-4 Deploy-rollback rehearsal: NOT STARTED
- Module 9: Frontend parity verification (apps/web tested against the new Hono API end-to-end). **Module 9 is operating as a defect-discovery pipeline, not a verification module. Each sub-item fix unblocks discovery of the next defect class.** Each sub-item follows propose→approve→write discipline. **Precondition: 8e produces production environment (met 2026-05-24). CORE CRUD JOURNEY CLEAN (2026-05-30) — but NOT fully done: the 2026-07-03 professionalization audit found 5 user-reachable frontend calls resolving to unmounted Hono routes (demo-workspace load, CSV import upload-preview + import-commit, CSV/XLSX export, template-suggestions, and dead featuresApi/bankApi). The "full user journey with no errors" done-condition is therefore NOT met for the demo/import/export paths. Remediation tracked in Phase 4 / Module 10b. See "Module 9 gap — 2026-07-03 audit" fix-forward.**
- 9.1 income_source enum: null → "not_set" (DONE: api.ts lines 138/176/265 type definitions updated to match Hono contract; sections.tsx:249 null-comparison changed to `=== 'not_set'` fixing silently broken income nudge banner; safe-to-spend test fixture base updated + positive/negative nudge-banner test coverage added)
- 9.2 analytics route prefix: /api/* → /api/analytics/* (DONE: 12 of 13 analyticsApi call sites in apps/web/src/lib/api.ts corrected from Flask-era bare paths to Hono's /api/analytics/* prefix; accountOverview was already correct; spendingIntelligence removed in 9.5c — see "Deferred indefinitely"; see TODO(module-9-network-mocking) in 8e entry)
- 9.3a Auth UI: OIDC login page + 2FA verify page + new-user path (DONE: LoginPage replaced with OIDC-only <a href="/api/auth/login">; TwoFactorVerifyPage added at /auth/2fa-verify; Hono callback new-user branch redirects to /welcome?source=signup; WorkspaceChoicePage gate updated for ?source=signup; dead-code removal deferred to 9.3b)
- 9.3b Dead-code removal (DONE: 5 Flask-era auth pages + RegisterPage.test.tsx + workspace-choice.ts deleted; CSRF machinery removed from apiFetch + downloadTransactionExport + uploadApi.preview; dead authApi methods removed; twoFactorDisable password field removed; AuthContext login/register removed; WorkspaceChoicePage gate simplified to source=signup param only)
- 9.4 /me route incompleteness (DONE: Phase 2 scaffold returned {session} stub instead of user record; AuthContext read data.user as undefined → user always null → ProtectedRoute bounced every authenticated user to /login; fixed by completing the route to query users table and return {ok, user, flags}; flags hard-coded false — template_suggestions and open_banking have no Hono routes; see TODO(module-9-feature-flags-audit))
- 9.5 Comprehensive business-route audit + Tier 1–4 fixes (IN PROGRESS: 17 defects inventoried across transactions, categories, merchants, budgets, debt, savings, memorized, notifications, analytics; classified as URL-drift ×16, Shape-drift ×1, Feature-drift ×2; Tier 1 = 4 commits 9.5a–9.5d fixing smoke blockers; Tier 2 = 2 batch CRUD commits; Tier 3 = template-suggestions removal; Tier 4 = test hygiene; smoke after Tier 1, after Tier 2, final after Tier 4; **9.5a DONE** transactions.create URL fix + transactions.get shape fix; **9.5b DONE** transactions.update PATCH + transactions.delete DELETE URL/method drift; **9.5c DONE** spendingIntelligence surface removed entirely — analyticsApi method deleted, SpendingIntelligencePage + MerchantIntelligenceCard + SpendingIntelligencePage.test deleted, App.tsx routes removed, AppShell.tsx commented-out entry removed, CommandPalette entry removed, InsightsPage query/card removed; **9.5d DONE** dismissBudgetAlert use alert_key — URL + body shape (38579d0); **9.5e DONE** serializeTransaction snake_case fix (e278ce6); **9.5f DONE** analytics methods unwrap envelope via readApiData (13e14a3))
- 9.6 ProfilePage parity (DONE: GET /api/auth/profile implemented (a7f7154); profile fields from userProfiles table (39cc5e4); deleteAccount UI deferred to 9.7f)
- 9.7a CRUD update URL drift across 6 resource types: POST /:id/update → PATCH /:id (DONE: 4fc2ac7)
- 9.7b CRUD delete URL drift + readErrorCode fix: reads top-level `code` field — was phantom `error_code`, undefined for every Hono error since the port (DONE: 6b90db8)
- 9.7c POST /api/auth/profile/update — profile preferences write path; unblocks setup guide and debt onboarding (DONE: ce8f670)
- 9.7d Remove Flask-era password-change UI (DONE: 7ab611e)
- 9.7e Unwrap 2FA setup/verify response envelopes via readApiData — BACKUP_CODES_LOW toast and QR render were silently dead (DONE: cfc272f)
- migration 0003 hotfix: register is_tombstone in _journal.json — orphaned SQL invisible to drizzle-orm/migrator.js; column applied to prod; login audit rows confirmed (DONE: 030b930)
- 9.7f Remove Flask-era account-deletion UI; Hono OIDC re-auth flow deferred to TODO(account-deletion-ui) (DONE: ab4c09a)

**Phase 4 — Professionalization (post-launch hardening). Started 2026-07-03. Scoped from the 2026-07-03 audit's prioritized gap list. Same propose→approve→write discipline. Sequence: 10a → 10f → 10b → 10c → 10d → 10e → 11 (10f inserted before 10b so no new backend route ships through a pipeline whose api gate is a silent no-op).**
- 10a Contract-test layer: guarantee every frontend api.ts call resolves to a mounted Hono route; a future unmounted-route bug fails CI before deploy. Closes the class that produced the 5 Module-9 escapes and the analytics 404 window. (DONE: `apps/web/src/contract/capture.ts` spies on `global.fetch`, invokes every method on the covered api objects, and commits `apps/web/contract/frontend-calls.json`; `apps/web/src/contract/frontend-contract.test.ts` is the web meta+fixture guard (every exported method exercised; fixture matches, regenerate via `pnpm --filter statera-frontend contract:generate`); `apps/api/src/contract/frontend-contract.test.ts` asserts each `(method, path)` resolves to a mounted route via `app.request` — ground-truth 404-vs-not (unmounted `/api/*` → app.notFound 404; mounted routes hit requireAuth → 401 before any DB), so the check is DB-free. Known-unmounted-but-called routes sit in an explicit ALLOWLIST with per-entry 10b dispositions; a stale-allowlist test forces deletion of a line once 10b mounts the route or removes the UI caller. `bankApi`/`featuresApi` deliberately EXCLUDED from the capture net (dead code, removed in 10b — no point allowlisting surfaces about to be deleted). CI wired in the test job: `pnpm --filter statera-api exec vitest run src/contract`. MSW behavioral tests noted as a possible later layer, not part of 10a.)
- 10f API CI rehabilitation: the `apps/api` typecheck + test steps in `.github/workflows/deploy.yml` were **silent no-ops since written** — they used `--filter api`, but the package is `statera-api`, so the filter matched no project; only the frontend was ever gated. Sequenced before 10b so no new backend route ships through a pipeline with no api gate. (DONE: (1) deploy.yml filters corrected `api`→`statera-api` on both tsc and test steps, stray `run` arg dropped (`test run` → `vitest run run`), NOTE removed, standalone 10a contract step removed as subsumed by the full suite. (2) Triage refined the hypothesis: all 172 failures were in 9 `routes/*.test.ts` files; **zero** MySQL (3306) connections — DB is fully mocked — so they were NOT misplaced integration tests. 169 were Redis (6379) hangs to the 5s timeout from two unmocked ioredis entrypoints — `middleware/auth.getAuthRedis()` (requireAuth sv deny-list; dominant, hit by the 9 files using real `createSessionToken`/`requireAuth`) and `lib/analytics-cache.getRedisClient()` (cache-bust/read; secondary). Remedy = mock-completion, not gating: `src/test/redis-mock.setup.ts` mocks `ioredis` module-wide (plain-function stub, immune to the `vi.resetAllMocks()` in 6 files that would otherwise strip a `vi.fn` impl → `get()` returns undefined ≠ null → spurious "Session invalidated" 401), wired via `vitest.config.ts` `setupFiles` only when `INTEGRATION !== "true"` (worker.integration.test.ts needs real BullMQ). The remaining 3 run-1 "assertion" failures were runner pollution from the hanging promises; the hermetic run left exactly 1 genuine failure — `dashboard-bundle R8` date-drift (hardcoded `2026-05` vs current-month default), fixed by pinning `?month=2026-05`. (3) 39 tsc errors (35 `res.json()`→unknown in intelligence.test.ts, 4 `testClient`→unknown in notifications.test.ts because the router is a mutated `new Hono()` with no RPC schema) fixed with a `src/test/json.ts` `readJson()` typed-envelope helper + standardizing notifications on `router.request()`; no production change. (4) CI architecture: **unit-only gate** (Option 1) — the api suite is now hermetic (no service containers). Revisit trigger recorded: reopen MySQL/Redis service containers when a Phase 4 module ships behavior only integration tests exercise (e.g., CSV import transaction atomicity in 10b). Result: `pnpm --filter statera-api exec tsc --noEmit` = 0 errors; `pnpm --filter statera-api test` = 713 passed / 5 skipped / 0 failed, hermetic. Both red-run proofs demonstrated then reverted (broken assertion → suite red; deliberate tsc error → exit 1).)
- 10b Broken-route remediation (mixed disposition, per 2026-07-03 decision; each MOUNT is a Flask port with its own propose→approve cycle): (a) demo-workspace `/api/auth/demo-data[/clear]` → MOUNT (primary onboarding CTA; includes the demo-CTA UX fix); (b) CSV import `/api/transactions/upload-preview` + `/import-commit` → MOUNT (bulk entry is core; prerequisite mindset for Module 11); (c) CSV/XLSX export → REMOVE the ProfilePage buttons (backend 3c stays deferred; 10c's GDPR data-export partially covers; full export returns later as its own module); (d) template-suggestions → REMOVE the plumbing (Flask flagged it off "do not port" — the UI caller should never have existed); (e) featuresApi / bankApi / BankPage → REMOVE dead code (bank returns via the statement-parsing initiative, which will look different). No "reachable but broken" survivors (standing rule 9.5c). (IN PROGRESS: **10b-1 DONE** (removals c/d/e) — (c) ProfilePage CSV/XLSX export buttons + handler + state removed, `transactionsApi.exportCsv`/`exportXlsx` + the sole-consumer `downloadTransactionExport` helper deleted (grep confirmed exports were its only callers); (d) template-suggestions plumbing removed — `transactionsApi.templateSuggestions` + `templateSuggestionFeedback`, both `useTemplateSuggestions` hooks (transactions/helpers.ts + expenses/hooks.ts, both already had zero live consumers), the dead `applyTemplateToPreviewRow`/`applyTemplateToExpenseForm` helpers, and the `TransactionTemplateSuggestion`/`TransactionTemplateItem` types; `flags.template_suggestions` in /me handling kept (backend contract field, see TODO(module-9-feature-flags-audit)); (e) `featuresApi` const deleted (FeatureFlags interface kept — consumed by authApi.me + AuthContext), `bankApi` + `BankPage.tsx` + `BankPage.test.tsx` + 8 bank types deleted; contract fixture regenerated (4 entries dropped: export-csv, export-xlsx, transaction-template-suggestions, transaction-template-suggestions/feedback — the template plumbing is two routes, not one), 4 stale `10b-remove` allowlist lines pruned after a captured red-run named exactly those 4 and none of the demo/import mounts. Remaining in 10b: (a) demo-workspace MOUNT = 10b-2, (b) CSV import MOUNT = 10b-3.) **10b-2 DONE** (2026-07-04; mount (a) demo-workspace, Flask port of personal-finance `routes/auth.py:1085`/`:1148` + `lib/demo_data.py`): new `apps/api/src/lib/demo-data-lib.ts` (routes thin) reproduces `load_demo_workspace` (6-month, 81-transaction seed + 7 budgets + Starter Card debt + Emergency Buffer goal + profile income/payday/country defaults + `demo_workspace_manifest`/`demo_data_loaded` product events) and `clear_demo_workspace` (manifest-scoped deletes with source/name fallbacks + nulls only still-default profile fields); `POST /api/auth/demo-data` (requireAuth, `createRateLimiter(10,60)`) and `POST /api/auth/demo-data/clear` (requireAuth, `createRateLimiter(3,600)` — the existing middleware already keys per-user+per-path, reproducing Flask's `key_func`, so zero middleware extension) mounted on the auth router; error codes match Flask exactly (`demo_data_not_empty`/`demo_data_not_loaded` 409, `demo_data_load_failed`/`demo_data_clear_failed` 500). Whole seed/clear wrapped in one `db.transaction()` (all-or-nothing); INTEGRATION-gated atomicity test `routes/demo-data.integration.test.ts` (seed-all, post-load rollback proof, conflict guard, clear + memorized-survives). `_has_financial_data` omits `BankConnection` (operator-approved; bank deferred). Seeding replicates Flask's `learn_transaction` priming per non-dup row (memorized rows survive clear — see Module 11 note). Frontend needed no functional change (WorkspaceChoicePage ported in 9.3a) except a Category C fix: added the missing `return` in the `demo_data_not_empty` catch so no misleading error toast fires. Contract: 2 demo allowlist lines pruned after a captured red-run showed both routes flip 404→401 and the stale-allowlist test named exactly those 2 via "now mounted" (import entries untouched = 10b-3). Remaining: (b) CSV import MOUNT = 10b-3.) **10b-3 DONE — Module 10b COMPLETE** (2026-07-04; mount (b) CSV import, Flask port of personal-finance `routes/upload.py:994`/`:1162` + `lib/importer.py`): new `apps/api/src/lib/import-lib.ts` (parse/validate/plan/persist/savepoint/atomic/preview-hints) + thin `apps/api/src/routes/upload.ts` mounting `POST /api/transactions/upload-preview` + `/import-commit` (both requireAuth, `createRateLimiter(10,60)`). **preview→commit is a client round-trip** (upload-preview returns `preview_rows`+`file_hash`; import-commit takes them back — no server-side plan). **XLSX/CSV libs (deviation-by-necessity):** exceljs + papaparse; **`.xls` dropped** from ALLOWED_EXTS + magic-byte checks + `ImportDialogs` `accept` (exceljs has no `.xls`; returns via Module-11 statement-parsing). **Atomicity:** Flask `begin_nested()` per-row savepoint + outer rollback → Drizzle nested `tx.transaction()` (mysql2 `SAVEPOINT`/`ROLLBACK TO SAVEPOINT`, drizzle-orm@0.39.3 `mysql2/session.js:213-227`); per-row nested txns caught so one row's rollback doesn't poison the batch; atomic blocking throws out of the outer `db.transaction()`. **Dup layers** faithful (within-batch triplet, existing-DB triplet, `import_row_hash` idempotency via the unique index, fuzzy preview hints incl. a faithful `difflib.SequenceMatcher.ratio()` port). **Size limit** 12 MB enforced in-handler → 400 `FILE_TOO_LARGE` (deviation from Flask's framework-level 413). Amounts parse **string → Decimal → formatKd** (no parseFloat). Zod schema `.passthrough()` tolerates unknown fields (`atomic` accepted-but-ignored, Flask parity). Response shapes stay top-level + error extras spread top-level to match the already-wired `uploadApi`/`ImportDialogs`. **D1:** `getDemoWorkspaceState` exported from `demo-data-lib` + import-commit reuses the demo-replace guard (`demo_data_replace_required` 409 / `clear_demo_workspace` in-transaction). **D2:** `GET /api/auth/profile` (and POST `/profile/update`) now return the real `getDemoWorkspaceState()` (stale "not ported" comment deleted). **No message/iMessage plumbing** (personal_statera-only). Contract: 2 import allowlist lines pruned after a captured red-run (both flip 404→401, named via "now mounted") — **the 10b allowlist is now empty (zero entries): every frontend api.ts call resolves to a mounted route.** INTEGRATION-gated `routes/import.integration.test.ts` (6 cases: happy path, atomic precheck rollback, savepoint isolation + atomic apply rollback, idempotency, transaction_id update, demo-replace + memorized survives) — executed green against the live stack. **Module 10b (a demo + b import mounts; c/d/e removals) is complete.**
- 10c Legal/compliance: privacy policy + terms pages, GET /api/account/data-export (TODO(GDPR-data-export)), re-expose account deletion (TODO(account-deletion-ui)). Launch blockers for a public financial app. (IN PROGRESS: **10c-1 DONE** (2026-07-05) — GDPR right-to-access endpoint `GET /api/account/data-export` (requireAuth + `createRateLimiter(5, 3600)` inline) mounted on the account router; backend-only (no frontend surface yet — the download UI ships with the account-deletion re-expose, both bundled per TODO(account-deletion-ui)). New `apps/api/src/lib/data-export-lib.ts` `buildUserDataExport(db, userId)` — sync sequence of SELECTs whose scope mirrors the account-deletion purge (`lib/account-deletion.ts`) minus documented exclusions. **Export=purge with departures** (all in the file-top deviations block + echoed at runtime in `DATA_EXPORT_EXCLUSIONS` / `meta.excluded`): field-level omits `users.external_id` (classified auth-infra, grouped with `totp_secret`/`totp_backup_codes_json`/`session_version`) + `transactions.name_key`/`import_row_hash`; table-level omits `dashboard_snapshots` (derived cache), `account_action_tokens` (auth-infra), and `security_events` tombstone rows. **Included** (ruling 1): `security_events` (non-tombstone) + `product_events`. **Included-always-empty** (Phase A): `template_suggestion_feedback` IS a present export section (always `[]`, `meta.counts` 0) because the feature is not in Hono — present, not omitted, so the export is self-evidently complete. **Deliberate asymmetry** (ruling 5 bookkeeping): `memorized_transactions.norm` IS exported (derived-data transparency) though `transactions.name_key` is not (redundant with name). Read-consistency deviation recorded (non-transactional reads; revisit trigger = concurrent automated writer). Envelope `{ ok, data: <export>, error: null, meta: { counts, excluded } }`. Tests: `lib/data-export-lib.test.ts` (8 — full shape, secret-field negative assertion per ruling 3, empty-state per ruling 4, norm-present, tx-omission, not-found, exact-exclusions deep-equal) + 2 route tests in `account.test.ts` (envelope+meta pass-through, null→401). **Process misses caught in reconciliation (2026-07-05, recorded per the Module 9 stale-PENDING-labels precedent — the record of the slip is part of the discipline):** (i) the original close-out report claimed "all four CLAUDE.md updates" while substituting a different four (contract entry + TODO(GDPR-data-export) CLOSED in place of the agreed TODO(fuzzy-ratio-equivalence-fixture), TODO(memorized-cascade-decision), and 10c content-track facts note); (ii) the implementation flipped one Phase-A-approved disposition unflagged — `template_suggestion_feedback` was shipped as a table-level EXCLUDE instead of the approved INCLUDE-always-empty, and `meta.excluded` shipped enriched with field-level entries beyond the approved 3-entry skeleton (retroactively approved). Both reverted/recorded in the follow-up commits. Remaining in 10c: account-deletion re-expose UI.) **10c-2 DONE** (2026-07-05) — public (pre-auth) privacy-policy + terms static pages, English-only, **placeholder structure only** (no legal copy — that arrives via the operator-owned content track). New `apps/web/src/components/pages/legal/`: `LegalPageLayout.tsx` (shared standalone frame — NOT the auth-gated AppShell; wordmark + title + "Last updated" slot + back-to-sign-in `<Link>`; file header records that the Arabic fast-follow (RTL + language switch) lands in this layout), `PrivacyPolicyPage.tsx`, `TermsPage.tsx`. Routes `/privacy` + `/terms` added to `App.tsx` as **siblings of `/login`, outside `<ProtectedRoute>`** (public, no redirect); lazy-loaded via the existing `lazyWithRetry` pattern; served by Caddy's existing SPA `/*` fallback (no Hono route). Each page = section-heading scaffolds with visible `TODO: content pending operator review` markers (accepted-risk: site unannounced, content draft follows immediately; placeholder window deliberately short). Privacy's "Data handling" section carries the two legally load-bearing commitment slots as **plain sections with stable data-testids** (`commitment-backup-retention`, `commitment-statement-files`) per ruling 3 (no FactualCommitment component — extract later only if the content track reveals genuinely repeated structure); these will render the "10c content-track facts" (365d backup retention disclosed as-is; statement files never persisted). `LoginPage.tsx` gained a footer with Privacy/Terms `<Link>`s. Tests: `legal/PrivacyPolicyPage.test.tsx` (4 — public render, scaffold+pending markers, **both commitment slots present** = the durable no-silent-drop guarantee, back-to-sign-in) + `legal/TermsPage.test.tsx` (3) + a LoginPage footer-links case. Frontend `contract` fixture untouched (no new `api.ts` calls). **10c-3 candidate (recorded 2026-07-05, NOT 10c-2 work):** authenticated users should also be able to reach `/privacy` and `/terms` from inside the app (e.g. a ProfilePage or AppShell footer link) — propose the placement in the 10c-3 Phase A. Remaining in 10c: account-deletion re-expose UI (TODO(account-deletion-ui)), and the in-app legal links (10c-3 candidate).) **10c-3 DONE — Module 10c technical track COMPLETE** (2026-07-06; frontend-only, zero `apps/api` changes; account-deletion re-expose UI + data-export download UI + in-app legal links). New `accountApi` object in `apps/web/src/lib/api.ts` (separate from `authApi` because these hit `/api/account/*`, not `/api/auth/*`): `dataExport()` (raw `fetch` → `res.blob()` → object-URL + synthesized `<a download>` → revoke; NOT `apiFetch`, which would `res.json()` and lose the bytes; throws `ApiError` so 429 rate-limit surfaces) and `deleteAccount()` (`DELETE /api/account` via `apiFetch`; `task_id` deliberately ignored — see deletion-status decision). **Deletion flow** (backend-pinned redirect targets discovered in `routes/auth.ts`: no-TOTP callback → `/delete-account/confirm`; TOTP → `/auth/2fa-verify?intent=delete`; `/2fa/verify` delete branch returns JSON, no redirect): ProfilePage "Data & privacy" section → delete dialog offers **data export BEFORE the destructive step** (the only point with a live session — export requires auth; post-reauth there is none) then a plain styled `<a href="/api/auth/delete-reauth">` (full-document nav, LoginPage pattern; XHR can't drive the OIDC chain) → Google re-auth → new **standalone** `DeleteAccountConfirmPage` at `/delete-account/confirm` (route OUTSIDE `<ProtectedRoute>`, so no authenticated shell can flash) with **type-to-confirm the literal word `DELETE`** → `accountApi.deleteAccount()`. Error codes handled visibly: `DELETE_INTENT_GONE` (410, incl. the 15-min dawdle) → "verification expired" + Start over→/profile; `ACCOUNT_INACTIVE` (403) → already-deleted + sign-in; `deletion_failed` (500) → inline error + retry. **2FA detour:** `TwoFactorVerifyPage` reads `?intent=delete`, and `AuthContext.verifyTwoFactor(code, type, { deleteIntent })` skips `refreshUser()` on the delete path (no session issued → `/me` would 401) and routes to `/delete-account/confirm`. **Post-deletion teardown:** new network-free `AuthContext.resetAuthState()` (setUser(null) + default flags + `queryClient.clear()`; NOT `logout()` which would POST an already-cleared session) → `navigate("/login?deleted=1", {replace})`; `LoginPage` renders "Your account has been deleted." when `?deleted=1`. **In-app legal links** in the same Data & privacy section (Privacy→/privacy, Terms→/terms). **deletion-status decision (recorded, as argued):** the client does NOT poll `GET /api/account/deletion-status/:taskToken` and NO `accountApi.deletionStatus` method exists — DELETE success is terminal (session cleared → redirect to /login); an uncalled method would violate the same no-caller=no-method rule that deferred `dataExport` out of 10c-1. **Latent bug found & avoided (not fixed — out of scope):** the shared `Button` component's `asChild` path destructures `children` and never passes it into `<Slot>`, so `<Button asChild>` renders nothing; the delete-reauth link uses a plain `buttonVariants`-styled `<a>` instead (the codebase's existing OIDC-nav pattern, cf. LoginPage). **Contract:** `accountApi` added to the exercised set in `capture.ts`; fixture 73→75 (+`DELETE /api/account`, +`GET /api/account/data-export`, both already-mounted so ALLOWLIST stays empty). **Future candidate (recorded 2026-07-06, NOT 10c work):** a global AppShell footer for legal links (reachable from every page) — natural home is the Arabic fast-follow when the legal surface reopens (RTL + language switch land in `LegalPageLayout`); 10c-3 placed the links in the ProfilePage section instead, to avoid new global layout surface. **Deployment status: 10c-1/10c-2/10c-3 DEPLOYED** — `main` was fast-forwarded to c6012e6 and deployed green 2026-07-06 (GitHub Actions run 28803984453; live `/healthz` confirmed). **10c-content DONE — Module 10c COMPLETE** (2026-07-06; frontend-only, zero `apps/api` changes; contract fixture unchanged at 75, allowlist empty): the `/privacy` + `/terms` placeholder scaffolds (10c-2) replaced with the final operator-approved legal copy (Privacy Policy 11 sections, Terms of Service 11 sections), effective date "6 July 2026" (= commit date) passed via `LegalPageLayout`'s `lastUpdated` prop on both pages. The two testid-pinned commitment slots now carry CONTENT-asserting tests (not just presence): `commitment-backup-retention` (Privacy §7) asserts `365` + the re-deletion-on-restore promise (`/re-apply all account deletions/`); `commitment-statement-files` (Privacy §4) asserts `/never stored/`. The "content pending operator review" pending markers + `PlaceholderMarker` helper were removed from both pages (and the stale scaffolding language dropped from `LegalPageLayout`'s header comment, preserving the Arabic fast-follow pointer); the tests that asserted marker presence now assert the markers are GONE (`queryAllByText(...).toHaveLength(0)`). Test count unchanged (166 / 36 — content-fill only, no `it` blocks added/removed). **Lawyer review remains an open PRE-ANNOUNCEMENT gate** (not pre-launch — the text ships now): a Kuwait-qualified lawyer must review Privacy §1 (E-Transactions Law consent), Privacy §7 (re-deletion wording), Terms §3 (age 18), Terms §7 (liability limitation), Terms §9 (governing law), and all factual claims before Statera is publicly announced — tracked in `docs/legal/lawyer-review-checklist.md`.
  - **10c content-track facts (recorded 2026-07-05, for the privacy-policy / terms copy in 10c-2+):** two disclosures the legal pages must state accurately. (a) **R2 backup retention is 365 days at the monthly tier** (`statera-prod-db-backups` lifecycle: daily/14d + weekly/56d + monthly/365d, per the 8f-1 entry) — the privacy policy discloses this retention window as-is; it is NOT to be shortened or rounded down for copy convenience. A user's data therefore persists in encrypted backups for up to a year after deletion, which must be stated. (b) **Uploaded statement files are never persisted** — the 10b-3 CSV/XLSX import is parse-and-discard: the upload is parsed in-handler, `file_hash` is computed for the preview→commit round-trip, and the file bytes are never written to disk or object storage (see `lib/import-lib.ts`). The privacy policy may state that imported files are processed in-memory and not retained; only the derived transaction rows persist.
- 10d Ops hardening: uptime monitoring (8f-3 — **DONE 2026-07-09**), restore drill (8f-2 — **DONE 2026-07-08**), CSP Report-Only → enforcing (**DONE 2026-07-10 — enforcing live + post-flip walk GREEN; TODO(module-8e-csp-enforcement) CLOSED**), rate-limit backfill on budgets/categories/merchants/notifications (TODO(phase4-rate-limit-backfill) — **DONE 2026-07-10**), Zod adoption backfill (TODO(phase4-zod-adoption) — **B0 DONE 2026-07-11**, **B1 DONE 2026-07-11**, **B2-1 DONE 2026-07-18** (analytics month zod ×5 + intelligence `days` zod; B2-2/B2-3 pending), B3 dispositioned at B2 close; re-partitioned over surviving routers only — see `docs/modules/phase4-zod-adoption-b2-approvals.md`), Docker log retention cap, **TODO(memorized-prune-rule-unification)**. (IN PROGRESS — 8f-2 + 8f-3 + CSP-enforcement + rate-limit-backfill all closed; the Zod adoption backfill is underway (B0 = shared `zodErrorToEnvelope()` helper + 6 already-zod sites converged; B1 = NEW shape-only zod schemas on budgets + notifications, byte-identical wire strings); the remaining 10d items are the Docker log cap and the memorized-prune-rule unification. **B1 scope-narrowing (operator decision 2026-07-11):** debt-accounts + savings-goals routers were EXCLUDED from B1 (originally in scope) — those features are slated for removal under a separate upcoming module, so their hand-rolled validation is left untouched (no converge, no zod) until that module handles them; B2 re-partitions the zod backfill over surviving routers only (auth → B3) — the debt/savings B2 that this line described is void, those routers were deleted in phase4 scope-narrowing (see `docs/modules/phase4-zod-adoption-b2-approvals.md`; B2 Phase A approved 2026-07-18). **RESOLVED (phase4 SC-1/2, 2026-07-16): the removal was NOT reversed — the debt-accounts + savings-goals routers were deleted, so the deferred B2 zod work on them is moot and closed. No zod backfill is owed for those routers.**)
  - **10d-0 DONE — deletion/re-login remediation** (2026-07-07; one sub-commit, three coupled fixes + two docs corrections; live defect — operator was locked out after deleting their own account against production). **(a) Purge revokes all sessions + clears TOTP:** `purgeUserAccountRows` (`lib/account-deletion.ts`) now reads the pre-purge `sessionVersion`, and the final soft-delete UPDATE bumps it (`oldSv+1`) AND nulls `totpSecret`/`totpEnabled`/`totpBackupCodesJson` (aligns purge with the published Privacy §8 immediate-removal claim); it returns `{ revokedSv }`. The Redis `sv_revoked:{userId}:{oldSv}` deny-list write is done by the two callers (`worker/jobs/delete-account-job.ts` + `routes/account.ts` sync fallback) **AFTER the tx commits** — deliberately outside the transaction, because a rollback with the key already written would hard-lock a still-active user for the 30-day key TTL. Both caller revokes are try/catch + Sentry (defense-in-depth must not fail the deletion). The DB bump is what lets reactivation (b) issue a non-self-denied token. **Mid-purge login race accepted** (documented in the file-top deviations block; not mitigated — small user base, sync fallback revokes inline). **(b) Reactivate-as-fresh (deliberate deviation from Flask — neither checkout ever reactivates; both reject inactive with `login.failed`/`auth_account_disabled` 403):** the OIDC callback's inactive branch (`routes/auth.ts`) now flips `isActive=true`, refreshes email/displayName from claims, **also nulls the three TOTP fields** (idempotent post-(a); heals legacy stubs purged before this fix so their 2nd+ logins don't hit the 2FA gate against a deleted-era secret), re-emits `signup_completed`, audits `account.reactivated`, and routes through the new-user redirect `/welcome?source=signup`. `sessionVersion` read as-is (already bumped by the preceding purge). No frontend change (WorkspaceChoicePage gate ported in 9.3a); no `userProfiles` insert (mirrors new-user branch — the purge deleted it and `/me`+`/profile` are null-tolerant of a missing profile row). **(c) Audit hygiene on the surviving rejection path:** the `/2fa/verify` inactive check (reachable via a pending_2fa cookie issued pre-deletion, completed post-purge) keeps its compliant `ACCOUNT_INACTIVE` envelope and now also emits `login.failed`/`account_disabled` (XHR endpoint — JSON is correct, no redirect-param). **Tests:** unit — purge sequence+return (`account-deletion.test.ts`), purge sessionVersion-bump+TOTP-null, reactivation branch (`auth.callback.test.ts`, new), `/2fa/verify` inactive envelope+audit (`auth.2fa-verify.test.ts`), caller revoke assertions (`account.test.ts` sync path + `delete-account-job.test.ts`, new); integration — committed-purge asserts bump+TOTP-null+soft-delete (`account-deletion.integration.test.ts`). **Verification:** tsc 0 errors; hermetic `pnpm --filter statera-api test` = 729 passed / 16 skipped / exit 0, no Unhandled Errors section; **INTEGRATION=true full suite (live Docker stack, 10d-0a touches a `db.transaction()` boundary per cadence rule) = 50 files / 745 passed / 0 failed / exit 0** (dev DB was behind — migration 0003 `is_tombstone` applied host-pointed before the run; not a code change). **Docs corrections folded in:** 8e health-path fix-forward corrected (`/health` is NOT Caddy-proxied — falls through to SPA; ops probes are `/healthz`+`/readyz` only); 8f-2 restore-drill re-purge gains the timestamp rule (re-purge only when the tombstone post-dates the backup, so a delete-then-reactivate user isn't wrongly re-purged). **Production verification PASSED (2026-07-07, deployed a8b1ac4, run 28876280437 success, live `/healthz`+`/readyz` both report a8b1ac4):** operator logged in with the previously-deleted Google account → landed on `/welcome?source=signup` as a fresh account (reactivation + legacy-stub TOTP heal exercised); second login → dashboard with NO 2FA prompt (amendment 3 confirmed against a pre-fix stub); account verified empty. **10d-0 CLOSED.**
  - **8f-2 restore drill — EXECUTED 2026-07-08, ALL STAGES PASS, CLOSED** (Phase A approved 2026-07-07 with 4 amendments; scripts reviewed + approved 2026-07-07 with 2 amendments; drill run + close-out committed 2026-07-08 — execution record in `docs/runbooks/backups.md §8f-2`, handoff `docs/recovery/2026-07-08-8f2-restore-drill.md`; three post-Phase-A script amendments landed during execution: `765ed6f` full-prefix-path, `dde1dfb` TCP init-race probe, and the close-out cosmetics/date/DB-name corrections). Deliverables (scripts sub-commit): `apps/api/src/lib/restore-repurge-lib.ts` (timestamp-gated tombstone match + scratch-DB scenario runners; reuses tested `purgeUserAccountRows`/`hashEmail`; DRILL-ONLY — `openScratchDb` refuses any non-127.0.0.1 URL; NO `revokeSessionVersion` — scratch has no sessions) + `restore-repurge-lib.test.ts` (6 hermetic gate cases); `deploy/restore-repurge.ts` (thin CLI, `--mode fixture|repurge`, `--expect` enforces the known-answer); `deploy/restore-drill.sh` (Stage 0 pull+decrypt+`-- Dump completed` truncation guard; Stage 1 isolated `mysql:8.0.41` scratch container on 127.0.0.1:3307, **declared 21-table exact assertion both directions [A2]**, row-count manifest, FK-integrity probes, regex-validated optional anchor; `--verify-only` monthly decrypt-check; resource guard [A4] before any container; PII shredded on every exit; `--teardown`); `docs/runbooks/backups.md §8f-2` filled (procedure + **incident variant with the post-purge `revokeSessionVersion` step [A3]** + execution-record template; run-sheet hardenings folded in 2026-07-07 — three gaps of one class at the operator-run seam, where `restore-drill.sh` self-sources secrets via sops but an interactive shell does not: (1) manual `rclone` object-listing needs `RCLONE_CONFIG_R2_*` + R2 creds sourced (backup-db.sh temp-file/non-evaluating pattern); (2) `MYSQL_ROOT_PASSWORD` sops-sourced into an env var before the read-only tombstone export + `unset` in teardown; (3) a UTC-consistency check on `T_backup` before Stage 3). **Gate boundary = `>=`** (2026-07-07 review, supersedes Phase-A strict `>`): at exact equality restored rows can only be pre-deletion, so equality re-purges; strictly-before does NOT (reactivation-before-backup protection). **A1 known-answer (as executed):** the full drill used a `weekly/` object (`2026-05-31T21:13:56Z`, the only object pre-dating the operator's real deletion at 2026-07-07 14:13:42 UTC — the daily timer was dead until 2026-07-08, see F1), so Stage 3 found exactly ONE tombstone match (operator's `email_hash`); 0 or ≥2 = failure. **Verification:** tsc 0; hermetic api suite 735 passed / 16 skipped / exit 0 (no Unhandled Errors); deploy CLI targeted tsc 0; `bash -n` OK; **run-sheet Step 0 shellcheck PASSED 2026-07-07 (clean, no output)**. **Runtime bug caught during write:** fixture insert referenced a non-existent `categories.name_key` column — fixed to `(user_id, name)` after checking `information_schema`. **Done: operator run-sheet executed 2026-07-08** (Step 0 shellcheck PASSED → Stage 0/1 → Stage 2 → Stage 3 known-answer → teardown → execution record + `docs/recovery/2026-07-08-8f2-restore-drill.md` handoff), closing 8f-1 AND 8f-2 together. The drill touched production read-only exactly once (the Stage-3 tombstone export); it never restored into, or wrote to, the live DB or R2.
  - **TODO(memorized-prune-rule-unification)** (recorded 2026-07-04, recon): the inline prune (`routes/memorized.ts:75`, count-tiered rule `count==1 & <90d OR count==2 & <180d` + `is_pinned`, matching personal_statera) and the batch job (`worker/jobs/maintenance-jobs.ts`, age-OR-lowcount rule `last_seen<180d OR (count<=1 & <90d)` + `is_pinned`, matching personal-finance) apply **different retention policies to the same `memorized_transactions` table**. Record only; the fix is its own propose→approve cycle under 10d.
  - **CI service-container decision (10b-3, 2026-07-04): CI stays hermetic (unit-only gate); MySQL/Redis service containers are NOT reopened.** Rationale: the recon's original revisit trigger ("integration tests have never executed") is resolved — the INTEGRATION suite is now proven runnable and green (47 files / 728 passed, executed 2026-07-04 for 10b-3), so it is a real, exercised gate rather than a paper one; 10b-3's atomicity/dup/savepoint behavior is covered by `import.integration.test.ts` against real MySQL, run locally pre-release; adding service containers would triple CI time and reintroduce the flakiness the 10f hermetic decision removed. The gap was cadence, not capability — addressed by the cadence line below.
  - **Integration-suite execution cadence (10b-3, 2026-07-04):** the full `INTEGRATION=true` suite (live Docker stack) must run **before any commit that touches a `db.transaction()` boundary or adds/edits integration cases**, and that commit's verification report must record the exact command's exit code. Two never-executed tests found on 2026-07-04 (the 10b-2 pre-Phase-A gate surfaced the account-deletion mock-contamination silent-rot; the 7.5 fix-forward proved convention-without-cadence rots silently) establish this discipline. A formal pre-release checklist item is 10d scope.
  - **TODO(fuzzy-ratio-equivalence-fixture)** (recorded 2026-07-05, carried from 10b-3): the 10b-3 import preview-hint fuzzy matcher ports Python's `difflib.SequenceMatcher.ratio()` by hand (`lib/import-lib.ts`), but there is **no fixture-based equivalence test** pinning the TS port against captured Python `ratio()` output. Every other non-trivial Flask calculation carries such a fixture (test convention). Add a captured-Python fixture (a set of `(a, b) → ratio` pairs generated from CPython's `difflib`) and assert the TS port matches, so a future refactor of the ratio math can't silently drift from the Flask semantics. Its own propose→approve cycle under 10d.
- 10e Email magic-link authentication: magic-link as a second auth path alongside OIDC. NO password column, ever (passkeys as a later addition). Account-linking policy = link by verified email — a magic-link request for an email already bound to a Google identity attaches to the existing user and never creates a second account. This amends but does not overturn the "no password column" architectural decision. Full design (magic_link_tokens schema, session-issuance reuse of createSessionToken, the totpEnabled 2FA gate replicated from the OIDC callback, dedicated rate limiting) goes through its own propose→approve cycle. (NOT STARTED)
- 11 Frictionless transaction logging (major initiative): builds on 10a's safety net and the learnTransaction/suggestTransactions engine. Absorbs the onboarding/empty-state groundwork — suggestion cold-start (learnTransaction has no priming; new users get zero suggestions) and seed categories for empty users. Scoped separately. **Input from 10b-2 (2026-07-04): loading the demo workspace primes `memorized_transactions` via `learnTransaction` per seeded row (faithful to Flask `create_transaction_with_dup_check`), and `clear_demo_workspace` does NOT remove those memorized rows — verified against both Flask checkouts (no `MemorizedTransaction` reference in either `lib/demo_data.py` clear path). So a user who loads then clears the demo keeps demo-derived suggestions. This is the one existing form of suggestion priming; Module 11's suggestion-quality design must decide whether that is desirable (demo merchants like "Careem"/"Lulu" surfacing for a user who cleared the demo) or whether clear should also prune the primed rows.** **TODO(memorized-cascade-decision)** (recorded 2026-07-05, carried from 10b-3): formalizes that open question — decide, as part of Module 11's suggestion-quality design, whether `clear_demo_workspace` (and, more generally, deleting the transactions that primed a memorized row) should cascade-prune the derived `memorized_transactions` rows, or whether primed suggestions are intentionally sticky. Current behavior: no cascade — memorized rows survive clear and survive transaction deletion (the FK is `onDelete: set null` on category/merchant only, never on the memorized row itself). Its own propose→approve cycle under Module 11. **Queue note (B2-R8, 2026-07-18):** Module 11 re-evaluates transaction-input validation (create/update/split/dup-check) as part of its own charter; these routes were affirmed hand-rolled-by-design under Decision 2 at B2 Phase A (B2-R8, 2026-07-18) — any zod adoption there is Module 11 scope, proposed fresh, not inherited from 10d. (NOT STARTED)

**Phase 4 — Scope narrowing (SC-1/2/3): remove the debt-accounts + savings-goals features. — COMPLETE (module CLOSED 2026-07-17).** Approved lineage in `docs/modules/phase4-scope-narrowing-approvals.md` (rulings a–f 2026-07-14, SC-0-waived amendment 2026-07-15, conditions C1–C9, §8 verbatim, 3 approved legal-copy strings, addendum A1–A6, SC-3 approval R1/R2/S1–S5). Two-deploy sequence (ruling a; approved additive-only-rule deviation, this module only). **DEPLOYED both deploys:** deploy 1 = SC-1/2 code removal (`0f27745`, deployed 2026-07-17, run `29546090623` green, probes SHA-matched, operator smoke PASS + C6 worker recreate observed); deploy 2 = SC-3 schema DROPs (`88a157f`, deployed 2026-07-17, run `29592560536` green, migrate applied 0004→0006, probes SHA-matched). **Operator on-box post-drop verification PASSED 2026-07-17:** `SHOW TABLES LIKE 'savings_goals'` / `'debt_accounts'` → Empty set; `DESCRIBE user_profiles` → 10 columns, no `has_debt_choice`; `docker compose ps` → api/web/worker recreated at `88a157f`, mysql/redis untouched. **Two fix-forwards carried out of this module, each its own future cycle:** `TODO(integration-rate-limit-test-isolation)` (10d) and **`TODO(module-9-contract-validation)` — C2 typed-drift:** the frontend `SnapshotResponse` type declares 12 KWD fields as `number` while R13 returns 3-decimal strings — 3 `net_position` (`income_total_kd`/`expense_total_kd`/`net_kd`) + 9 `SnapshotCashFlowWindow` (`{30d,60d,90d}` × `{income_kd,expense_kd,net_kd}`); types are wrong, runtime is string; recorded 2026-07-16, NOT fixed here (own cycle; strengthens the case for a shared frontend/backend contract check).
- **SC-1/2 — atomic backend + frontend removal + SC-legal rider (2026-07-16, PRE-COMMIT at close-out):** BACKEND — deleted `routes/debt.ts`, `routes/savings-goals.ts`, `lib/debt-calculator.ts`, `lib/savings-goals-lib.ts`, `worker/jobs/goal-milestone-email-job.ts` (+ tests); unmounted both routers in `app.ts`; removed `send-goal-milestone-email` from the `JobName` union + `jobHandlers` registry (C6) and the `goal_milestone` email template; purge (`lib/account-deletion.ts`) 13→11 tables; data-export (`lib/data-export-lib.ts`) debt/savings SECTIONS removed + `has_debt_choice` field removed, `DATA_EXPORT_EXCLUSIONS` untouched (C5); R9 safe-to-spend committed=budget-only + debt/savings fields+warnings removed (ruling d); R8 dashboard-bundle `debt_summary` removed; R13 snapshot `total_debt_kd`/`total_savings_kd` removed (ruling c); `OWNED_TABLES` (restore-repurge-lib) −2; demo-data-lib (C9) stops seeding/clearing debt/savings, manifest keys no longer written/read (legacy `debt_account_ids`/`savings_goal_ids` tolerated on read + clear guard). FRONTEND (§8) — deleted `debtApi`/`goalsApi` + debt/savings types, `PayoffPlanPanel`/`DebtAccountsSection`/`GoalsTab`/`DebtDialog`/`PlanSummaryPanel` (+ tests); Plan page collapsed to a single Budget view, no vestigial tab bar (ruling e); Dashboard debt-summary card + snapshot debt/savings KPIs + safe-to-spend debt/savings blocks removed; CommandPalette Plan copy/keywords; contract fixture regenerated (allowlist stays empty). LEGAL — 3 approved copy strings applied (Privacy §3 "Financial data you enter"; Terms §2 "What Statera is" + "not advice"), no named-regression test-file edits. **Addendum §8 audit (2026-07-16, review-channel A1–A6) surfaced and fixed additional coupling the first pass missed:** (i) `routes/categories.ts` savings-goals coupling — the category delete/remap `linkedCategoryId` relink + the `dependent_counts.goals` / remap `goal_count` were removed (client-observable contract change; frontend `CategoryDependentCounts.goals` + `CategoryRemapResult.goal_count` + `SettingsDialog` counts.goals render removed to match); (ii) `routes/auth.ts` profile GET/POST `has_debt_choice` read+write path removed (REQUIRED so the SC-3 `has_debt_choice` column DROP is safe — ruling a "code stops reading/writing") + `UserProfile.has_debt_choice` frontend type + api.ts payload field (ruling b); (iii) stale `["debt-accounts-summary"]`/`["savings-goals"]` query-key invalidations pruned from `TransactionsPage`, `QuickAddContext`, `DashboardPage`; (iv) `api.test.ts` dashboard-bundle fixture + `error-recovery.test.ts` "goal" case updated. **C4 ImportDialogs rider FOLDED IN (option c, 2026-07-16):** ImportDialogs.tsx was found CLEAN (design change landed, last commit 53ce85b), so per the contingent ruling the demo-copy was fixed here, the two OPTIONAL `DemoWorkspaceState.debt_accounts`/`savings_goals` shim fields were removed, and `import-dialogs.test.tsx` updated — no deferred rider remains. **A6 proved the INTEGRATION 4-failure set (rate-limit/zod in budgets/categories/merchants) is pre-existing** (identical 4 on clean HEAD with flushed Redis) → `TODO(integration-rate-limit-test-isolation)`, own cycle under 10d, not fixed here.
- **SC-3 — schema DROP migrations (deploy 2; COMMITTED 2026-07-17, deploy-2 push HELD for operator go):** three one-DDL-per-file migrations generated via 3 sequential `drizzle-kit generate` calls (barrel-index approach: `drizzle.config` reads `schema/index.ts`, so each re-export was removed one at a time to force a single DDL per migration) — `0004_plain_omega_flight` = `` DROP TABLE `savings_goals` ``, `0005_parallel_the_watchers` = `` DROP TABLE `debt_accounts` ``, `0006_illegal_rachel_grey` = `` ALTER TABLE `user_profiles` DROP COLUMN `has_debt_choice` `` (ruling a/b; R2 order). Both tables are leaf (zero inbound FKs). Each migration's second `generate` reported "No schema changes" (S1); schema files `debt-accounts.ts`/`savings-goals.ts` deleted + `index.ts` re-exports removed + `hasDebtChoice` removed from `users.ts`; api+web `tsc --noEmit` both 0 after deletions (S2, proves no surviving import of the deleted exports); hermetic api suite unchanged 634/49 exit 0 (S3). `deploy/restore-drill.sh` `DECLARED_TABLES` manifest reduced 21→19 (R1 option a) + the three-fact §8f-2 runbook note (pre-SC-3 backups carry 21 tables/expected-not-defect; restore self-heals via next `migrate` re-dropping 0004–0006; that re-drop also erases legacy debt/savings rows of deleted users, preserving the Privacy §7 re-deletion commitment the 11-table re-purge no longer covers) — all in the SC-3 commit (S4). **Deploy 2 is DESTRUCTIVE (no code-rollback; recovery = R2 restore).** S5 (amended non-gating): a pre-drop backup is RECOMMENDED not required; the nightly R2 backup is the accepted recovery point (worst case ~24h single-user data). Deploy-2 push STOPS for operator go per the SC-3 approval.

**Deferred indefinitely:**
- Bank sync (CBK Open Banking dependency; replaced future scope: bank statement PDF parsing for Kuwaiti banks)
  - NOTE (2026-07-03 audit → 10b): dead frontend `bankApi` + unrouted `BankPage.tsx` **removed in 10b-1** (`bankApi`, `BankPage.tsx`, `BankPage.test.tsx`, and the 8 bank types deleted). Bank returns via the statement-parsing initiative, which will look different anyway.
- Exports (3c)
  - NOTE (2026-07-03 audit → 10b): exportCsv/exportXlsx ProfilePage buttons **removed in 10b-1** (buttons + handler + state, `transactionsApi.exportCsv`/`exportXlsx`, and the `downloadTransactionExport` helper deleted); backend 3c stays deferred. 10c's GDPR data-export partially covers the need; a full transaction export returns later as its own module.
- sendTemplatedEmail (until first caller module) — now implemented in 6c
- transaction-template-suggestions (feature-flagged off in Flask; do not port)
  - NOTE (2026-07-03 audit → 10b): the frontend plumbing (`useTemplateSuggestions` in transactions/expenses hooks + `transactionsApi.templateSuggestions`/`templateSuggestionFeedback` + the dead `applyTemplateTo*` helpers + `TransactionTemplateSuggestion`/`TransactionTemplateItem` types) **removed in 10b-1**. This never-ported route had two paths (suggestions + feedback), both dropped from the contract fixture/allowlist.
- `rebuild_memorized_from_transactions` (Flask `lib/suggestions.py`): data-recovery utility that replays a user's transactions through `learn_transaction` to rebuild memorized rows after a prune. **Not ported to Hono** (no equivalent; noted during the 2026-07-04 recon). No user-facing surface; if ever needed, the 2026-07-04 parity table is the map. Not a defect — recorded so a future reader doesn't assume it was missed.
- spendingIntelligence / SpendingIntelligencePage (9.5c): Flask composite endpoint returning top_merchants + category_benchmarks + category_deltas + recurring_bills was never ported to Hono. Surface fully removed in 9.5c — analyticsApi.spendingIntelligence deleted, SpendingIntelligencePage.tsx + MerchantIntelligenceCard.tsx + SpendingIntelligencePage.test.tsx deleted, App.tsx routes removed, CommandPalette entry removed. **Standing rule (9.5c):** A deferred route must have no UI entry point. "Deferred and reachable but broken" is not an acceptable state.

## Module fix-forwards

Fixes shipped after the original module commit, capturing real-world deployment findings.

**budgets-crash typed-drift — profile_context money fields are strings, typed number (2026-07-10)** `Plan page crashed to the error boundary: budget_to_income_pct arrives as the string "45.5" but was typed number|null, so budgetPct.toFixed(1) threw "toFixed is not a function"`
1. **Root cause (typed-drift, cf. 9.1 income_source):** the Hono budgets serializer builds `profile_context` KWD/decimal fields as **strings** via `Decimal.toFixed` (`routes/budgets.ts:114-123` — `budget_total_kd`, `monthly_income_kd`, `budget_to_income_pct`), consistent with the project's money-as-string convention. The frontend typed all three as `number | null` (`budget/sections.tsx` `BudgetProfileContext` + `types/api.ts`). **Two lies compounded:** the `monthly_income_kd` string (`"1500.000"`) is `!== null`, defeating `IncomePlanningCard`'s `if (monthlyIncome === null) return` early-out, so the card renders; then `budget_to_income_pct` (`"45.5"`) hit `.toFixed(1)` → `TypeError: n.toFixed is not a function` (a **string** receiver — `.toFixed` is undefined, not "cannot read of null"). **Trigger:** any account/month with **positive resolved income** (`budget_to_income_pct` is non-null only when `incomeKd !== null && !isZero`) viewing Plan — NOT empty-state-specific; latent since the Phase-3 port, surfaced when an income-bearing account first reached the Plan page (found during the CSP pre-flip walk 2026-07-10).
2. **Falsified hypothesis (recorded so the record shows the correction):** the first triage's leading hypothesis was `formatKD(null)` on the Goals tab. The stack falsified it — `.toFixed(1)` is one-decimal (neither `formatKD`→`toLocaleString` nor `fmt3`→`toFixed(3)`), and "is not a function" is a string receiver, not null. Re-triage with the stack pinned `budgetPct.toFixed(1)`. The lesson of the miss: **the first pass trusted the frontend type annotations as ground truth for runtime shapes.**
3. **Fix:** corrected the frontend types to `string`/`string | null` (both `BudgetProfileContext` and `types/api.ts`), which made `tsc` surface **exactly** the three number-assuming sites (two tone comparisons + the `.toFixed`), then coerced with `Number(...)` at each. Plus **whole-class hardening** (previewed in triage, shipped though it wasn't today's crash): `formatKD` + `fmt3` made null-tolerant (param widened to `null | undefined`, explicit `=== null/undefined` guard — `isNaN(null) === false` lets null slip a bare guard and crash `.toLocaleString`/`.toFixed`). **Regression proven RED-first:** `budget/sections.test.tsx` renders the **real** `IncomePlanningCard` with a **string-valued** `profile_context` fixture and asserts the card renders `"45.5%"` (presence + content, since the guard-defeat makes the card's rendering part of the contract) — failed with the exact `budgetPct.toFixed is not a function` against the unfixed component, then green after the fix. `BudgetPage.test.tsx` adds a both-tabs empty-account render.
4. **TODO(module-9-contract-validation) link + audit lesson (verbatim):** frontend type annotations verified against backend serializers would have caught **both** lies at audit time. **When auditing a render path for type safety, treat frontend types as claims to verify against the backend serializer — especially money/decimal fields, which this codebase returns as strings.** **Record (not action):** this incident is a concrete argument for **bumping TODO(module-9-contract-validation)'s priority** — a shared-type or response-shape contract check would mechanically catch this typed-drift class (it also produced 9.1 and the 9.4 stub).

**F1 · 8f-1 — backup timer committed but never installed; production unprotected 37 days (2026-07-08)** `systemd timer unit committed in 8f-1 but never copied to /etc/systemd/system — commit ≠ activation; the R2 bucket held exactly one object, discovered only by the 8f-2 drill's object listing`
1. **Root cause**: the `statera-backup.{service,timer}` units were committed in 8f-1 but never installed/enabled on the server. The bucket held exactly ONE object — the 8f-1 smoke-run weekly of `2026-05-31` — with zero daily and zero monthly; `systemctl status statera-backup.timer` reported the unit "could not be found"; zero journal history.
2. **Window/severity**: 37 days since the last (and only) backup at discovery on 2026-07-08, and only 19 days short of that lone object's 56-day weekly-tier lifecycle expiry — after which the app would have had ZERO backups.
3. **Remediation (same-day, root via Hetzner VGA console)**: units copied to `/etc/systemd/system`, `daemon-reload`, `enable --now`, a manual run fired; the fresh daily (`2026-07-08T15:23:03Z`) landed and was content-verified by the drill's Stage 0. Timer verified running.
4. **Durable lessons**: (i) an "installed artifact" claim needs an activation-verification step — "committed" ≠ "running", the same silent-rot class as the 8e lesson ("an artifact documented as existing needs an active verification step that confirms it works"); (ii) this re-prioritizes 8f-3 — a dead-man healthcheck ping on the backup timer catches this class on day 1, not day 37. **8f-3 was NEXT in the 10d queue by explicit decision; delivered and DONE 2026-07-09 (see the 8f-3 entry — the induced dead-man DOWN test proves the exact "day 1, not day 37" guarantee this lesson demanded).**

**F2 · 8f-2 drill — deploy-user sudo escalation untested; password lost; VGA-console recovery (2026-07-08)** `root-requiring drill steps had no tested escalation; deploy password lost; a first recovery attempt wrote an invalid sudoers file that made sudo worse until removed`
1. **Root cause**: the drill's root steps (systemd unit install) had no tested escalation. The deploy password set at the 2026-05-15 bootstrap was lost, and a first VGA-console recovery attempt wrote a syntactically invalid `/etc/sudoers.d/deploy` (a `NOPASSWORD` typo + two commands concatenated on one line, typed into the paste-less VGA console), which broke sudo for that rule until removed.
2. **Final state**: `/etc/sudoers.d/deploy` = `deploy ALL=(ALL) NOPASSWD:ALL`, validated with `visudo -c`, verified from a normal SSH session.
3. **Durable lessons**: (i) operational runbooks that require root MUST state the escalation path, and it must be tested before it's needed; (ii) any `sudoers.d` edit MUST be followed by `visudo -c` before leaving the root session — a syntax error there fails closed for the rule and can lock out escalation; (iii) VGA-console typing is error-prone (no paste) — prefer single short commands, echo-verify each before the next (cf. the 2026-05-22 operator-key-rotation VGA lessons).

**F3 · 8f-2 drill — runner assumed a host Node toolchain the server deliberately lacks (2026-07-08)** `pnpm exec tsx invocations in the runbook + the script's printed hints fail on the server — Node runs only inside Docker images there by design`
1. **Root cause**: `restore-repurge.ts` was documented as `pnpm --filter statera-api exec tsx …`, but the production host has no host-level Node (it runs Node only inside Docker images, by design). Both the runbook Stage 2/3 blocks and the script's printed "Next" hints failed.
2. **Working invocation (now documented)**: `docker run --rm --network host -v /home/deploy/statera:/repo -v /dev/shm:/dev/shm -w /repo node:22-alpine sh -c "corepack enable && pnpm install --frozen-lockfile --filter statera-api && pnpm --filter statera-api exec tsx /repo/deploy/restore-repurge.ts …"`. Note the **ABSOLUTE `/repo` path** — `pnpm exec` chdirs to `apps/api`, so a relative `deploy/…` path breaks; `--network host` reaches the scratch container on `127.0.0.1:3307`; `-v /dev/shm` shares the tombstones JSON.
3. **Applied**: folded into runbook §8f-2 Stage 2/3 and the script's printed "Next" block. Record-only (not built): reuse the already-present `statera-api` image instead of `node:22-alpine` to skip the per-invocation install.

**F4 · 8f-2 drill — two script fixes already merged mid-drill (cross-reference only)** `the drill surfaced two script defects, each fixed and merged as it was found — full write-ups live in the commits/runbook, one line each here`
1. **Prefix-hardcoding reversal (`765ed6f`)**: `--object`/`--verify-only` now accept a full prefix path (auto-detect on `/`), so the drill can target the `weekly/` known-answer object; not-found vs zero-byte distinguished.
2. **Two-symptom mysql:8 init race (`dde1dfb`)**: the socket readiness probe blessed the temporary init server, so the dump load hit **ERROR 2002** (post-init restart gap) on the first attempt and **ERROR 1045** (pre-password temp server) on the warm-image second; the TCP probe is immune to both. Stage 1 passed on the third attempt (2002 → 1045 → PASS).

**10d-0 — dev-DB drift + green-for-the-wrong-reason rollback test (2026-07-07)** `local dev MySQL was behind on migrations (0003 is_tombstone unapplied); the account-deletion rollback integration test's bare catch{} was satisfied by the resulting missing-column error, masking that it never exercised its injected failure`
1. **Dev-DB drift**: the 10d-0 INTEGRATION run failed on the new committed-purge case with `Unknown column 'is_tombstone' in 'field list'`. The dev MySQL (`public_statera-mysql-1`, up 3 days) had only 3 rows in `__drizzle_migrations` — migration `0003_add_security_events_is_tombstone.sql` had never been applied to it (prod got it via the 030b930 hotfix; this dev container predated or was recreated without it). Fixed by applying migrations host-pointed: `DATABASE_URL=mysql://…@127.0.0.1:3306/statera pnpm --filter statera-api exec drizzle-kit migrate` (no code/repo change). After apply, the full INTEGRATION suite went green (50 files / 745 passed / exit 0).
2. **Green-for-the-wrong-reason**: the pre-existing rollback test (`account-deletion.integration.test.ts`) wrapped its transaction in `try { … throw new Error("simulated mid-purge failure") } catch { /* expected */ }` — a bare catch that swallows ANY error. On the drifted DB the tombstone `insert(...).values({ isTombstone: true })` threw the missing-column error BEFORE the `throw` was reached, and the bare catch absorbed it. The test's only assertion (user row still present after rollback) held either way, so it passed while never actually exercising its injected failure. A failure-injection test that can be satisfied by an unrelated failure is not testing what it claims.
3. **Fix (same commit, e4c262f)**: the rollback test now captures the error message and asserts `expect(caughtMessage).toBe("simulated mid-purge failure")`, so an unrelated error (missing column, connection drop, etc.) fails the test loudly instead of masquerading as the injected one.
4. **Durable rule**: **failure-injection / rollback tests must assert the SPECIFIC expected error, never merely that "some failure occurred."** A bare `catch {}` around an injected-failure block is a silent-rot vector — it converts any environmental breakage into a false green. Assert the exact message/type. Corollary (operational): a local INTEGRATION run is only trustworthy against a migrated DB — run `drizzle-kit migrate` (host-pointed) before treating a dev-stack integration pass as real, since a behind DB can both fail real tests and hide broken ones.

**7.5 fix-forward — account-deletion integration test mock contamination (2026-07-04)** `an INTEGRATION-gated rollback test embedded in a unit file that mocks db/connection at module scope was structurally never-runnable since 5b7bbba; surfaced by 10b-2's pre-Phase-A integration run`
1. **Root cause**: `apps/api/src/lib/account-deletion.test.ts:7` declared `vi.mock("../db/connection", () => ({ getDb: vi.fn() }))` at module scope for its unit tests. The same file also carried an `INTEGRATION=true`-gated `describe.skipIf` block ("purgeUserAccountRows — transaction rollback") whose `getDb()` (via a dynamic `await import("../db/connection")`) therefore resolved to the **mock** — `getDb()` returned `undefined`, so `db.insert(...)` threw `TypeError: Cannot read properties of undefined (reading 'insert')` on the first line past setup. Introduced in the 7.5 commit `5b7bbba` and **never executed**: CI is hermetic (the block skips), and no prior local `INTEGRATION=true` run had exercised it. It only surfaced when 10b-2's pre-Phase-A obligation ran the full integration suite for the first time. Classic silent-rot: written, gated, never run, and broken from day one.
2. **Fix**: extracted the integration `describe` verbatim (assertions unchanged; only imports/setup adapted for the split) into a new dedicated `apps/api/src/lib/account-deletion.integration.test.ts` with **no** module-level db mock, so `getDb()` returns the real client. The unit portion keeps its mock and stays in `account-deletion.test.ts` (now 6 unit tests).
3. **First-ever execution result**: GREEN. Against the live Docker dev stack (`INTEGRATION=true`, host-pointed `DATABASE_URL`/`REDIS_URL`) the extracted test passed (exit 0) — the rollback logic works; no purge defect. Full integration suite then fully green: 46 files / 722 passed / 0 failed / exit 0. Hermetic gates unaffected: `tsc --noEmit` = 0, `pnpm --filter statera-api test` = 713 passed / 9 skipped / exit 0 (the new integration file skips cleanly when ungated; the unit file still mocks).
4. **Durable rule (see the new test-convention standing rule below)**: INTEGRATION-gated `describe`s must live in dedicated `*.integration.test.ts` files with no module-level db/redis mocks. Mixing an integration block into a mock-contaminated unit file yields a test that can never run — the same silent-rot the 8e lesson warns about ("an artifact documented as existing needs an active verification step that confirms it works").

**10f-fix — false-green verification + incomplete Redis stub** `10f shipped red: the ioredis stub lacked script/evalsha/decr, health.test.ts (real rate-limiter) produced 24 unhandled rejections, and CI exited 1 — but green was claimed from a pass-count grep that never checked the exit code`
1. **The runtime bug**: `health.test.ts` calls `createApp()` (the full app) without mocking `../lib/rate-limit`, so every rate-limited router constructs a real hono-rate-limiter `RedisStore`. Its constructor eagerly calls `loadIncrementScript()`+`loadGetScript()` → `makeRedisClient.scriptLoad` → `redis.script("LOAD", …)`. The 10f `src/test/redis-mock.setup.ts` stub had no `script` method → `TypeError: redis.script is not a function`, thrown inside unawaited constructor promises → 24 unhandled rejections (~12 rate-limited routers × 2 scripts). Vitest reports `713 passed` **and** `Errors 24 errors` and exits 1. `health.test.ts` was the sole trigger (the contract test, the only other `createApp()` caller, mocks rate-limit).
2. **Fix (Option A)**: extended the stub with the three commands the rate-limit adapter legitimately issues, per hono-rate-limiter's dist protocol read directly (not guessed): `script()` → string sha (`scriptLoad` must return a string or RedisStore throws "unexpected reply"); `evalsha()` → `[1, 60000]` (`parseScriptResponse` needs a 2-element `[totalHits, timeToExpire]`; `1` = first hit → never rate-limits a test); `decr()` → `0`. Documented the stub's class-based **fail-fast** as intentional: an unmodeled command must throw loudly, never no-op — a Proxy catch-all would have hidden this omission. Committed as `phase-4: 10f-fix` (new commit; `433e6cc` was already published, so no amend).
3. **The verification failure (durable lesson)**: the 24 rejections were present in the 10f verification captures (`testrun3.txt`/`testrun4.txt` both show `Errors 24 errors`) and were **dismissed as "expected stderr from the Sentry-error test."** Green was claimed by grepping the `Test Files|Tests` pass/fail summary, which read `713 passed / 0 failed`, without ever checking the process exit code or the `Errors`/`Unhandled Errors` section. As a result main's run **28675965998 went red** on the api step. Even the 10f red-run proof only showed a broken assertion flips a test to *failed* — it never established that the *baseline* exit code was 0.
4. **Durable rule (verification practice, applies to every module going forward)**: **green = the exact CI command exits 0, asserted explicitly.** Pass/fail counts are NOT a verification — a suite can report "N passed / 0 failed" and still exit non-zero on unhandled rejections, unhandled errors, or setup failures. Every "is it green?" check must (a) capture and assert `$? == 0` on the exact command CI runs, and (b) confirm there is no `Errors`/`Unhandled Errors`/`Unhandled Rejection` section in the output. Never rationalize visible stack-trace noise as "expected" without confirming it is not counted against the run.

**8f-1 — EU-jurisdiction R2 endpoint** `bucket in EU jurisdiction requires .eu. S3 endpoint; standard-host endpoint returns empty-body 403 indistinguishable from an auth failure`
1. **Root cause**: the R2 bucket `statera-prod-db-backups` was created in the EU jurisdiction. Cloudflare R2 EU buckets require the `.eu.` S3 endpoint (`<account>.eu.r2.cloudflarestorage.com`); the default/standard endpoint (`<account>.r2.cloudflarestorage.com`) returns HTTP 403 with an empty body and empty RequestID/HostID headers. This looks identical to an authentication failure or invalid credentials — not a routing error — making it hard to diagnose.
2. **Fix**: updated `R2_S3_ENDPOINT` in `secrets/.env.prod.sops.yaml` to `https://afce8d0ab0e452a1bcf73e4ed5146760.eu.r2.cloudflarestorage.com`.
3. **Lesson**: when creating an R2 bucket, the jurisdiction (Standard vs EU vs APAC) determines the required S3 endpoint. Standard buckets use `<account>.r2.cloudflarestorage.com`; EU buckets require `<account>.eu.r2.cloudflarestorage.com`. Always verify the bucket jurisdiction and match the endpoint when provisioning R2 access tokens.

**8d — §4 secrets plumbing** `VAR=$(cmd) silently swallows sops failure under set -e; fixed by decrypting to temp file`
1. **Root cause (Layer 1, confirmed)**: `ENV_VARS=$(sops -d --output-type dotenv ...)` does not trigger `set -e` on sops failure — in bash, `VAR=$(cmd)` command substitutions always exit 0 regardless of the substituted command's exit code. A sops failure silently sets `ENV_VARS=""`, execution continues, all `${VAR}` interpolations in the Compose YAML resolve to empty strings, and MySQL refuses to start with empty `MYSQL_ROOT_PASSWORD`.
2. **Layer 2 (latent, not confirmed)**: `--env-file <(printf '%s' "$ENV_VARS")` passes a process substitution pipe FD to docker compose. Even when ENV_VARS is populated, a pipe FD leaves no artifact if something goes wrong. The temp file approach improves debuggability (an operator can inspect it during a paused execution) and incidentally hardens against process-substitution edge cases on specific Docker Compose versions.
3. **Fix**: decrypt sops output to a temp file (`mktemp --tmpdir=/dev/shm` for RAM-backed tmpfs, fallback `/tmp`); use `if ! sops ...; then die; fi` to catch failure explicitly; validate non-empty output; `compose()` and `_rollback()` use `--env-file "$ENV_FILE"`; EXIT trap deletes the file on all exit paths including `set -e` exits and signals.
4. **Lesson**: never use `VAR=$(cmd)` when the command's failure must abort the script under `set -e`. Use `if ! cmd > file; then die; fi` instead.
5. **Verified operational 2026-05-23**: the §4 fix in d3c8e7c was first exercised on trigger commit 5d3dbef, which confirmed the temp-file decrypt pathway worked — 29 vars decrypted to /dev/shm, secrets reached the Compose environment. Full end-to-end pipeline green came after the subsequent MYSQL_PASSWORD URL-safety and Compose registry/repo fixes (both a62a6b6). Forward deploy and rollback test on commits b0e413b and 79e19ba both green by session close.

**8d — bootstrap §6 repo clone gap** `manual git clone required on production server before first deploy`
1. **Root cause**: bootstrap.sh §6 (repo clone) was removed in daec3d1 — repo checkout was declared deploy.sh's responsibility. However, deploy.sh §1 calls `git fetch` on an *existing* repo; it does not clone. The production server's `~/statera/` directory existed but was empty (created by bootstrap), so `git fetch` aborted immediately on the first deploy attempt.
2. **Fix**: manual `git clone https://github.com/KhaledAlrashi1/public-statera.git ~/statera` as the deploy user (2026-05-22). One-time operation per server.
3. **How to apply**: any new server provisioned with bootstrap.sh requires a manual clone before the first CI deploy can succeed. Add an explicit note to DEPLOY.md §6, or add a guarded one-time clone step to bootstrap.sh (`[ -d ~/statera/.git ] || git clone ...`). Ties into TODO(module-8b-bootstrap-rerun-strategy).

**8d — authorized_keys bare CI key** `CI deploy key installed without command= restriction; forced command and SendEnv silently dropped`
1. **Root cause**: the CI deploy pubkey was appended to `authorized_keys` during the 2026-05-22 key-rotation recovery without the `command=` restriction or `no-*` options. A bare entry opens a plain shell — deploy.sh never ran. The workflow's `SendEnv=DEPLOY_SHA` is silently discarded by sshd when no forced command is configured; `$DEPLOY_SHA` never reached deploy.sh. The SSH session opened, showed `/etc/motd`, and exited 0 in ~2 seconds, producing a "green" deploy job that did nothing.
2. **Fix**: replaced line 3 of `authorized_keys` with the full restricted entry from DEPLOY.md §2 on 2026-05-22: `command="bash /home/deploy/statera/deploy/deploy.sh",no-port-forwarding,no-X11-forwarding,no-agent-forwarding,no-pty ssh-ed25519 ... statera-ci-deploy`.
3. **Lesson**: when adding a CI deploy key — including during incident recovery — the `command=` restriction must be included from the first write. A bare CI key is operationally indistinguishable from a broken pipeline until someone reads the deploy log carefully.

**8d — short-SHA workflow_dispatch** `deploy.sh §1: rev-parse expansion to prevent tag-mismatch on docker pull`
1. **Root cause**: `workflow_dispatch` accepts `inputs.sha` verbatim with no validation or expansion. A caller supplying a short SHA (e.g. `893b6a3`) causes `DEPLOY_SHA` to flow through `SendEnv → AcceptEnv → GIT_SHA → IMAGE=ghcr.io/.../statera-api:893b6a3` unchanged. `docker pull` then returns "not found" because build-push-action tags images with the full 40-char `github.sha` only; no short-SHA alias is pushed.
2. **Fix**: after `git fetch` in deploy.sh §1, resolve `GIT_SHA` to its full form via `git rev-parse --verify "${GIT_SHA}^{commit}"`. Git resolves short SHAs post-fetch; the call is a no-op for full SHAs already in the correct form. Wrapped in an explicit error block (not bare `set -e` abort) so callers see an actionable message.
3. **Why fixed in deploy.sh (and later also in the workflow)**: deploy-bootstrap.sh can be invoked manually outside the workflow; one fix covers both manual invocation paths. A separate `resolve-sha` job was subsequently added to the workflow (commit e1decfb) to expand SHAs before `actions/checkout` runs — a distinct failure mode where `ref: 79e19ba` causes checkout to match branch/tag patterns rather than commits. See "8d — actions/checkout short-SHA limitation" below.

**8d — MYSQL_PASSWORD URL-safety** `openssl rand -base64 generates / chars; Node URL parser rejects them in connection strings; rotated via ALTER USER without volume drop`
1. **Root cause**: Node's `new URL()` parser rejects passwords containing `/`, `=`, or `+` in the authority component of a `mysql://user:pass@host/db` URL. The initial `MYSQL_PASSWORD` generated with `openssl rand -base64 32` contained `/`, causing Drizzle ORM to fail at startup with "Invalid URL" when parsing `DATABASE_URL`.
2. **Fix**: rotated `MYSQL_PASSWORD` on the running MySQL container via `docker exec ... mysql -e "ALTER USER 'statera'@'%' IDENTIFIED BY 'newpass'; FLUSH PRIVILEGES;"`. No volume drop or data loss. Re-encrypted `secrets/.env.prod.sops.yaml` with the new value (commit a62a6b6).
3. **Pattern for future rotations**: `ALTER USER ... IDENTIFIED BY ...` on the running container, re-encrypt secrets file, deploy. No data migration required when only credentials change.
4. **Lesson**: use `openssl rand -hex 32` (hex alphabet only) for secrets that appear in connection URLs. `openssl rand -base64` is not URL-safe.

**8d — Compose registry/repo embedding bug** `embedding the repo name in the REGISTRY default value drops /statera-api when REGISTRY is set to namespace-only`
1. **Root cause**: `docker-compose.prod.yml` used `${REGISTRY:-ghcr.io/khaledalrashi1/statera-api}:${GIT_SHA:-dev}`. When `REGISTRY` is overridden with only the namespace (`ghcr.io/khaledalrashi1`), the `/statera-api` suffix from the default is dropped, producing `ghcr.io/khaledalrashi1:${GIT_SHA}` — an invalid image reference that fails at `docker pull`.
2. **Fix**: split the default — `${REGISTRY:-ghcr.io/khaledalrashi1}/statera-api:${GIT_SHA:-dev}` — so `REGISTRY` controls only the namespace and `/statera-api` is always appended. Applied to all three image lines (api, worker, migrate services); commit a62a6b6.
3. **Lesson**: when a compose image reference has a stable suffix (the repository name), do not fold it into the env-var default. Keep `${REGISTRY:-namespace}/stable-repo-name:${TAG}` so the suffix is unconditional.

**8d — thin-bootstrap restructure** `bash reads the script at SSH open; git reset --hard updates disk but the running session executes the stale in-memory version`
1. **Root cause (trap 1 — script self-update)**: bash reads a script into memory when the SSH session opens. `git reset --hard $GIT_SHA` updates on-disk `deploy.sh`, but the running process continues executing the version loaded at startup. A changed `deploy.sh` in the new commit silently runs the old version.
2. **Root cause (trap 2 — secrets read before checkout)**: `deploy.sh` decrypted `$SECRETS_FILE` at startup (§1). At that point `git reset --hard` had not yet run, so the secrets file on disk was the *previous* commit's version. A rotation commit's new credential never reached the Compose environment on that deploy cycle — the rotate + deploy was silently a no-op for credentials.
3. **Fix**: introduced `deploy/deploy-bootstrap.sh` — a stable, thin wrapper that `authorized_keys command=` invokes. It runs: `git fetch origin`, `git rev-parse --verify "${GIT_SHA}^{commit}"`, `git reset --hard "$GIT_SHA"`, then `exec bash deploy/deploy.sh`. The `exec` replaces the process, loading the just-checked-out `deploy.sh` and ensuring the secrets file reflects the new commit before decryption runs. Commit 79e19ba.
4. **Operational constraint**: `deploy-bootstrap.sh` cannot self-update via CI — it is loaded at SSH open time, before any git operations. Changes must be applied manually on the server. The file header documents this explicitly.
5. **Lesson**: for SSH-invoked deploy scripts, separate the "update the working tree" step from the "run the updated script" step using `exec`. The `exec` system call replaces the running process with a freshly loaded binary, carrying the exported environment.

**8d — actions/checkout short-SHA limitation** `actions/checkout ref: resolves via git ls-remote (branch/tag tips only); historical commits not at a ref tip are not found`
1. **Root cause**: GitHub Actions `actions/checkout` resolves `ref:` by calling `git ls-remote` to match branch and tag names. A short SHA like `79e19ba` matches nothing in `refs/heads/` or `refs/tags/`, so checkout cannot locate the commit and fails. This makes `workflow_dispatch` rollbacks supplying a short SHA fail at the checkout step.
2. **Fix**: added a `resolve-sha` job that runs before `test`, `build-push`, and `deploy`. It uses `gh api repos/{owner}/{repo}/commits/{sha}` to expand any 7–40 character hex SHA to the full 40-character form. A regex guard (`^[0-9a-fA-F]{7,40}$`) rejects branch names, tag names, and whitespace early — accepting a branch name would silently rewrite a rollback into "deploy HEAD of that branch." All three downstream jobs consume `${{ needs.resolve-sha.outputs.full_sha }}` for checkout ref, image tags, build-args, OCI labels, and `DEPLOY_SHA`. Commit e1decfb.
3. **Lesson**: treat `workflow_dispatch` SHA inputs as untrusted, unresolved strings. Expand and validate them in a dedicated first job before any downstream job consumes them.

**8c §5 — 5a2abf3** `production secrets created; four discoveries during encryption`
1. **sops 3.13 requires `--filename-override` for non-matching input paths.** The SECRETS.md documented encrypt command (`sops -e ... .env.prod.filled > secrets/.env.prod.sops.yaml`) fails with `error loading config: no matching creation rules found` because sops matches `path_regex` in `.sops.yaml` against the *input* file path, not the output. Workarounds: (a) place the plaintext input at a path matching the regex (e.g., `secrets/.env.prod.filled`, but this puts plaintext inside the repo — unsafe), or (b) pass `--filename-override secrets/.env.prod.sops.yaml` to tell sops which rule to match against. SECRETS.md fixed in this commit to use option (b). Unclear whether this is a sops 3.13 regression or whether the docs were always slightly wrong; not worth investigating further.
2. **sops 3.13 on macOS does not auto-discover keys at `~/.config/sops/age/keys.txt`.** Decryption fails with "Did not find keys in locations: SOPS_AGE_SSH_PRIVATE_KEY_FILE, SOPS_AGE_SSH_PRIVATE_KEY_CMD, ~/.ssh/id_rsa, SOPS_AGE_KEY, SOPS_AGE_KEY_FILE, SOPS_AGE_KEY_CMD" — note the default path is not in this list. Setting `SOPS_AGE_KEY_FILE=~/.config/sops/age/keys.txt` resolves it. The fix-forward note in "8c — phase-3: 8c" item 5 said the env var was unneeded for any caller in this project's flow; that claim is now invalidated for the operator's local flow on sops 3.13. The server's deploy script already sets this env var explicitly (per SECRETS.md), so the server is unaffected. Operator should add to shell rc (e.g., `~/.zshrc`).
3. **Operator key disclosure and rotation.** During decryption debugging, an `age-keygen -y` diagnostic that should have only printed the public key was preceded by a `head -3 ~/.config/sops/age/keys.txt` diagnostic that rendered the private key into the conversation transcript. Recovery: deleted compromised `keys.txt`, generated new keypair (`age1rf548rfn2hqklfj5nc7slxaym8w0wzmkhrrh7c20ja0sv76lxyds5rzera`), updated `.sops.yaml`, re-encrypted from the same plaintext. No encrypted file with the compromised key was ever committed. Provider-side secrets (Postmark, Sentry, Google OAuth) were never exposed. Old public key `age1wl979xgh88849kxm7q079cqxaaacg306n5pp2u3uuhkslpzln54q9wknq2` is now non-functional and should not be reintroduced. **Lesson:** sensitive diagnostic commands like `head -3 keys.txt` should be avoided; future runbooks should use `head -2` or `age-keygen -y` (which only prints public keys to stdout) when only the public material is needed.
4. **`.env.prod.example` `PENDING_2FA_MAX_FAILURES` ordering.** The variable exists in `deploy/.env.prod.example` but was missed in a curated draft-file template used during this session, then appended manually but was already present in the draft, causing a one-line duplication caught by `awk '!seen[$0]++'` dedup before encryption. No code change; future Path A re-runs (e.g., for a staging environment) should use a draft template generated directly from `.env.prod.example` (e.g., `grep -E '^[A-Z][A-Z0-9_]*=' deploy/.env.prod.example > draft.txt`) rather than a curated subset.

**operator-key-rotation 2026-05-22** `unplanned recovery, no module commit — operator SSH key rotated after laptop loss`
1. **Trigger and constraint**: Operator lost access to the laptop holding the only working operator SSH private key (`~/.ssh/id_ed25519_statera`). Without it, no SSH access to `deploy@statera-prod`. The CI deploy pubkey was generated pre-session but not yet installed on the server. Recovery via Hetzner VGA console was required. The Hetzner VGA console UI on this server has no Send-Text / paste-clipboard button, so the operator pubkey had to be transferred without keyboard entry of long base64 strings. Workaround used: published the two pubkeys (operator v2 + CI deploy) to a temporary public GitHub gist, downloaded via `curl` from the VGA console, appended to `/home/deploy/.ssh/authorized_keys`, then deleted the gist.
2. **Result**: `/home/deploy/.ssh/authorized_keys` grew from 1 line to 3 lines: pre-rotation operator (kept as fallback, not pruned this session — see open decisions in handoff), `statera-operator-20260522` (new operator v2), `statera-ci-deploy` (CI deploy key, installed but not yet wired into GHA secrets). Owner `deploy:deploy`, perms `600`, trailing newline present. Verified end-to-end via `ssh statera-prod 'whoami'` returning `deploy`. Operator's `~/.ssh/config` rewritten from duplicated `Host statera-prod` blocks to a single clean block pointing at `id_ed25519_statera_v2`.
3. **VGA console UX lesson — UFW kernel logs**: with UFW logging enabled, kernel `[UFW BLOCK]` messages spam tty1 every few seconds (every public IPv4 is constantly port-scanned). This makes typing the root password at the login prompt extremely difficult — the visible cursor scrolls past whatever's being typed even though the password buffer captures the keystrokes correctly. The fix is `dmesg -n 1` immediately after a successful root login. This requires already being logged in, so the first login attempt has to be made through the noise. Carry this forward: any future recovery work through the Hetzner VGA console should expect this and budget for retries on the password prompt.
4. **Gist-trailing-newline trap**: GitHub gists do not always include a trailing newline on the last line. A `cat file >> target` append where the source has no trailing newline produces a target whose last-key line has no terminator, which (a) makes the shell prompt appear glued to the last line in subsequent `cat` output, and (b) causes `wc -l` to undercount by one. Future appends to line-oriented files (`authorized_keys`, `/etc/hosts`, etc.) should use `{ cat source; echo; } >> target` to guarantee a trailing newline, or visually verify the source ends in `\n` via `tail -c 1 source | od -c`.
5. **Path-typo near-miss in `cat ... >> path`**: on the first attempt at the append, the redirect target was misspelled (`authroized_keys` instead of `authorized_keys`). Because bash creates a new file at the typo'd path rather than failing, the real `authorized_keys` was untouched and the new keys ended up in a stray root-owned file in `/home/deploy/.ssh/`. The `wc -l` verification of the target file caught it immediately (line count didn't change). Lesson: for any high-stakes append/redirect, the verification pattern is `wc -l target` before and after, and a line count that didn't grow is the signal that the target wasn't written. Tab-completion on long paths is cheaper than checking spelling by eye, especially when the shell is running as root and there's no second chance.
6. **Cross-Claude conversation handoff**: this session involved transferring recovery state across two Claude conversations. The receiving Claude verified the CI pubkey value against the on-disk file before acting on it, rather than trusting the value carried over from the previous transcript. No private key material was ever pasted into chat. Future cross-conversation handoffs should preserve the same discipline: verify any sensitive-shaped string (key, hash, token) against the local source before using it; treat the previous Claude's quoted strings as untrusted input.

**orphan operator pubkey prune 2026-05-23** `pre-rotation operator pubkey deferred three sessions, finally removed before 8e`
1. **Trigger**: Following the operator-key-rotation incident 2026-05-22, the pre-rotation operator pubkey was retained on `/home/deploy/.ssh/authorized_keys` line 1 as a fallback. Pruning was deferred across the 2026-05-22 rotation recovery, the 2026-05-22/23 thin-bootstrap session, and the 2026-05-23 8d operational-pass completion. Third deferral established that "no acute exposure" (private half on the lost laptop, not in third-party hands) was being repeatedly accepted as an indefinite postponement reason. Prune executed at start of 2026-05-23 (PM session), explicitly before 8e (TLS + Caddy) adds environmental complexity that would make `authorized_keys` investigations harder.
2. **Procedure**: followed the protocol from `docs/recovery/2026-05-22-8d-operational-pass.md`: escape-hatch SSH session opened first, timestamped backup (`authorized_keys.bak.1779566412`), pre-state verification (3 lines, 3 fingerprints, line 1 matched `SHA256:VkHYe2dE...` per CLAUDE.md), `sed -i '1d'` line-scoped delete, post-state verification (2 lines, 2 fingerprints, `command=` restriction on line 2 intact), fresh laptop terminal validation (`ssh statera-prod 'whoami'` returned `deploy`), then laptop-side `rm` of orphan keypair. No errors at any step.
3. **Lesson — deferral counting**: a deferral with a stated "re-evaluate next session" framing should be tracked with an explicit count. By the third deferral, the framing reveals itself as the deferral mechanism, not as an evaluation trigger. CLAUDE.md now records deferral counts when an open item is deferred.
4. **Open item carried forward**: `~/.ssh/authorized_keys.bak.*` files accumulate one per operational session that touches the file (currently 3 backups beside the live file). Audit before final public release. Each is a snapshot of historical key state — none should be readable by `sshd` (they don't match the `authorized_keys` filename pattern), but defense-in-depth and historical-data hygiene both argue for cleanup.

**9.4 — /api/auth/me returned session stub instead of user record** `Phase 2 scaffold incomplete; never caught because Module 7 smoke validated /me via curl without comparing response shape to frontend's expected type`
1. **Root cause**: Phase 2 scaffold commit 13c709a wrote `router.get("/me", requireAuth, (c) => c.json({ session: c.var.session }))` as a stub and it was never completed through 7a/7b/7c/7.5/9.3a. Response shape was `{ session: { userId, externalId, authProvider, sv } }`. Frontend `authApi.me()` expects `{ ok, user: User, flags }`. `AuthContext` reads `data.user ?? null` → always null → ProtectedRoute redirects every authenticated user to /login.
2. **Diagnosis chain**: OIDC callback works → session cookie set → /me returns 200 (requireAuth passes) → response has no `user` field → `setUser(null)` → redirect to /login. The cookie itself was never the bug.
3. **Fix**: completed /me to query the users table by userId and return `{ ok: true, user: {id, email, display_name, first_name, last_name, totp_enabled, created_at}, flags: {template_suggestions: false, open_banking: false} }`. Flags hard-coded false — features deferred indefinitely, env-var wiring would be a footgun.
4. **Defect class #4 (route-incompleteness)**: distinct from typed-drift (9.1), URL-drift (9.2), feature-drift (9.3). A backend route that returns 200 with the wrong payload shape is invisible to any test that only checks the status code. The Module 7 smoke test validated /me as "returns 200" without asserting the response matched the frontend's declared expected shape — so the stub passed smoke for months.
5. **Lesson — TODO(module-9-contract-validation)**: backend smoke tests in isolation miss contract-mismatch bugs. See TODO entry in Public API contracts section.

**9.3 — Flask-era auth feature stack dead since Phase 3 port** `nine pages, twelve endpoints, CSRF machinery — never connected to Hono`
1. **Root cause**: the email/password auth stack (LoginPage form, RegisterPage, ForgotPasswordPage, ResetPasswordPage, ConfirmEmailChangePage, ConfirmPasswordChangePage, and associated authApi methods + CSRF machinery) was copied from the Flask frontend intact. Hono has no POST /api/auth/login, no /api/auth/register, no password column — OIDC-only since Phase 3. No audit was performed at Phase 3 port time or at 8e cutover.
2. **Silent failure mode**: every page-level call to authApi.login/register returned HTTP 404. AuthContext swallowed these into error state with no visible feedback. The CSRF machinery generated one 404 noise request per page load from getCsrfToken → /api/csrf-token; harmless but invisible.
3. **Detected**: Module 9 smoke test — observed LoginPage rendering an email/password form against a backend with no such endpoint.
4. **Lesson — whole-feature audit at port time**: when a backend port deliberately omits a feature class, the frontend must be audited and dead entry points removed in the same commit window. Deferring to a parity module means dead UI ships to production. The correct process is audit-before-first-deploy, not audit-after-launch.

**9.2 — 12 of 13 analytics endpoints returning HTTP 404 since production launch** `Flask-era bare /api/* paths never updated to Hono's /api/analytics/* prefix`
1. **Root cause**: all 13 `analyticsApi` methods in `apps/web/src/lib/api.ts` were copied from the Flask frontend in commit `c925cc3` (phase-1 copy). Flask served analytics at bare `/api/<name>` paths. Hono mounts both analytics routers at `/api/analytics` (confirmed in `app.ts:52-53`), so all routes live at `/api/analytics/<name>`. 12 of the 13 methods were copied with Flask paths unchanged. One method, `accountOverview`, was written with the correct `/api/analytics/account-overview` path from day one — showing the correct convention was known at copy time but not applied consistently. Git archaeology (`git log --follow -p apps/web/src/lib/api.ts`) confirms both the correct and incorrect paths arrived in the same initial commit; no partial audit was performed at any later point, including at the 8e cutover when analytics first became reachable by a real browser.
2. **Impact**: every dashboard number, chart, and analytics feature (safe-to-spend, budget metrics, income pattern, weekly digest, snapshot, spend charts) returned HTTP 404 from the moment the frontend was first deployed in commit 27ae763. The only working analytics endpoint was `accountOverview`. `apiFetch` throws `ApiError` on 404; TanStack Query enters error state; components silently rendered zero-state or loading skeletons with no error UI for most callers.
3. **Detected**: Module 9 URL table grep (`grep -rn 'apiFetch' apps/web/src/lib/api.ts`). The 12 mismatched strings were visible in one pass.
4. **Window**: live from 2026-05-24 (commit 27ae763, first frontend production deploy) until this fix. Real-user impact: zero — site was unannounced.
5. **Lesson A — URL audit as deploy prerequisite**: when a backend port restructures a URL convention (`/api/<name>` → `/api/analytics/<name>`), the frontend URL-table audit is a prerequisite for the backend's first production deploy, not a follow-up task. The `/api/analytics/*` restructure was recorded in CLAUDE.md's "Public API contracts" before 8e cutover. The frontend audit was deferred to Module 9, by which point the broken state had already shipped.
6. **Lesson B — mock-layer blind spots**: the unit test suite mocks at the `analyticsApi` function level, bypassing the network layer entirely. No test asserts that `fetch` was called with a specific URL. URL drift between frontend and backend is therefore invisible to the test suite. See TODO(module-9-network-mocking).
7. **Lesson C — partial correctness at init time**: `accountOverview` being correct in the same commit that introduced 12 wrong paths is a recognizable pattern: the person who wrote it knew the Hono convention but did not apply it retroactively to the existing entries. Any URL convention change — at copy time or at port time — needs either an all-at-once pass or an explicit per-entry checklist. Partial correctness is worse than full incorrectness because it removes the signal that something is wrong.
8. **Deferred**: `spendingIntelligence` calls `/api/spending-intelligence`, which is a Flask-era composite endpoint (`top_merchants`, `category_benchmarks`, `category_deltas`, `recurring_bills`) not ported to Hono. No Hono route exists for it. SpendingIntelligencePage nav link is already commented out in AppShell.tsx. Requires a separate 9.x decision: build the Hono route or formally defer/hide the page.

**9.1 — income nudge banner silently dead in production** `null-comparison against "not_set" enum value caused banner to never render after 8e cutover`
1. **Root cause**: `apps/web/src/components/pages/dashboard/sections.tsx:249` compared `safeToSpend.income_source === null`. The Hono backend (modules 5b/5c) returns `"not_set"`, not `null` — a documented Flask-to-Hono deviation (CLAUDE.md "Public API contracts"). The frontend was never updated for the deviation, so the condition was always false, and the `showIncomeNudge` banner that prompts users without a detected income to set one in Profile never rendered.
2. **Detected**: Module 9 scoping grep of `income_source` usage in `apps/web`. Type definitions matched Flask shape; runtime comparison matched Flask shape; both silently disagreed with the live backend response.
3. **Window**: live from frontend's first production exposure (commit 27ae763, 2026-05-24) until this fix. Real-user impact: zero — site was unannounced throughout the window.
4. **Lesson — typed-deviation drift**: when a backend port deliberately deviates from the original contract (here, returning `"not_set"` instead of `null`), the frontend type definitions and runtime branches must be migrated in lockstep with the backend release. Otherwise the type system continues asserting the old contract, the runtime continues comparing against the old contract, and the deviation is invisible until someone reads the contract doc with the code open. Module 9 was created to do exactly this walkthrough; the catch validates the module's framing.
5. **Lesson — test-file type exclusion**: `tsconfig.json` excludes `*.test.tsx` from typecheck. A factory missing a required field of a strict response type goes undetected by `tsc --noEmit`. This is a standing gap; not fixed in this commit but tracked for future quality work.

**8e — .dockerignore exclusions for cross-Dockerfile builds** `single-Dockerfile .dockerignore rules silently broke a second Dockerfile's build context twice`
1. **Root cause**: `.dockerignore` rules written for `apps/api/Dockerfile` excluded directories the new `deploy/web.Dockerfile` needed. First iteration: `deploy/` was a blanket exclusion (apps/api/Dockerfile never references deploy/, so this was fine before), which prevented `COPY deploy/Caddyfile` from finding the file. Second iteration after the first fix: `apps/web/src/`, `apps/web/public/`, `apps/web/index.html`, `apps/web/*.config.*`, and `apps/web/tsconfig*.json` were all excluded (apps/api/Dockerfile doesn't COPY them, so their absence from its build context was invisible), which broke `pnpm --filter statera-frontend build` at the `tsc -b` step because tsconfig.json wasn't in the context.
2. **Fix iteration 1**: added `!deploy/Caddyfile` as a whitelist exception under the `deploy/` exclusion. Minimal — whitelists exactly the Caddyfile and nothing else from `deploy/`.
3. **Fix iteration 2**: replaced the five-line frontend exclusion block with a single `apps/web/dist/` exclusion (the only rule that applies to both images). The other entries became whitelisted by absence — they flow into both build contexts. Harmless for the api Dockerfile because it doesn't COPY them.
4. **Lesson**: when adding a second Dockerfile to a repo, audit `.dockerignore` for rules that were silently scoped to the existing Dockerfile's needs. A `.dockerignore` is a context-shaping document, not a security boundary — entries should be there because every plausible Dockerfile in the repo wants them excluded, not because the current Dockerfile happens not to need them. The alternative pattern — per-Dockerfile `.dockerignore` files (`deploy/web.Dockerfile.dockerignore`, a BuildKit feature) — was considered and deferred. If a third Dockerfile is added, that pattern becomes the right move.

**8e — frontend test suite silent rot** `12 broken tests across 6 files discovered when CI gating was added; suite never ran in CI before this session`
1. **Trigger**: commit 2a added `pnpm --filter statera-frontend run test:unit` to the test job in `.github/workflows/deploy.yml` as part of frontend CI gating. The local pre-commit validation (run the new commands locally before committing) immediately exited 1: 12 tests failing across 6 files.
2. **Root causes**: mixed.
   - 5 failures in `helpers.test.ts`, `dialogs.test.tsx`, `import-dialogs.test.tsx` were a *real production bug*: `applyTransactionSuggestion` was not applying category/merchant from the suggestion object. Production code defect, not test drift. Tests had been correct.
   - 1 failure in `InsightsPage.test.tsx`: test hardcoded `'2026-03'` as "a prior month" without pinning `vi.setSystemTime`. As real-world time advanced past 2026-04, the test's expectation drifted from current-month math.
   - 1 failure in `AppShell.test.tsx`: test looked for a "UI gallery" menu item that had been removed from the component without updating the test.
   - 2 failures in `debt-summary.test.tsx`: elements not found — component shape had changed without test updates.
   - 3 failures in `dialogs.test.tsx` overlapping with the helpers cluster: same production bug, surfaced through the UI suggestion workflow.
3. **Fix**: 1 commit (1caa098) bundling 12 logical fixes across 6 files — each fix categorized as Category A (test bug, fix test), B (test drift from production change, fix test to match current code), or C (real production bug, fix code). One of the fixes was Category C — `applyTransactionSuggestion` shipped to production with bug intact in commits 4d45603 onwards but was never observable because frontend was never deployed to production until commit 27ae763. Triage suite green confirmed before commit 2a's CI gating shipped.
4. **Lesson — silent-rot pattern**: a test suite that exists but never runs is worse than no test suite. It gives false confidence ("we have tests, look") while drift accumulates invisibly. The lesson generalizes: any artifact that's documented as existing — tests, monitoring, runbooks, alerts — needs an active verification step that confirms it works. "It was set up correctly when written" is not a property that persists without intentional maintenance. Apply this to: alert rules (do they actually fire on the conditions they describe?), runbook accuracy (does the procedure still match reality?), monitoring dashboards (do they show data?).
5. **Lesson — gate from day one**: production code should be CI-gated from the moment it ships. If commit 2a had not happened, the `applyTransactionSuggestion` bug would have shipped to production via commit 27ae763 unnoticed and surfaced as a user-visible bug after launch. The cost of catching it in the triage session was 2 hours; the cost of catching it post-launch would have been hours of debugging + reputation cost from broken user-facing behavior.

**8e — Caddyfile SPA fallback try_files** `Caddy's try_files is a rewrite directive, not an nginx-style fallback test; needs three-arg form for directory-style URLs`
1. **Root cause**: Caddy v2 `try_files` is internal-rewrite, not nginx-style fallback-on-miss. The two-arg form `try_files {path} /index.html` works for most SPA cases but doesn't handle directory-style URLs that resolve to nested index files. The canonical Caddy SPA recipe uses three args: `try_files {path} {path}/ /index.html`. The middle `{path}/` entry catches directory-style URLs before falling back to root index.html.
2. **Fix**: shipped the three-arg form in commit 4d45603's Caddyfile from the start. For a Vite SPA with no nested index files, the `{path}/` entry is inert but harmless; including it matches the documented Caddy pattern.
3. **Lesson**: don't transliterate nginx syntax to Caddyfile. Caddy v2 directives are conceptually similar but mechanically different. When porting nginx config to Caddy, consult the Caddy docs for each directive's actual semantics rather than assuming similar names imply similar behavior.

**8e — Caddy reverse_proxy header_up redundancy** `Caddy passes X-Forwarded-Proto and X-Forwarded-For automatically; explicit header_up for those produces validation warnings`
1. **Root cause**: `caddy validate` warns on explicit `header_up X-Forwarded-Proto` and `header_up X-Forwarded-For` directives because Caddy's reverse_proxy passes both headers automatically. Initial Caddyfile (commit 4d45603) included all three forwarding headers explicitly, translating directly from the nginx.conf pattern. `caddy validate` reported four warnings (two per proxy block × two proxy blocks).
2. **Fix**: commit 59d69fe removed the two redundant directives from both proxy blocks and ran `caddy fmt` for canonical formatting. `X-Real-IP` retained: Caddy does NOT pass that header automatically, so the explicit `header_up X-Real-IP {remote_host}` is load-bearing.
3. **Lesson**: Caddy's defaults often subsume what nginx required explicitly. After translating any nginx config to Caddy, run `caddy validate` and address warnings — they document where Caddy is doing something automatically that the operator may not realize.

**8e — health-path drift between commit-3 verification and Caddyfile reality** `prompt referenced /api/healthz; Caddyfile correctly serves /healthz at root per design decision; Claude Code silently substituted correct path during verification`
1. **Root cause**: commit-3's post-deploy verification step in the prompt to Claude Code called for `curl -sf https://staterafinance.app/api/healthz`. This contradicted the architectural decision made earlier in the same session (health endpoints stay at root, not under `/api/*`) which the Caddyfile correctly implemented. The verification step prompt was written with stale path assumption.
2. **Detected**: Claude Code reported the discrepancy after the deploy went green and substituted `/healthz` for the actual verification curl. Site verification passed against the correct path.
3. **Lesson — prompt-vs-implementation drift**: when an architectural decision is made mid-conversation, all subsequent steps (verification curl paths, validation greps, etc.) need to be checked against the decision. The drift here was harmless because Claude Code surfaced it, but a less-attentive substitution could have produced "verification failed" that wasn't really a failure. Future prompts that include verification steps should derive paths from the just-approved artifacts (Caddyfile in this case), not from earlier assumptions.
4. **Correct paths for reference**: `/healthz`, `/readyz`, `/health` are all defined at root in the Hono application (`routes/health.ts:9-11`), but **only `/healthz` and `/readyz` are proxied by Caddy** — the `@probes path /healthz /readyz` matcher (`deploy/Caddyfile:31`) does not include `/health`, so `/health` at the edge falls through to the SPA `/*` fallback and returns `index.html`, NOT Hono's JSON. **Correction (10d-0-docs, 2026-07-07):** the earlier claim that `/health` is served at root "by both Caddy and Hono" is wrong at the Caddy layer. The SHA-reporting ops probes are `/healthz` and `/readyz` **only**; UptimeRobot and any future monitoring (8f-3) must probe those and **never `/health`**.

**module-7 smoke — first interactive run 2026-05-18** `phases 1/2/3 passed; four follow-up fix-forwards surfaced`
1. **Phases 1, 2, 3 passed cleanly.** 7a (TOTP enable/disable), 7b (TOTP verify-on-login), 7c (revoke-all + security events) are smoke-verified against the live Docker dev stack. Phase 4 step 4.1 passed unexpectedly (delete-reauth returned 302); step 4.2 failed at the same `get_login_url`-class bug that the existing phase-4 deferral entry documents — the script issued a curl-extracted Google OAuth URL, causing the browser to complete a regular login instead of the delete-reauth flow, and no `statera_delete_intent` cookie was issued. Phase 4 remains structurally deferred.
2. **Queued fix-forwards** (NOT addressed in this run; scheduled for next session): (a) ~~**Encryption key empty-string fallback**~~ — **RESOLVED** in "module-7 smoke — e2aa1cd" below. (b) ~~**`mapfile` portability**~~ — **RESOLVED** in "module-7 smoke — 46e8591" below. (c) ~~**`assert_status` body-on-failure ergonomics**~~ — **RESOLVED** in "module-7 smoke — 46e8591" below. (d) ~~**P0 stack health race**~~ — **RESOLVED** in "module-7 smoke — 46e8591" below.

**module-7 smoke — 54527a2** `phase-3: module-7 fix-forward — backup code count drift Flask vs Hono`
1. **Hono `BACKUP_CODE_COUNT` was 8**: `apps/api/src/lib/totp-lib.ts` shipped with `const BACKUP_CODE_COUNT = 8`, causing `POST /api/auth/2fa/setup` to return 8 backup codes. The inline comment and the `generateBackupCodes` default-count test both encoded the same wrong value.
2. **Flask source of truth is 10**: the smoke test (`tools/module-7-smoke.sh:332, :437`) asserts 10 backup codes, which is the Flask contract. Phase 1.5 of the smoke test failed with the assertion `expected 10 backup codes, got 8`.
3. **Fix**: `BACKUP_CODE_COUNT` set to 10; stale inline comment updated (`8 codes per setup event` → `10 codes per setup event`); test description updated (`generates 8 codes by default` → `generates 10 codes by default`). Existing users with 8 stored backup codes are unaffected — the JSON column stores whatever array length was issued; consumption counts dynamically via `JSON_LENGTH`.

**module-7 smoke — 45f003d** `phase-3: module-7 fix-forward — smoke test OAuth state cookie host mismatch`
1. **curl-to-browser cookie handoff bug**: `get_login_url()` called `curl -si http://localhost:3000/api/auth/login`, extracted the Google OAuth `Location` header, and discarded the response (including the `statera_oauth_state` Set-Cookie). The extracted Google URL was shown to the operator for browser paste. The browser reached `/api/auth/callback` with no state cookie and the callback rejected with HTTP 400 "Missing state cookie".
2. **localhost/127.0.0.1 origin mismatch**: curl hit `localhost:3000` while the API's `redirect_uri` is `127.0.0.1:3000`. Browsers treat these as different cookie origins, so even if the cookie had reached a browser somehow, it would not have been sent on the callback request.
3. **Fix**: deleted `get_login_url()`. Replaced both call sites (`interactive_pending_login()` and the P3 block) with a direct constant `LOGIN_URL="http://127.0.0.1:3000/api/auth/login"`. Operator prompts updated to "navigate to this URL" with the short stable endpoint URL. The browser hits the API directly, receives the state cookie on `127.0.0.1`, follows the 302 to Google, completes OAuth, and returns to the callback with the state cookie present on the same origin. No `apps/api/` change.

**module-7 smoke — 46e8591** `phase-3: module-7 fix-forward — smoke test ergonomics (mapfile, assert_status, P0 race)`
1. **`mapfile` portability (b)**: `tools/module-7-smoke.sh` used `mapfile -t` (bash 4+ only). macOS ships bash 3.2 so the script aborted before phase 1.5 on stock macOS. Replaced with a portable `while IFS= read -r line; do ... done < <(cmd)` loop for the `BACKUP_CODES_2` population.
2. **`assert_status` body on failure (c)**: on a status mismatch the function printed `expected X, got Y` and exited without showing the response body. The body contains the actual Hono error string, so each failure required manually re-issuing the curl to diagnose. Changed the third argument of `assert_status` from the pre-extracted status (`$(http_status "$RESP")`) to the full `curl -si` response. The function now extracts the status internally and prints the response body before calling `fail()` on mismatch. All 23 call sites updated; `http_status()` itself is unchanged.
3. **P0 stack health race (d)**: the one-shot `curl -sf /healthz` would immediately abort if the API was still binding its port after a `docker compose restart`. Replaced with a 30-second poll loop (`seq 1 30`, `sleep 1` per iteration) that breaks on first non-empty response. On timeout after 30 iterations the existing ABORT message fires. The `jq -e '.ok == true'` check is preserved after the loop exits.

**module-7 smoke phase-4 deferral — 45f003d** `design decision pending`
1. **Phase 4 calls `/api/auth/delete-reauth` which requires `requireAuth`**: the endpoint sets a `statera_oauth_state` cookie and redirects to Google. For this to work from a browser, the browser must present a valid `statera_session` cookie on the initial request.
2. **The smoke harness has no frontend**: all post-TOTP sessions are obtained via `curl POST /api/auth/2fa/verify`, which puts the resulting `statera_session` cookie on the curl process — never on the operator's browser. There is no point in the test flow where the operator's browser holds the current `SESSION_MAIN`.
3. **Fixing this requires a design decision**: options include a dev-only test-helper endpoint in `apps/api/` (e.g., `GET /api/test/set-session-redirect` gated on `env.isDev`), manual DevTools cookie injection (rejected as brittle — JWT values are long and a single typo causes a silent failure), or an alternative test-harness pattern. Decision deferred to its own proposal cycle; no `apps/api/` change is made in this commit.
4. **Phase 4 currently fails at step 4.1 with HTTP 400 "Missing state cookie"**: this is expected and acknowledged. A runtime banner was added to the smoke script at the phase-4 start to alert the operator. Phases 1, 2, and 3 passing is the definition of a successful run until this is resolved.

**module-7 smoke — e2aa1cd** `phase-3: module-7 fix-forward — encryption key empty-string fallback`
1. **`optional()` passed empty-string through**: `env.ts optional(key, fallback)` used `??`, which only falls back on `null`/`undefined`. An empty-string value (e.g. `ENCRYPTION_KEY=` in `.env`) passed through as `""`, a valid string. Any developer copying `.env.example → .env` without filling in `ENCRYPTION_KEY` would hit HTTP 500 on the first `encrypt()` call because AES-256-GCM rejects a zero-length key.
2. **`.env.example` shipped `ENCRYPTION_KEY=` (empty)**: no dev default was provided, so the bug was guaranteed to manifest on a fresh repo checkout.
3. **Fix**: `optional()` now treats `""` as absent (`val !== undefined && val !== ""`); `optional` is exported for unit-testability. `.env.example` now ships `ENCRYPTION_KEY=0000...0` (64 zeros) with a comment explaining it is a dev default only and pointing to `openssl rand -hex 32`. A `console.warn` fires at startup in dev mode when `encryptionKey` equals the all-zeros default. Three unit tests added in `apps/api/src/lib/env.test.ts` (empty-string falls back, undefined falls back, valid value passes through).

**8c — phase-3: 8c** `runbook bugs found in proposal review (not a production failure)`
1. **Operator key filename**: `age-keygen -o ~/.config/sops/age/operator.key` named the file `operator.key`, but sops defaults to `keys.txt`. Every local decrypt step would silently fail without `SOPS_AGE_KEY_FILE` being set. Fixed: key now generated as `keys.txt`; no env var needed.
2. **Missing existence guard**: `age-keygen -o keys.txt` overwrites silently, destroying the key and access to all encrypted files. Fixed: guard added that hard-errors if `keys.txt` already exists; rotation must follow the age key rotation procedure in SECRETS.md.
3. **§4 smoke test stdin bug**: `sops -e /dev/stdin` fails because sops matches creation rules against the input file path, and `/dev/stdin` does not match `secrets/.*\.sops\.yaml$`. Fixed: write to `secrets/smoke.sops.yaml` (matches regex), encrypt in-place, then scp + decrypt. `secrets/smoke.sops.yaml` added to `.gitignore` to prevent accidental commits.
4. **Backup note wrong path**: `age-keygen -y /tmp/operator.key` referenced a path that does not exist. Fixed: corrected to `~/.config/sops/age/keys.txt`.
5. **SOPS_AGE_KEY_FILE section removed**: no caller in this project's flow (operator uses default `keys.txt` path; CI never decrypts; server uses default path after scp). Section was dead text in a linear runbook.

**8c — §14 directory backfill (2026-05-16)** `discovered during 8c gate pre-flight`
1. Production server bootstrapped 2026-05-15 from commit 1bd02f0, but bootstrap.sh exited early on §4 (Docker Compose URL broken) on first run and §6 (git clone broken) on second run. §14 (creates `~deploy/.config/sops/age/`) sits after both failure points, so it never executed on this server.
2. Discovered 2026-05-16 during 8c gate pre-flight when `ssh deploy@server 'ls ~/.config/sops/age/'` returned "No such file or directory."
3. Fix: ran `mkdir -p ~/.config/sops/age && chmod 700` as deploy user (no root, no bootstrap re-run). §14 at HEAD is correct and idempotent — no code change required. See TODO(module-8b-bootstrap-rerun-strategy) for the broader audit.

**8b — daec3d1** `phase-3: 8b fix Compose install via apt, remove repo clone`
1. **Docker Compose installation changed**: Docker Compose v5+ restructured GitHub Releases assets; the v2.35.1 pinned-binary URL no longer resolves. Replaced with `apt-get install -y docker-compose-plugin`, which tracks the Docker engine version automatically. The `DOCKER_COMPOSE_VERSION` variable and the binary-download block were removed from bootstrap.sh.
2. **Repo clone block removed**: Bootstrap was cloning the repo from a hardcoded GitHub URL that required credentials and referenced a wrong-named repo. Repo checkout is 8d's responsibility (deploy.sh `git reset --hard $GIT_SHA`); bootstrap is self-contained server preparation. Replaced with an explanatory comment.

**8b — manual §14 backfill (2026-05-16)** `discovered during 8c gate pre-flight`
1. **§14 execution gap**: §14 (sops age key directory, added in 468696c) never executed on this production server. The bootstrap ran twice pre-daec3d1: Run 1 exited at §4 when `curl -fsSL` failed on the broken Docker Compose v2.35.1 URL (`set -euo pipefail` exits immediately); Run 2 exited at §6 when the hardcoded git clone URL was wrong. Both failure points precede §14 in the script. The operator manually recovered both times and treated the server as functionally bootstrapped; no third run was performed after daec3d1 fixed the broken sections, so §14 was never reached on any run.
2. **Discovered**: during the 8c gate pre-flight on 2026-05-16 — `ssh deploy@89.167.76.236 'ls -la ~/.config/sops/age/'` returned "No such file or directory," contradicting the CLAUDE.md record of "bootstrapped end-to-end 2026-05-15."
3. **Fix applied**: `ssh deploy@89.167.76.236 'mkdir -p ~/.config/sops/age && chmod 700 ~/.config/sops/age'` run as the deploy user (no root required; deploy user owns their own `$HOME`). Equivalent to what §14 would have done.
4. **No code change**: §14 in bootstrap.sh is correct at HEAD. The gap is a historical execution failure, not a script bug.

**Module 9 — frontend-backend parity (9.1–9.7f)** `8 defect classes resolved 2026-05-24 to 2026-05-30; CORE CRUD journey clean; demo/import/export paths later found broken (see 2026-07-03 audit); 2 launch blockers deferred`
1. **Defect class inventory**: 8 distinct categories surfaced, all resolved except 2 deferred launch blockers. (1) Enum-value drift: `income_source` null→"not_set" (9.1). (2) URL-prefix drift: 12/13 analytics routes on Flask-era bare paths (9.2). (3) Dead auth UI: Flask email/password stack with no Hono counterpart (9.3a/b). (4) Route incompleteness: /me stub returning `{session}` (9.4). (5) Bulk CRUD URL+shape drift: 17 defects across 6 resource types (9.5a–f). (6) Profile route missing: GET /api/auth/profile 404 (9.6). (7) Auth feature drift: password-change UI, 2FA envelope mismatch, CRUD error codes returning undefined (9.7a–e). (8) Flask-era dead account-deletion UI: two-step flow was never implemented in Hono (9.7f, deferred to TODO(account-deletion-ui)).
2. **readApiData pattern (discovered 9.7e)**: `apiFetch<T>` returns `res.json()` raw — the type parameter is a TypeScript hint only. All business data is nested under `data:` in the Hono envelope. Any api.ts method that reads root-level fields returns `undefined` for every field, silently. Pattern: always use `readApiData<T>(payload)` in new api.ts methods; grep for direct field access on `apiFetch` return values before each commit.
3. **readErrorCode field name (discovered 9.7b)**: Hono emits `{ ok: false, data: null, error: "...", code: "ERROR_CODE" }`. The `readErrorCode` helper was reading `rec.error_code` (a Flask-era field that doesn't exist in Hono), returning `undefined` for every Hono error since the port. The correct field is `rec.code`. Fix in 6b90db8.
4. **Migration journal atomicity (0003 hotfix, 030b930)**: `drizzle-orm/migrator.js` iterates `journal.entries` only. A `.sql` file not listed in `meta/_journal.json` is invisible to every migrate run — the schema is never applied, no error is reported. `0003_add_security_events_is_tombstone.sql` was committed in 7.5 without a journal entry; the column was missing from production for weeks. Fix: `git rm` the orphaned SQL, run `drizzle-kit generate` (produces `.sql` + journal entry + snapshot atomically), run `drizzle-kit generate` a second time — it must report "No schema changes" to prove the snapshot is authoritative. The three-file atomic unit is non-negotiable.
5. **Stale PENDING labels**: Two sub-items (9.5d and all of 9.6) showed "PENDING" in CLAUDE.md long after their commits shipped. A CLAUDE.md update that doesn't reflect actual commit state creates false blockers in future close-out prompts. CLAUDE.md updates are part of the commit, not a follow-up.
6. **Launch blockers carried forward**: (a) TODO(account-deletion-ui) — account deletion UI requires OIDC re-auth flow, bundled with TODO(GDPR data-export); neither ships alone. (b) docs/runbooks/key-rotation.md — Hono-era re-encryption procedure not yet written; stub in place pointing to TODO(key-rotation-runbook).
7. **Production verification**: Sentry confirmed zero new errors during the Module 9 session; login audit rows confirmed in `security_events` after 0003 hotfix; the login → dashboard → profile → 2FA settings → budget/transaction CRUD journey was manually verified clean on staterafinance.app 2026-05-30. **This journey did NOT exercise demo-workspace load, CSV import, or export — all three call unmounted routes (2026-07-03 audit). "Verified clean" was scoped to the paths walked, not the whole surface; the manual walkthrough is exactly the blind spot Module 10a's contract test closes.**

**Module 9 gap — 2026-07-03 professionalization audit** `5 unmounted-route escapes; same class as the analytics 404 window`
1. **Finding**: 5 frontend api.ts call sites resolve to routes not mounted in app.ts. User-reachable: (a) `authApi.loadDemoData`/`clearDemoData` → `/api/auth/demo-data[/clear]` — WorkspaceChoicePage's primary post-signup CTA 404s; (b) `uploadApi.preview`/`importCommit` → `/api/transactions/upload-preview`+`/import-commit` — ImportDialogs broken; (c) `exportCsv`/`exportXlsx` → `/api/transactions/export-{csv,xlsx}` — ProfilePage buttons 404 (deferred 3c). Guarded/dead: (d) `templateSuggestions` (gated behind `useTemplateSuggestions(enabled)`); (e) `featuresApi` `/api/features` (no UI caller) and `bankApi` (BankPage.tsx not routed).
2. **Why Module 9 missed them**: 9.x walked the analytics + CRUD surface only; the "full user journey" verification (fix-forward point 7) never clicked demo/import/export. Unit tests mock at the api-function level (TODO(module-9-network-mocking)), so no test asserts a URL resolves to a real route.
3. **Remediation (2026-07-03 decisions)**: Module 10b — MOUNT demo-workspace (a) and CSV import (b) as Flask ports (each its own propose→approve cycle); REMOVE export buttons (c), template-suggestions plumbing (d), and dead featuresApi/bankApi/BankPage (e). Module 10a — contract test to prevent recurrence.
4. **Lesson**: a manually-walked "clean journey" proves only the paths walked. The guarantee that *every* declared frontend call hits a real route requires a mechanical check against the backend route table — not a smoke walkthrough.

## Standing rules for every module

- **Persist-first for multi-session modules (earned phase4 SC-1/2/3, 2026-07-17).** A module's Phase A proposal and its approval block(s) are saved to `docs/modules/` as the module's **first action, before any implementation** — the approved lineage (rulings, conditions, verbatim spec sections, approved copy) must survive context compaction. A relayed approval that lives only in conversation is a single point of failure: SC-1/2's §8 and the approved legal copy were nearly lost to a summarized-away relay, and a re-send arrived as an empty placeholder. Write the record to disk first; implement from the file.
- **Relay/approval blocks must be self-contained (earned phase4 SC-1/2/3, 2026-07-17).** When relaying an approval or spec into a session, include the full text inline — no placeholders (`[paste here …]`), no "re-sent alongside" or assemble-on-paste steps. An approval that references content not present in the same message is not actionable and must be treated as a blocker (do not fabricate the missing content). The SC-1/2 §8 re-send arrived empty twice; the fix is self-contained blocks.
- **Port-source reference (added 10b-2, 2026-07-04).** Ports quote **personal-finance** (`/Users/khaledalrashidi/DevLocal/personal-finance`, HEAD `202a1548`) as the primary Flask reference — the 2026-07-04 recon established it is the ancestor of the Hono port (name-string memorized storage, `MEMORIZED_PRUNE_ALL/SINGLE_DAYS` naming, no `is_pinned`). Wherever **personal_statera** (`/Users/khaledalrashidi/personal_statera`, HEAD `73583fe`) diverges on the ported surface, quote **both** versions and flag the divergence for explicit operator choice in the proposal (personal_statera is later evolution of the same feature; neither checkout is an exact port-source match — e.g. it added `WorkspaceChoicePage` and `is_pinned` that the Hono port variously does/doesn't track). See the 2026-07-04 recon parity table.
- **Propose before implementing.** Show Flask source, propose Hono equivalent, wait for approval.
- **One commit per module** (or sub-commit if large), prefix `phase-3: <module>`.
- **Decimal.js for ALL arithmetic on KWD amounts.** Drizzle decimal returns strings — never pass to `Number()` or use `+` arithmetic. Grep for `Number(`, `parseFloat(`, and `+` near amount fields before each commit.
- **Response envelope:**
  - Success: `{ ok: true, data: <resource>, error: null, meta: {} }`
  - Error: `{ ok: false, data: null, error: <msg>, code: <string> }`
- **Timestamp format:** `date.toISOString().replace(/\.\d{3}Z$/, '+00:00')` — matches Flask's `+00:00` format, no milliseconds.
- **Validation:** The standard for route-input validation is **in-handler `zod` `.safeParse` + the shared `zodErrorToEnvelope()` helper** (`routes/route-helpers.ts`) — **not** `@hono/zod-validator`/`zValidator` middleware (which is not even a dependency). Rationale: the helper keeps the 400 error envelope byte-identical across routes while each handler keeps its own **diagnostic ordering** and business-rule checks (existence/conflict/funding), which middleware-level validation would flatten. This supersedes the earlier "zValidator is the standard" line (Phase-A Decision 1, TODO(phase4-zod-adoption), 2026-07-11). **Reality:** validation is migrating onto this mechanism incrementally (TODO(phase4-zod-adoption), Module 10d — B0 done 2026-07-11: helper + the 6 already-zod sites in categories/merchants/aggregation converged onto it; B1 done 2026-07-11: NEW shape-only zod schemas added to budgets (`GET /`, `POST /`) + notifications (`GET /budget-alerts`, `POST /budget-alerts/dismiss`), all emitting byte-identical Hono wire strings — the `POST /budgets` combined-presence-then-format message is modeled with `superRefine` + early return to preserve first-fail ordering (D1); the `alert_key` `String(…??"").trim()` coercion stays hand-rolled with zod validating only the normalized non-empty shape (D2)). Shape checks move to zod; money/Decimal parsing, business-rule checks, and diagnostic-order-sensitive checks stay hand-rolled in the handler **by design** (Decision 2). Do not describe the codebase as fully zod-validated or as "no hand-rolled validation."
- **Auth:** `requireAuth` middleware on every business-logic route. No exceptions.
- **Rate limiting:** Reuse the Redis-backed middleware from transactions; apply to any endpoint Flask rate-limited. **TODO(phase4-rate-limit-backfill) DONE 2026-07-10** — budgets, categories, merchants, and notifications routers are now rate-limited (`readRateLimit` 60/min, `writeRateLimit` 30/min, `heavyWriteRateLimit` 20/min for `POST /api/budgets` + the two remaps), each reusing `createRateLimiter` exactly. Flask left these four unlimited, so the limits are deviation-by-addition; see the Public API contracts "Rate-limit 429 body" entry for the per-route table and the new standard-envelope 429.
- **Sentry on all swallowed errors.** Never silently ignore.
- **Migrations must be additive and backwards-compatible.** New NULLABLE columns, new tables, new indexes only. NEVER DROP, RENAME, or change the type of in-use columns in a single migration. Destructive changes require a two-deploy sequence: (1) deploy code that tolerates both old and new schema, (2) migrate, (3) deploy code that drops old-schema support. Rationale: MySQL DDL is not transactional; a failed migration may partially apply with no automatic rollback. The CI deploy pipeline aborts before code rollout if drizzle-kit migrate fails, but cannot undo partial DDL. Additive-only migrations make rollback safe: old code running against a migrated schema can ignore new columns it doesn't know about.
  - **Approved deviation — phase4 SC (debt/savings removal), ruling (a), 2026-07-14, this module only:** the DROP of `savings_goals`, `debt_accounts`, and the `user_profiles.has_debt_choice` column is exercised under the SAME two-deploy discipline this rule mandates, NOT collapsed to one deploy. Deploy 1 (SC-1/2) ships code that stops reading/writing those tables/column while they still exist; deploy 2 (SC-3) runs the three one-DDL-per-file DROP migrations after deploy 1 is proven live. The DROPs are therefore backwards-compatible in the only sense that matters — the running code no longer references the dropped objects. This exception is scoped to this module; the standing additive-only rule is otherwise unchanged.
- **One DDL statement per migration file.** MySQL DDL is not transactional — multiple ALTER TABLE statements in one file can fail mid-way and leave the schema partially migrated. One DDL operation per file ensures a failure is cleanly "this migration didn't apply" rather than "this migration half-applied." Use IF NOT EXISTS / IF EXISTS guards where applicable for safe re-run.
- **Fire-and-forget writes** (audit, tracking, cache bust): try/catch + Sentry, never block the main response.
- **Deviations from Flask:** flag in the proposal *before* implementing, not in the summary after. Add a "Deliberate deviations from Flask" comment block at the top of files where deviations exist.
- **TODO format:** `// TODO(module-X-name):` for grep-able deferred work.
- **Update this file:** at the end of every module commit, update the "Migration status" section above to reflect the new state. This is part of the commit, not a follow-up.
- **Close-out report — mandatory sections (added design-5.4a, 2026-07-13).** Every module/sub-commit close-out MUST contain: (1) the **verbatim** test-run tail + the **captured exit code** of the exact command CI runs (pass/fail counts alone are not verification — assert `$? == 0` and confirm no `Errors`/`Unhandled Errors`/`Unhandled Rejection` section, per the 10f-fix rule); (2) the **verbatim** `tsc --noEmit` output + its captured exit code; (3) the baseline-line hunk (old → new) whenever test or file counts move. **Close-outs missing any of these three sections are auto-returned without review.** This codifies the verification discipline the 10f-fix false-green established as a durable rule. **Template notes (N1/N2, folded in at the design-5.4 deploy, 2026-07-15):** N1 — the item-(1) test tail MUST include the `Test Files N passed (N)` summary line, not only the `Tests N passed` line (a suite can pass every test it loads while a whole test *file* fails to load — the file-count line is what surfaces that). N2 — the item-(3) baseline change is confirmed **with the diff hunk itself** (the `git diff` old→new lines), not a prose restatement of the counts.
- **Sequential gates are sequential.** When a prompt requires a verification report (e.g., a status check on a previous module) before work on a new module proceeds, deliver the verification report and wait for explicit approval before starting the new work. An approval issued conditionally on a prior report being clean is not standing approval to ship in parallel. If a verification surfaces a problem, the new work pauses until the prior gap is resolved.
- **Frontend api.ts envelope unwrapping.** Every business method in `apps/web/src/lib/api.ts` must use `readApiData<T>(payload)` to unwrap the Hono response envelope. `apiFetch<T>` returns `res.json()` raw — the generic type parameter is a TypeScript hint only. Reading root-level fields on the return value silently returns `undefined` because all business data lives under `data:`.
- **Frontend readErrorCode field.** The `readErrorCode` helper reads `rec.code` — the top-level field Hono emits in the error envelope (`{ ok: false, data: null, error: "...", code: "..." }`). Do not read `rec.error_code` (Flask-era name; does not exist in Hono).
- **Migration journal atomicity.** A `.sql` file under `apps/api/src/db/migrations/` must have a matching entry in `meta/_journal.json` and a corresponding `meta/NNN_snapshot.json`. These three files are an atomic unit — `drizzle-orm/migrator.js` iterates `journal.entries` exclusively; unlisted SQL is invisible to every migrate run. Always use `drizzle-kit generate` to produce all three together from `apps/api/`. Never write or edit migration files by hand. See **TODO(migration-journal-guard)**.
- **Migration second-run check.** After `drizzle-kit generate` produces a migration, run `drizzle-kit generate` a second time immediately. It must report "No schema changes." A second run that reports changes means the snapshot is stale and the prior migration is incomplete. Hand-edited snapshot files cannot provide this guarantee.
- **Deferred features must have no UI entry point.** "Deferred and reachable but broken" is not an acceptable state. When a backend route is deferred, remove all frontend nav links, route entries, and UI surface in the same commit window. Do not leave dead UI in production.
- **CSP is enforcing (as of 2026-07-10; narrowed design-5.4e 2026-07-13).** Any NEW external resource the frontend introduces — a font host, an image/CDN origin, a third-party script, or a cross-origin `connect`/fetch target — MUST be added to the `Content-Security-Policy` header in `deploy/Caddyfile` in the SAME commit that introduces it, or production will block it (it will silently work in dev). The current policy is **same-origin-only** except `img-src data:` (2FA QR) and `style-src 'unsafe-inline'` (Recharts inline styles — do NOT remove without separate scoping). Fonts are self-hosted (`@fontsource`), so `style-src`/`font-src` are back to `'self'` and the former `img-src https:` was narrowed to `'self' data:` (design-5.4e). There is no Report-Only safety net anymore — a missed directive is a production breakage, not a console warning.
- **Redesign-branch ride-along on deploys (operator instruction, 2026-07-10 — supersedes the earlier freeze).** Commits from the parallel UX-redesign thread (e.g. `design-0`/`design-1`) MAY ride along on a deploy merge to `main`, **provided each riding commit is (a) checked for CSP compatibility — no new external origin without a same-commit `deploy/Caddyfile` change, per the enforcing-CSP rule above — and (b) named in the deploy report.** This corrects the earlier "ship both" framing: the ride-along is a standing allowance under those two conditions, not a per-deploy ad-hoc call. Before any FF to `main`, **diff `origin/main..HEAD`** (the actually-deployed ref — **NOT** local `main`) and enumerate the riding commits; if one is not the deploy's own work, verify (a) and list it in the report. (Mid-incident is the one time to reconsider bundling — surface it rather than assume.) **Method correction (2026-07-10):** the first application of this rule diffed local `main..HEAD` and missed a riding commit (`21baaf1` design-2) because the parallel thread had already advanced *local* `main`; it was CSP-safe so it shipped harmlessly and was caught immediately post-FF, but the check MUST compare against `origin/main` (or reconcile with the live `/healthz` SHA), since local `main` is not a reliable proxy for what production is running.
- **Attribution provenance (operator standing rule; review-channel block "B1 ADDENDUM — reconciles except one provenance item, 2026-07-11").** Any report claim of the form "per your ruling / approved / operator condition / operator-approved" MUST cite the specific review-channel block it derives from (date + block title). Operator selections made via `AskUserQuestion` or ANY other in-session mechanism are NOT review-channel rulings: quote them verbatim in the next report and label them **"operator selection (in-session) — pending review-channel ratification"** until the operator ratifies them in the review channel. Dates/attributions in committed docs derive from the cited ruling-block date, NOT the session system clock. Rationale: two attribution slips in two cycles — D1/D2/exclusion reported "per your rulings" and the tangle-resolution reported as an "Option-C ruling", both actually in-session `AskUserQuestion` selections; and a doc line stamped `2026-07-12` (session wall-clock) instead of the `2026-07-11` ruling-block date. Citing the block makes the slip structurally impossible rather than merely prohibited.

## Design-track standing constraints

The UX-redesign thread (`design-*` commits) carries a set of constraints that used to live only in prompt lineage. Recorded here so future design modules do not depend on a specific prompt to know the rules. (Established across design-5.x; codified design-5.4a, 2026-07-13.)

- **Scope boundary.** No debt/goals restyle or deletion — removal has its own pending module. Token-swap effects that flow through shared tokens are fine; targeted edits to debt/goals surfaces are not.
- **Protected journey.** QuickAdd internals are untouchable (token flow-through only). The FAB topology settled in 5.3 is fixed: icon-only FAB (56px, `bg-primary`, `z-40`, `aria-label="Log transaction"`, tooltip `"Log transaction — L"`) is the sole visible QuickAdd trigger; global "L" shortcut with focus/overlay guards; no header trigger.
- **No renames** of files, exports, or components.
- **Pinned strings.** FAB `aria-label` `"Log transaction"` + tooltip `"Log transaction — L"`; the two legal `data-testid`s `commitment-backup-retention` and `commitment-statement-files`.
- **Logical properties only** — no physical `ml-`/`mr-`/`pl-`/`pr-` additions (Phase 6 RTL sweep depends on this).
- **`components/ui/*` stays direction-free** — primitives carry no brand-specific styling; branding lives in tokens and page/feature components.
- **Named regression files stay green and untouched** (additionally to the whole suite staying green): `components/layout/AppShell.test.tsx`, `components/pages/legal/PrivacyPolicyPage.test.tsx`, `components/pages/legal/TermsPage.test.tsx` (confirmed design-5.4, 2026-07-13).
- **Caddyfile CSP ride-along.** Any resource change and its CSP change land in the SAME commit (mirrors the enforcing-CSP standing rule).
- **"Ink & brass" identity (approved 2026-07-12, direction A).** Brass is rationed: logo mark, eyebrows, and free-to-spend only. Buttons are ink, not brass. The Month Snapshot bar recolors ink/neutral/brass — the traffic-light red/amber/green treatment is retired. Success keeps muted sage; warnings are a rust family (`--warning` = `18 70% 41%` light / `18 74% 64%` dark, design-5.4b).
- **Brass-as-text is not AA-viable in light mode** (`text-accent` on light surface ≈ 2.08). Free-to-spend uses `--accent-strong` (darker brass, large/bold ≥ WCAG large-text threshold); eyebrows use a **non-text brass cue** (leading dot or 2px underline) with label text staying `text-muted-foreground` in BOTH modes. No brass eyebrow text anywhere.
- **Chart-token exemption (R4, design-5.4).** Data-visualization palettes (`--color-chart-*`, `--card-chart-*`) are EXEMPT from the brass ration: decision (i) governs UI *accenting*, not data *encoding*. Chart tokens flow through to ink/brass as the token migration dictates; this is not a ration violation. (If charts are ever decoupled to a neutral data palette, replace this line — do not leave the decoupling implicit.)
- **Test-impact caveat (standing).** "No test impact expected" is a prediction, not an allowance. Any red test, or any forced selector/class edit to a test, surfacing during implementation is a NAMED forced edit requiring explicit approval before it ships (5.3 precedent).
- **R10-class discovery that changes an APPROVED value is stop-and-ask, not ship-with-named-deviation (R14, design-5.4).** When a verification step (contrast reproduction, size/threshold check, etc.) falsifies an already-approved token/value — e.g. design-5.4c-1's `--accent-strong`, where the R10 script showed the A3-approved `L40` = 2.97 on the light bg (sub-3.0), defeating the approved intent, and `L38` (3.51/3.27) was shipped instead — the correct move is to STOP and get the corrected value approved before committing. Naming the deviation in the commit body is the mitigation, NOT the mechanism: a named deviation does not substitute for re-approval of a changed approved value. (The 5.4c-1 `L38` value was retroactively approved under R14; the process rule stands for next time.) Distinguish from a value the operator's ruling explicitly delegated to the implementer (those ship as attributed implementation choices, e.g. R15 below).
- **Attribute the mandate to the operator, the resolution to the implementer (R15, design-5.4).** When a ruling mandates a property but leaves the means open — e.g. R6 required the brass free-to-spend KPI to meet the WCAG large-text threshold, and the fix (enlarging `text-lg`→`text-[1.25rem]` semibold so 20px clears 18.66px at accent-strong's 3.39 on the real inner-card bg) was the implementer's — the mandate is the operator ruling and the specific resolution is an attributed IMPLEMENTATION CHOICE. (The design-5.4d commit body filed the enlargement under an "Operator ruling" heading; per R15 that resolution is an implementation choice within R6's mandate — recorded here as the durable attribution rule.)
- **Deploy record — design-5.4 shipped to production (2026-07-15).** `main` fast-forwarded `fd20169`→`5d42c03` and deployed GREEN — GitHub Actions **Deploy run `29424845136`**, exit 0 (resolve-sha → test → build-push → deploy); live `/healthz` + `/readyz` both report `5d42c03`. **8 riders over base `fd20169`** (= prior live; the zod-B1 base commit is NOT a rider): design-5.4 ×6 — `8446364` (5.4a), `40ea178` (5.4b), `56e27b4` (5.4c-1), `53ce85b` (5.4c-2), `83dab64` (5.4d), `acfd452` (5.4e) — plus close-out `5d42c03` and `aa39718` (10d attribution-provenance, CLAUDE.md-only). **CSP policy change activated by `acfd452` (5.4e), named:** `style-src`/`font-src` narrowed to `'self'` (Google Fonts hosts removed; Space Grotesk + IBM Plex Mono self-hosted via `@fontsource`) and `img-src` narrowed `https:`→`'self' data:` — atomic with the font self-host + index.html `<link>` removal in the one `statera-web` image (no in-deploy ordering hazard). No schema migrations rode. Rollback = `workflow_dispatch sha=fd20169` (image + Caddyfile revert as one SHA-tagged unit). **5.4e is NOT closed by this deploy** — the R9(b) operator console walk (Safari; hard-reload login/dashboard/insights/import/2FA-QR, console open, fonts render + zero `Refused to load`) is the acceptance gate, and its close ruling comes from the review channel after the walk.

## Operational work conventions

Unplanned operational work that touches production state (key rotations, server config changes, incident recovery, etc.) must leave two artifacts before the session ends:

1. A new entry under `## Module fix-forwards` in this file, dated, summarizing what was done and the durable lessons.
2. A handoff document at `docs/recovery/YYYY-MM-DD-<slug>.md` capturing what was deliberately not done, open decisions for the next session, and a suggested opening prompt for the next Claude conversation.

The fix-forward is the durable lesson record; the handoff is the operational continuity document. Both are required because they serve different readers and different time horizons. See `docs/recovery/2026-05-22-operator-key-rotation.md` as the precedent.

## Tooling conventions

- **Claude Code permission rules (added 2026-07-06):** committed `.claude/settings.json` is load-bearing — `permissions.allow` scopes the verification loop (api/frontend typecheck + test + contract:generate + INTEGRATION api suite + `rg`) to run prompt-free, and `permissions.deny` protects secret material (`Read` denies on `~/.config/sops/age/**`, `./secrets/**`, `~/.ssh/**`, `./.env`, `./.env.prod*`; `Bash` denies on `ssh`/`scp`/`sops`). Deny wins over allow at every scope, so the denies are a real guarantee; the config is a housekeeping change, not a Phase 4 module. Read-only git/grep/ls/cat need no allow rule (Claude Code built-in read-only set).

## Key architectural decisions (do not revisit)

- **Auth:** provider-agnostic OIDC via openid-client; Google as initial provider. Users table has `(auth_provider, external_id)` composite unique. No password column.
- **sv deny-list (added in 7a):** `requireAuth` validates session version exclusively via a Redis deny-list key (`sv_revoked:{userId}:{oldSv}`, 30-day TTL matching JWT expiry). Zero DB cost per request. Fail-open on Redis outage (consistent with rate-limiter behavior). Old JWTs without sv claim pass through and expire naturally within 30 days. Revocation writes the deny-list key via `revokeSessionVersion(userId, oldSv)`; also bumps `users.sessionVersion` in DB so new sessions issued after revocation carry the next sv. Do not revert to per-request DB lookup — that was Option 1 and was rejected.
- **statera_pending_2fa (added in 7b, extended in 7.5):** 5-minute HS256-signed JWT cookie carrying `{ userId, pendingAt, deleteIntent? }`. Issued by the OIDC callback when `totpEnabled=true`; real session cookie not issued until `/2fa/verify` succeeds. For delete-reauth flows, `deleteIntent=true` is embedded; on success `/2fa/verify` issues `statera_delete_intent` instead of a new session. Failure counter stored in Redis (`pending_2fa_failures:{userId}`, 5-min TTL, `MULTI/INCR/EXPIRE` pipeline). Max failures = 3; 3rd failure deletes the pending cookie (PENDING_2FA_RESTART). The pending cookie is independent of sv — `/2fa/verify` reads `users.sessionVersion` from DB at verify time.
- **statera_delete_intent (added in 7.5):** 15-minute HS256-signed JWT cookie carrying `{ userId, issuedAt }`. Scoped to `Path=/api/account` so it cannot be sent to unrelated endpoints. Issued by the delete-reauth OIDC callback (no TOTP) or by `/2fa/verify` (with TOTP, deleteIntent=true). `DELETE /api/account` verifies this cookie, checks userId match (anti-replay), then consumes it (deletes immediately). Flask used password + session token; Hono uses OIDC re-auth because there is no password column.
- **Account deletion tombstone (added in 7.5):** `security_events.is_tombstone` boolean column (migration 0003). Tombstone row inserted with `user_id=NULL, is_tombstone=true` BEFORE the purge loop. The purge DELETE for security_events uses `AND is_tombstone=false`, so the tombstone survives. Do not rely on NULL-WHERE semantics alone — the explicit column is the safety guarantee.
- **DB:** MySQL 8, utf8mb4, utf8mb4_0900_ai_ci collation, DATETIME(3) for timestamps, DATE for transaction dates.
- **Money types:** DECIMAL(10,3) or DECIMAL(12,3) for KWD; DECIMAL(6,3) for APR.
- **Bank sync deferred:** do not port `bank_consents`, consent receipts, `DataAccessLog`, or bank-sync Celery jobs. Schema tables remain from Phase 2.
- **Snapshot strategy:** Option B — rebuild only users with `last_login_at` within `SNAPSHOT_REBUILD_WINDOW_DAYS` (default 14). NULL `last_login_at` users included only if `created_at` is also within the window.
- **BullMQ concurrency:** Option B — p-limit with configurable concurrency, default 5 (`SNAPSHOT_REBUILD_CONCURRENCY`).
- **Per-user budget vs MySQL timeout:** per-user budget = `analyticsComputeTimeoutSeconds + 2` so the two error modes are distinguishable in Sentry.
- **Template emails deferred:** when adding, use a separate `email-templates.ts` module; preserve the Flask path-traversal guard (reject template names containing `..`, `/`, or `\`).
- **Production server:** Hetzner CPX31 (3 vCPU AMD, 8GB RAM) in Helsinki HEL1, ~€13.59/mo + IPv4 ~€0.60/mo. CX32 was unavailable in Helsinki at provision time; CPX31 is the AMD equivalent with faster per-core performance. The repo's Docker Compose configuration is the source of truth for production deploys. Manus AI (manus.im) was briefly considered at scaffold stage and removed in commit 13c709a; do not reintroduce platform-specific hosting assumptions. **Running stack (as of 8e close):** Caddy web service (statera-web image, ports 80/443 published, ACME state in `caddy_data` Docker volume, runtime config in `caddy_config`), API service (statera-api image, internal-only on 3000/tcp), worker service (same image, no exposed ports), MySQL 8 (named volume `mysqldata` bind-mounted to `${MYSQL_DATA_PATH:-/mnt/mysql-data}`), Redis 7. Migrate service runs only via `docker compose run --rm migrate`; not part of the always-on stack. The nginx service has been removed entirely; do not reintroduce.
- **Production domain:** staterafinance.app, registered through Cloudflare Registrar. DNS managed on Cloudflare with proxy disabled (gray cloud) — Caddy handles TLS end-to-end via Let's Encrypt ACME. Do not enable Cloudflare proxy (orange cloud) without reconsidering the TLS chain.
- **Operator SSH key (current)**: `id_ed25519_statera_v2`, comment `statera-operator-20260522`, public key `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAINw18iTyP7Jkk+HV5Llmwo1w8h20f1I0YUMReovzNuwB`. Rotated 2026-05-22 after laptop loss; see "operator-key-rotation 2026-05-22" in Module fix-forwards. Pre-rotation operator pubkey pruned from `/home/deploy/.ssh/authorized_keys` 2026-05-23 (see "orphan operator pubkey prune 2026-05-23" in Module fix-forwards); laptop-side pre-rotation keypair (`~/.ssh/id_ed25519_statera` + `.pub`) also removed. `authorized_keys` is now 2 lines: operator v2 line 1, CI deploy command-restricted entry line 2. CI deploy key (`statera-ci-deploy`, `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIDfODLG7HNE/YfmsHG3ERyypJje534hX22GZ18jgz+iE`) is installed on server with `command=deploy-bootstrap.sh` restriction; private key is in GitHub Actions `DEPLOY_SSH_KEY` secret; pipeline operational as of 2026-05-23.
- **Apex architecture (8e):** staterafinance.app serves both frontend (`/`) and API (`/api/*`) from a single Caddy virtual host. No CORS complexity, same-origin cookies, single TLS cert. The Caddyfile is committed at `deploy/Caddyfile` and baked into the `statera-web` Docker image at build time — NOT copied to `/etc/caddy/Caddyfile` by the deploy pipeline. Any Caddyfile change requires rebuilding the `statera-web` image; CI does this automatically on push, image is tagged with the resolved SHA, deploy pulls it. Health endpoints (`/healthz`, `/readyz`) are routed at root by Caddy, not under `/api/*` — see Public API contracts.
- **Frontend delivery model (8e):** The `statera-web` Docker image carries both Caddy and the built `apps/web/dist/` together. Built in CI alongside `statera-api` via `deploy/web.Dockerfile` (multi-stage: node:22-alpine builds dist, caddy:2-alpine carries it). Pushed to GHCR tagged with `${GIT_SHA}` and `:latest` — identical tagging convention to `statera-api`. Frontend and API are versioned together by SHA: a rollback of one is implicitly a rollback of the other because both images pulled by `docker-compose.prod.yml` reference `${GIT_SHA}` from the same deploy. Alternatives considered and rejected at scoping time: (A-separate) separate static-server image, (B) rsync built dist to server at deploy time, (C) build frontend on server during deploy. Rejected respectively because: (A-separate) adds complexity without benefit for this single-team project; (B) introduces mutable on-server state that the thin-bootstrap pattern was designed to avoid, plus rollback semantics get weird; (C) puts Node toolchain on production server, expanding attack surface.
- **Thin-bootstrap deploy pattern (8d):** `authorized_keys command=` on the CI deploy key invokes `deploy/deploy-bootstrap.sh`, not `deploy.sh` directly. Bootstrap does: `git fetch origin`, `git rev-parse --verify "${GIT_SHA}^{commit}"`, `git reset --hard "$GIT_SHA"`, then `exec bash deploy/deploy.sh`. The `exec` replaces the process, loading the just-checked-out `deploy.sh` (self-update fix) and ensuring the secrets file on disk reflects the new commit before sops decryption runs (secrets-before-checkout fix). `deploy-bootstrap.sh` is intentionally stable and thin — all deployment logic lives in `deploy.sh`. Operational constraint: bootstrap cannot self-update via CI; changes must be applied manually on the server.

## Helpers and patterns to reuse (do not reimplement)

- `lib/name-key.ts` — `buildNameKey`, `forceUniqueNameKey` (use for any text normalization)
- `lib/kd.ts` — `formatKd` (KWD formatting; 3 decimal places)
- `lib/crypto.ts` — AES-256-GCM field encryption with `enc1:` prefix (preserve prefix exactly for future rotation)
- `lib/email.ts` — `sendEmail`, `sendEmailBackground`
- `lib/sentry.ts` — `initSentry`, `sentryBeforeSend`
- `lib/transaction-lib.ts` — `learnTransaction` (canonical; memorized module consumes it, does not duplicate)
- `lib/dashboard-snapshot-lib.ts` — snapshot helpers
- `lib/analytics-cache.ts` — cache layer, `withAnalyticsTimeout`, `getDashboardMetricsWithCache`
- `lib/analytics-helpers.ts` — `currentLocalDate`, `currentMonthKey`, `calendarMonthBounds`, `buildMonthWindow`, `ymExpr`, `roundedKd`
- `lib/payday-lib.ts` — `incomeCategoryFilter`, `expenseCategoryFilter`, `currentPayPeriod`
- `lib/income-lib.ts` — `detectMonthlyIncome`, `resolveIncomeForPeriod` (typed `IncomeSource` / `IncomeResolution`)
- `lib/intelligence-lib.ts` — `buildIncomePatternPayload`, `buildRecurringPatternsPayload`, `buildSnapshotPayload`, `confidenceFromVariance`, `confidenceFromIntervalVariance`, `classifyRecurringFrequency`, `intervalVarianceRatio`, `classifyRecurringGroup`
- `db/sql-helpers.ts` — `nullsLast` helper
- Rate limit middleware from transactions — reuse for new endpoints
- Worker task tracking from 1c — `markWorkerTaskStarted`, `markWorkerTaskFinished` (call once at batch start/end, not per-user)
- `tools/capture-flask-fixtures.py` — Flask fixture capture tool (subcommands: income-pattern, recurring-patterns, snapshot). Seeds deterministic data into the live PostgreSQL container, calls Flask payload builders, prints JSON, rolls back. Run before implementing each 5c sub-commit to capture expected values for equivalence tests.
- `routes/route-helpers.ts` — `parseIntParam` (consolidated from aggregation.ts and intelligence.ts local copies in 5c-3)
- `routes/auth.ts` — `auditSecurityEvent(db, eventType, opts)` fire-and-forget helper writing to `security_events` table (use for all auth security events); `verifyDeleteIntentToken(token)` exported for `routes/account.ts`
- `lib/account-deletion.ts` — `hashEmail(email)` SHA-256 (trim+lowercase, no salt), `purgeUserAccountRows(userId, emailHash, ipAddress, userAgent, db)` (tombstone-first, 13-table purge, soft-delete)
- `deploy/web.Dockerfile` — multi-stage build for the `statera-web` image: node:22-alpine stage builds the Vite frontend (`pnpm --filter statera-frontend build`), caddy:2-alpine stage carries the built dist into `/srv` plus the Caddyfile at `/etc/caddy/Caddyfile`. GIT_SHA stamped via ARG → ENV for image versioning. Reference for any future image that combines a built frontend with a runtime server.
- `deploy/Caddyfile` — production reverse-proxy config for apex architecture: ACME via Let's Encrypt, HTTPS-redirect from :80, `/api/*` + `/healthz` + `/readyz` reverse-proxied to api:3000, `/assets/*` with immutable 1-year cache, SPA fallback for `/*`, HSTS + Report-Only CSP. Reference for any TLS work — staging environment if ever added, secondary apex setup, etc.

## Test conventions

- **Route tests:** Proxy mock pattern (mock Drizzle db), unit-level.
- **Drizzle proxy-mock pattern:** use a flat self-referential proxy where every property access returns either a `then`-resolver (for awaitability) or the same proxy (for chaining). Do NOT use outer/inner alternation — Drizzle chain lengths vary by query (3 for `select().from().where()`, 4+ when joins are present), and alternation only resolves on chains that happen to end on the thenable side. The flat pattern resolves any chain length. See `apps/api/src/lib/income-lib.test.ts` for the canonical example.
- **Lib tests:** fixture-based equivalence against Flask output for any non-trivial calculation. Captured Python output is hardcoded as expected value.
- **Integration tests:** gated on `INTEGRATION=true` env var, run against docker-compose MySQL.
- **TODO(integration-rate-limit-test-isolation)** (recorded phase4 SC-1/2, 2026-07-16, review-channel R3): four hermetic-only unit cases — `routes/budgets.test.ts` "POST /api/budgets rate limit … never reaches the handler" + "B1 zod … D1 ordering", `routes/merchants.test.ts` + `routes/categories.test.ts` "POST … rate limit …" — FAIL under `INTEGRATION=true` because the module-wide `ioredis` mock (`src/test/redis-mock.setup.ts`) is skipped then, so they hit REAL Redis with a stateful, non-reset rate-limit counter. Proven pre-existing (identical 4 fail on clean HEAD with flushed Redis, added by rate-limit-backfill 2026-07-10 + zod-B1 2026-07-11, after the last INTEGRATION baseline); NOT caused by SC-1/2. Fix: these hermetic-only rate-limit/zod tests must either reset Redis per-test (`FLUSHALL` in `beforeEach`) or `skipIf(INTEGRATION)`. Same silent-rot class as the 7.5 fix-forward. Own propose→approve cycle under 10d. Until fixed, the INTEGRATION suite's real signal is the 6 `*.integration.test.ts` files (which pass); these 4 unit failures are the known-noise exception.
- **INTEGRATION-gated tests live in dedicated `*.integration.test.ts` files with NO module-level db/redis mocks (added 7.5 fix-forward, 2026-07-04).** An integration `describe` placed inside a unit-test file that `vi.mock`s `../db/connection` (or ioredis) gets the mocked client — `getDb()` returns `undefined` and the test throws on its first DB call. It then skips in CI (hermetic) and can only fail once someone runs `INTEGRATION=true` locally, so it can sit broken indefinitely — the silent-rot pattern. Precedent: `account-deletion.integration.test.ts` was extracted from `account-deletion.test.ts` after being structurally never-runnable since `5b7bbba` (see "7.5 fix-forward — account-deletion integration test mock contamination"). Run the integration suite (`INTEGRATION=true`, live stack) before treating any new integration test as real.
- **Atomicity tests:** required for any route that uses `db.transaction()`. Mock transactions always commit; use real DB to verify rollback behavior.
- **Decimal assertions:** assert string equality on amounts (`expect(tx.amount_kd).toBe("12.500")`), not numeric equality.

- **Playwright e2e suite is NOT a CI gate and carries pre-existing rot (recorded design-5.5, 2026-07-16).** The `apps/web/e2e/*.spec.ts` suite runs via a separate `test` script, not the `test:unit` CI gate, and is not run on deploy. design-5.5's V1 repo-wide sweep — the first to scan `*.spec.*`, which Phase A's `*.test.*`-only scan missed — found it stale: `auth-pages.spec.ts` still tested `/register` + `/forgot-password` (pages deleted in 9.3b), undetected until this module. design-5.5 deleted only the `/dev-ui`-specific e2e coverage (two `visual.spec.ts` "dev-ui gallery" tests + their two snapshots, one `auth-pages.spec.ts` test) as collateral of the DevUiPage deletion; it did NOT revive, rewrite, or audit the rest of the suite — the Option-B generic-rewrite was rejected because rewriting tests inside a dead suite manufactures apparent-but-inactive coverage (the commit-without-activation class). **A dedicated e2e disposition module (run the suite, then fix or formally retire it) is on the open ledger.** Related unit-layer gap: TODO(module-9-network-mocking).
- **Frontend test baseline (as of phase4 SC-1/2 debt/savings removal, 2026-07-16):** `pnpm --filter statera-frontend run test:unit` = **166 tests / 35 files**, `tsc --noEmit` 0 errors, exit 0 (no Errors/Unhandled section). **SC-1/2 removed the debt/savings frontend surface: 3 whole test files deleted (`budget/goals-tab.test.tsx`, `profile/DebtDialog.test.tsx`, `dashboard/debt-summary.test.tsx` — the last tested the now-deleted `PlanSummaryPanel`), plus 2 in-file test removals (`BudgetPage.test.tsx` "Goals & Debt tab" case, `dashboard/safe-to-spend.test.tsx` "savings-goal reserve details" case); the safe-to-spend + demo-workspace-banner fixtures/assertions were updated to drop debt/savings fields. Net −16 tests / −3 files vs the 182/38 design-5.5 line. The 3 legal-copy edits (Privacy/Terms) needed NO test change (moot pre-approval per C4 note — the legal tests never asserted the debt/savings phrasing).** Prior baseline (as of design-5.5, 2026-07-16): **182 tests / 38 files**, exit 0. **design-5.5 (end-of-phase dead-code / dev-surface pass) added ZERO unit tests and forced ZERO unit-test edits.** It deleted the orphaned `TransactionsHero` export (+ its now-unused `useQuery`/`formatKD` imports), the unreachable `DevUiPage` (page + `/dev-ui` route + CommandPalette `ui-gallery` entry + `Palette` import) and the DevUiPage-only `.badge-success/.badge-warning/.badge-danger/.badge-neutral` CSS, and the R5-deferred dead decorative brand-gradient family (`--panel-accent-*`/`--panel-border`/`--card-line-1..3`/`--card-glow-1..4`/`--card-wash-1..3` at all three sites + `.panel-soft`/`.card-accent`/`.card-edge`/`.card-pop` + the `.theme-*` token block, with the inert `.theme-*` class strings stripped from the four page wrappers). No Vitest (`*.test.*`) file references any deleted item, so the unit baseline held at 182/38. **design-5.5's named forced edits are e2e/Playwright + docs only** — NOT counted in this unit baseline (Playwright is a separate, non-CI suite): two `visual.spec.ts` "dev-ui gallery" tests + their two `dev-ui-{light,dark}-chromium-darwin.png` snapshots deleted, and the `auth-pages.spec.ts` "dev-ui route requires authentication" test deleted (R-A1/R-A2); plus the `/dev-ui` doc lines in `apps/web/README.md` + `apps/web/memory/MEMORY.md` (R-A3). Prior baseline (as of design-5.4, 2026-07-13): **182 tests / 38 files** — the ink & brass migration (rust 5.4b, ink/brass tokens + `--accent-strong` 5.4c-1, gradient retirement + flat brass mark + favicon 5.4c-2, Month-Snapshot traffic-light retirement 5.4d, self-hosted fonts + CSP narrowing 5.4e; CLAUDE.md 5.4a) added ZERO tests and left the three named regression files (`AppShell.test.tsx`, `legal/PrivacyPolicyPage.test.tsx`, `legal/TermsPage.test.tsx`) green + untouched; count held 182/38 across all six sub-commits. Prior baseline (as of design-5.3, 2026-07-12): **182 tests / 38 files**. design-5.3 (single-trigger FAB + "L" shortcut + dialog parity + insights layout) added **+3** tests, all in `components/layout/AppShell.test.tsx` (the L-shortcut guards: fires-with-no-modal, suppressed-in-text-input, suppressed-while-dialog-open); no files added/removed; the two existing AppShell quick-add cases had only their FAB selector string updated (`"Add transaction"` → `"Log transaction"`, per the aria-label ruling) with byte-identical behavior assertions, and the test harness gained a `TooltipProvider` wrapper (AppShell's FAB tooltip needs it). Items 3/4 were layout-only (no test add/remove). Prior baseline (as of the design-3 ride-along, 2026-07-11): **179 tests / 38 files**, exit 0 (no Errors/Unhandled section). The `TODO(phase4-rate-limit-backfill)` commit (`0feb513`) added **zero** frontend tests (backend-only). The +3 tests / +1 file vs the prior 176/37 line is entirely the **design-3** UX-redesign ride-along (`f593315`, 11 frontend files, transaction-dialog rebuild): new `transactions/dialogs.test.tsx` (+1 file — form-submit / suggestion-apply / arrow-nav cases ported in from `expenses/dialogs.test.tsx`) plus `useQuickAdd` mocks added to `ExpensesPage.test.tsx` / `IncomePage.test.tsx`; net +3 after the ported cases left `expenses/dialogs.test.tsx`. (Recorded here to stop the ride-along baseline drift flagged on the design-0/1 cycle — this is the third such cycle.) Prior baseline (as of budgets-crash typed-drift fix, 2026-07-10): **176 tests / 37 files**, `tsc --noEmit` 0 errors, exit 0 (no Errors/Unhandled section). That commit added **+3**: `budget/sections.test.tsx` +2 (real-`IncomePlanningCard` string-`profile_context` typed-drift regression, proven RED-first, + the null-profile empty prompt) and `BudgetPage.test.tsx` +1 (both-tabs empty-account render; `beforeEach` now clears `localStorage` so the goals-tab test's persisted tab doesn't leak). The rest of the jump from the previously-recorded 166/36 absorbs the **design-0 + design-1** redesign tests that merged via ride-along deploys (2026-07-09/10) without updating this line (+1 file, +7 tests across those two commits). Prior baseline (as of 10c-content, 2026-07-06): 166 tests / 36 files, 0 typecheck errors, exit 0 (no Errors/Unhandled section). 10c-content (legal-copy fill) changed the two legal test files IN PLACE (rewrote the scaffold/pending-marker assertions in `PrivacyPolicyPage.test.tsx` + `TermsPage.test.tsx` to assert markers are gone and the two commitment slots carry content) — **no `it` blocks added or removed, count unchanged at 166 / 36 vs the 10c-3 baseline.** Prior 10c-3 baseline (2026-07-06): 166 tests / 36 files. 10c-3 added `DeleteAccountConfirmPage.test.tsx` (5) + `profile/DataPrivacySection.test.tsx` (4) + 1 TwoFactorVerifyPage delete-intent case + 2 LoginPage `?deleted=1` cases → +12 vs 10c-2's 154; +2 files. Prior 10c-2 baseline: 154 tests / 34 files. 10c-2 added `legal/PrivacyPolicyPage.test.tsx` (4) + `legal/TermsPage.test.tsx` (3) + 1 LoginPage footer-links case → +8 vs 10b-3's 146; +2 files. Prior 10b-3 baseline: 146 tests / 32 files (unchanged since 10b-1 — 10b-2 added a one-line `return` fix; 10b-3's only frontend change was dropping `.xls` from ImportDialogs; no tests added/removed; the earlier "151" (9.7f) predated 10a's contract meta-test). Update this number when adding or removing tests.

- **API test baseline (as of 10d zod-adoption B2-1 + COND-1, 2026-07-18):** hermetic `pnpm --filter statera-api test` = **645 passed / 16 skipped / 0 failed across 49 files** (43 passed + 6 skipped integration files), exit 0 with no Unhandled Errors section, and `pnpm --filter statera-api exec tsc --noEmit` = 0 errors, both hermetic. **B2-1-COND-1 (D-CO-a cure) added +2 first-fail-ordering cases in `routes/aggregation.test.ts` — r5 (bad dimension + bad month → "dimension must be one of…" wins) and r7 (bad range + bad month → "range must be one of…" wins), proving the separate post-schema month `safeParse` preserves the B0 schema-field-first order (643→645; files/skipped unchanged).** **B2-1 (TODO(phase4-zod-adoption), analytics month family + intelligence `days`) converted 5 hand-rolled `month` checks (account-overview, safe-to-spend, dashboard-bundle, r5 expense-breakdown, r7 budget-metrics) to a shared `MonthFormatSchema` zod + `zodErrorToEnvelope` (byte-identical no-period string) with the `if (!month) default` branch kept hand-rolled (D2), and the R12 `days` range to a `DaysRangeSchema` zod with `parseIntParam` (default 90 + non-numeric leniency) kept hand-rolled (D2). Added +9 hermetic passes: `routes/aggregation.test.ts` +7 (absent-month→200 for safe-to-spend/expense-breakdown/budget-metrics/dashboard-bundle + malformed-month→400 identity for account-overview/budget-metrics/dashboard-bundle; account-overview absent-month reuses the existing "default month (no param)" case, safe-to-spend/expense-breakdown malformed reuse existing "invalid month format" cases) and `routes/intelligence.test.ts` +2 (non-numeric days→90→200 leniency-preserved + upper-bound days=366→400 identity; absent-days→90 covered by the existing "count and days meta" case) → 634→643; files unchanged at 49, skipped unchanged at 16. Byte-identical wire strings, no money touched (D7). The r5/r7 month zod is a SEPARATE post-schema safeParse (their existing `r5Schema`/`r7Schema` parse runs first — ordering preserved). MONTH_RE retained (still used by the out-of-scope r6/dashboard-metrics `until` residuals = named follow-on "aggregation until residuals"). B2-1 STOP-1 (R14) removed weekly-digest from scope as a phantom (no month query param). Prior baseline (as of phase4 SC-1/2 debt/savings removal, 2026-07-16): hermetic `pnpm --filter statera-api test` = 634 passed / 16 skipped / 0 failed across 49 files (43 passed + 6 skipped integration files), exit 0 with no Unhandled Errors section, `tsc --noEmit` = 0 errors. **SC-1/2 removed the debt/savings backend: 4 whole test files deleted (`routes/debt.test.ts`, `routes/savings-goals.test.ts`, `lib/debt-calculator.test.ts`, `lib/savings-goals-lib.test.ts`), plus in-file removals (email-templates `goal_milestone` block, aggregation R9 F1–F5/WC1–WC4 debt/savings fixtures + WC4 deleted, intelligence R13 total_debt/savings, data-export debt/savings sections, demo-data-lib C9 seed/clear, account-deletion 16→14 purge-call count). Net −124 passed / −4 files vs the 758/53 B1 line; skipped unchanged at 16. The INTEGRATION suite (6 gated files) is unchanged in count — its debt/savings assertions in `demo-data.integration.test.ts` + `import.integration.test.ts` were pruned in place.** Prior baseline (as of 10d zod-adoption B1, 2026-07-11): 758 passed / 16 skipped / 0 failed across 53 files (47 passed + 6 skipped integration files), exit 0. Zod-adoption B1 (TODO(phase4-zod-adoption)) added NEW shape-only zod schemas to budgets + notifications and +9 hermetic passes: `routes/budgets.test.ts` +5 (a "B1 zod shape message identity + first-fail ordering" block — GET required/format, POST combined-presence, POST valid-month-items-absent → still-combined-presence (D1), POST valid-presence-bad-format (D1); its `beforeEach` calls `vi.resetAllMocks()` before setting `getDb`, matching the other budgets describes — omitting it hangs the real-`requireAuth`/rate-limit path) and `routes/notifications.test.ts` +4 (message identity ×3 + a D2 case proving a numeric `alert_key` still coerces to string and is accepted) → 749→758; files unchanged at 53 (added to existing files, no new file). The 4 converged shape sites (budgets GET+POST, notifications GET+dismiss) are byte-identical on the wire; money/Decimal parsing + the budgets duplicate-category + amount checks stay hand-rolled (Decision 2 / money gate: no `Number(`/`parseFloat`/amount touched). Prior 10d zod-adoption B0 baseline (2026-07-11): hermetic `pnpm --filter statera-api test` = 749 passed / 16 skipped / 0 failed across 53 files (47 passed + 6 skipped integration files), exit 0 with no Unhandled Errors section, and `pnpm --filter statera-api exec tsc --noEmit` = 0 errors, both hermetic. Zod-adoption B0 (TODO(phase4-zod-adoption)) added `routes/route-helpers.test.ts` (6 cases — 4 `zodErrorToEnvelope` + 2 `parseIntParam`, new file) + 1 multi-invalid-field ordering case each in `routes/categories.test.ts` (name-before-is_income → "Name is required.") and `routes/aggregation.test.ts` (r5 all-fields-invalid → dimension message) + 1 message-identity case in `routes/merchants.test.ts` → +9 hermetic passes vs the rate-limit backfill's 740 (740→749; +1 file); skipped unchanged at 16. The 6 converged sites (categories create, merchants create+patch, aggregation r5/r6/r7) are byte-identical on the wire; the two `remap` hardcoded-message envelopes are deliberately preserved (not routed through the helper). Prior 10d rate-limit-backfill baseline (2026-07-10): hermetic `pnpm --filter statera-api test` = 740 passed / 16 skipped / 0 failed across 52 files (46 passed + 6 skipped integration files), exit 0 with no Unhandled Errors section, and `pnpm --filter statera-api exec tsc --noEmit` = 0 errors, both hermetic. The rate-limit backfill added `lib/rate-limit.test.ts` (1 counting/429-envelope case, new file) + 1 over-limit case each in `routes/budgets.test.ts`, `routes/categories.test.ts`, `routes/merchants.test.ts`, `routes/notifications.test.ts` → +5 hermetic passes vs 8f-2's 735 (735→740; +1 file); skipped unchanged at 16. Prior 8f-2 baseline (2026-07-07): hermetic `pnpm --filter statera-api test` = 735 passed / 16 skipped / 0 failed across 51 files (45 passed + 6 skipped integration files), exit 0 with no Unhandled Errors section, and `pnpm --filter statera-api exec tsc --noEmit` = 0 errors, both hermetic. 8f-2 added `lib/restore-repurge-lib.test.ts` (6 gate cases, new file) → +6 vs 10d-0's 729 (729→735; +1 file); skipped unchanged at 16. Prior 10d-0 baseline (2026-07-07): 729 passed / 16 skipped across 50 files. 10d-0 added `worker/jobs/delete-account-job.test.ts` (3, new) + `routes/auth.callback.test.ts` (1, new) + 1 unit case each in `lib/account-deletion.test.ts` and `routes/auth.2fa-verify.test.ts` → +6 hermetic passes vs 10c-1's 723 (723→729; +2 files) + 1 new skipped integration case in `lib/account-deletion.integration.test.ts` (15→16 skipped). **Integration run (verified green 2026-07-07 against the live Docker stack, `INTEGRATION=true`): 50 files / 745 passed / 0 failed / exit 0.** Prior 10c-1 baseline (2026-07-05): 723 passed / 15 skipped across 48 files. 10c-1 added `lib/data-export-lib.test.ts` (8 cases, new file) + 2 route cases in `routes/account.test.ts` → +10 hermetic passes vs 10b-3's 713 (713→723); skipped unchanged at 15. Prior 10b-3 baseline (2026-07-04): 713 passed / 15 skipped across 47 files; 10b-3 added `routes/import.integration.test.ts` (6 cases) → +6 skipped vs the 7.5 fix-forward's 9; hermetic pass count was unchanged at 713. New deps `exceljs` + `papaparse` (+`@types/papaparse`) for XLSX/CSV parsing. **Integration run (verified green 2026-07-04 against the live Docker stack, `INTEGRATION=true`): 47 files / 728 passed / 0 failed / exit 0** — all 6 integration files (`merchants`, `categories`, `worker`, `account-deletion`, `demo-data`, `import`) pass. The unit suite is now a CI gate. `ioredis` is mocked module-wide for the unit run via `src/test/redis-mock.setup.ts` (a plain-function stub — NOT `vi.fn()`, so `vi.resetAllMocks()` cannot strip it and `get()` reliably returns null = "sv not revoked"); the mock is skipped when `INTEGRATION=true` (worker.integration.test.ts needs real BullMQ). Route tests read responses via `readJson()` from `src/test/json.ts` (app.request/router.request `.json()` infers `unknown`). Integration tests remain gated behind `INTEGRATION=true` and are a local/pre-release discipline, not part of CI. Update this number when adding or removing tests.

- **Module 7 smoke test:** `tools/module-7-smoke.sh` — interactive end-to-end smoke against the local Docker dev stack. Covers 7a/7b/7c/7.5. Runs once per session (account deletion destroys the test user; next run creates a fresh one). Requires jq, oathtool, docker, curl, and the dev stack up. Operator pastes `statera_session` and `statera_pending_2fa` cookies from browser DevTools at five interactive points; rest is automated.

## Public API contracts (do not change without coordination)

- `GET /api/auth/me` (completed in 9.4): requires auth. Returns `{ ok: true, user: User, flags: { template_suggestions: boolean, open_banking: boolean } }`. `User` contains `{ id, email, display_name, first_name, last_name, totp_enabled, created_at }`. Excludes secret material (`totpSecret`, `totpBackupCodesJson`, `sessionVersion`, `externalId`, `authProvider`, `isActive`, `lastLoginAt`). `flags` currently hard-coded `false` for both fields — underlying features not implemented in Hono; see TODO(module-9-feature-flags-audit). Error response for user-not-found (should be unreachable): `{ ok: false, data: null, error: "User not found.", code: "user_not_found" }` HTTP 401.
- **TODO(module-9-feature-flags-audit):** Wire up `template_suggestions` and `open_banking` flags in /me response to `env.enableTemplateSuggestions` and `env.enableOpenBanking` when underlying features are ported. Currently hard-coded `false` because the features don't exist in Hono. Flipping env vars to `true` before the routes exist would show UI nav items backed by dead endpoints.
- **TODO(module-9-contract-validation):** No mechanism currently validates backend response shapes against frontend declared types. Module 7 smoke test validated /me as HTTP 200 without checking whether the response matched the frontend's expected shape — the stub `{ session }` passed smoke for months while the frontend always received `undefined` for `data.user`. A process or tooling decision is needed before Module 9 closes: options include (a) MSW-based fetch-level mocking that validates request/response shapes, (b) shared type contracts between apps/api and apps/web, or (c) lightweight integration tests that call the real API and assert response shapes.
- Error codes are part of the API: e.g., `PAYMENT_TOO_LOW`, `category_name_exists`, `merchant_name_exists`, `budget_duplicate_category`. Match Flask exactly. **(phase4 SC-1/2: the debt/savings error codes `debt_name_conflict`, `goal_inactive`, `goal_fully_funded`, `PAYMENT_TOO_LOW`/`would_exceed`/`deposit_conflict` no longer exist — their routers were removed with the debt-accounts + savings-goals features.)**
- **Rate-limit 429 body (changed 2026-07-10, `TODO(phase4-rate-limit-backfill)`):** every `createRateLimiter` instance now returns the standard error envelope on 429 — `{ ok: false, data: null, error: "Too many requests. Please try again later.", code: "rate_limit_exceeded", meta: { retry_after: <windowSec> } }` (matches Flask's `code`/`retry_after`). This **changed the 429 body of the pre-existing limiters too** (transactions POST/search, auth, account data-export) from hono-rate-limiter's default plain text → this JSON envelope — a client-observable change, hence recorded here. `RateLimit-*` headers (`standardHeaders: "draft-6"`) are unchanged. Frontend `apiFetch` and `accountApi.dataExport` key their 429 branch on `res.status`, not the body, so no frontend change was required; the envelope simply upgrades a formerly-generic `ApiError("HTTP 429")` to carry `code:"rate_limit_exceeded"`. Backfilled routes and limits: budgets `POST /` 20/min, `GET /` + `GET /months` 60/min; categories `POST /` + `DELETE /:id` 30/min, `POST /:id/remap` 20/min, `GET /` 60/min; merchants `POST /` + `PATCH /:id` + `DELETE /:id` 30/min, `POST /:id/remap` 20/min, `GET /` 60/min; notifications `POST /budget-alerts/dismiss` 30/min, `GET /budget-alerts` 60/min. All deviation-by-addition (Flask left these four routers unlimited). Keying is per-(userId, concrete path); `:id` routes are per-(user, resource-id) — see the deviations block in `lib/rate-limit.ts`.
- ~~Diagnostic check order on the savings-goals deposit endpoint: not_found → inactive → fully_funded → would_exceed → deposit_conflict.~~ **REMOVED (phase4 SC-1/2): the savings-goals router (and its deposit endpoint) was deleted with the debt/savings feature removal.**
- **R9 safe-to-spend (phase4 SC-1/2):** the debt/savings fields were removed from the payload — `committed_kd` = budget allocations only; the `debt_minimum_total_kd`, all `savings_goal_*` fields, and the `committed_breakdown_kd.debt_minimums` / `.savings_goal_reserve` / `.savings_goal_budget_covered` sub-fields are gone (breakdown now carries only `budget_allocations`); the `debts_not_set_optional` and `savings_goals_unscheduled_optional` warnings no longer emit. The Safe-to-Spend headline rises accordingly (operator-acknowledged, ruling (d)). Module 11 queue note: recurring-pattern work should evaluate deriving R9 committed obligations from detected recurring transactions.
- **R8 dashboard-bundle (phase4 SC-1/2):** the `debt_summary` key was removed from the bundle response with the debt feature.
- POST `/api/budgets` is a full atomic replace for the given month. Frontend must send the complete list of budgets for the month.
- Categories `remap` does not delete the source. Merchants `remap` merges (deletes the source). This asymmetry is documented in code; preserve it.
- **Category dependent-counts / remap contract (changed phase4 SC-1/2, 2026-07-16 — new client-observable change beyond Phase A, per review-channel R2):** category **delete** `dependent_counts` no longer carries a `goals` key, and category **remap** result (`RemapCounts`) no longer carries `goal_count` — both dropped when the savings-goals feature was removed (savings goals used to link to a category via `linked_category_id`, so the delete/remap flow relinked them and counted them). Frontend `CategoryDependentCounts.goals`, `CategoryRemapResult.goal_count`, and the `SettingsDialog` "N goals" reassign-copy were removed to match. `transactions`/`budgets`/`memorized` counts are unchanged.
- Memorized POST/PATCH does name lookup only — does not create categories or merchants. Differs from transactions CRUD intentionally.
- `confidence` stable enum (R11 income-pattern, R12 recurring-patterns): `"high" | "medium" | "low"`. Do not change values.
- `income_source` stable enum (R11, R9, R10 safe-to-spend, weekly-digest): Hono returns `"detected_from_transactions" | "declared_in_profile" | "not_set"`. Flask returns `null` instead of `"not_set"` — this is a documented deviation. **Module 9 must update `apps/web/src/types/api.ts` (lines 138, 176, 265) and `sections.tsx:249` from `null` to `"not_set"` before frontend parity testing.**
- Analytics routes URL prefix: all analytics routes mount at `/api/analytics/*` (Hono) vs Flask's `/api/*` root paths. Module 9 verifies frontend URL parity.
- R12 recurring-patterns feature flag: `ENABLE_RECURRING_PATTERNS=false` returns HTTP 200 with `{ ok: true, data: { patterns: [] }, meta: { count: 0, enabled: false } }`. This is a client-observable behaviour: frontend must handle `enabled: false` in meta without rendering a missing-data error. Do not change the response shape or the HTTP status code.
- R13 snapshot: all KWD amount fields (`income_total_kd`, `expense_total_kd`, `net_kd`, and per-window `income_kd`/`expense_kd`/`net_kd`) return as 3-decimal strings (e.g., `"500.000"`). Flask R13 returns floats via `_rounded_number`/`to_display_float`; Hono normalizes to strings via `formatKd` to match the project-wide KWD-as-string convention used by R3/R4/R9/R10/R11/R12. Module 9 frontend types must treat these fields as `string`, not `number`. **(phase4 SC-1/2: the `net_position.total_debt_kd` and `total_savings_kd` fields were removed with the debt/savings features — ruling (c), R13-a, no zeroing.)**
- `sv` JWT claim (added in 7a): session version number embedded in the `statera_session` JWT. Module 9 must NOT decode the JWT client-side and rely on `sv`; session management is server-side. If the frontend ever decodes the JWT for UX purposes, add `sv: number` to the JWT type definition in `apps/web/src/types/`. Error code `session_invalidated` (HTTP 401) means the token's sv was explicitly revoked — frontend must clear local state and redirect to login.
- `statera_pending_2fa` cookie (added in 7b): short-lived (5-min) JWT issued by the OIDC callback when `totpEnabled=true`. Carries only `{ userId, pendingAt }`. The real session cookie is NOT issued until POST `/api/auth/2fa/verify` succeeds. Frontend must handle the `/auth/2fa-verify` redirect from the callback. Error codes: `PENDING_2FA_GONE` (HTTP 410) = cookie absent or expired; `PENDING_2FA_RESTART` (HTTP 401) = 3 failures hit, must restart login. `BACKUP_CODES_LOW` warning in `data.warning` when ≤2 backup codes remain after successful backup-code login.
- `GET /api/auth/profile/security-events` (added in 7c): returns only `event_type LIKE 'profile.%'` events — a profile-change audit trail, not a full security log. Login, auth, and session events are written to the table but intentionally excluded. Returns empty until profile-update routes (Module 9) emit `profile.*` events. Pagination: `limit` (default 20, max 50), `offset` (default 0). `has_more` flag in both `data` and `meta`. Do not change the filter to include `login.*` or `sessions.*` without coordinating with the frontend.
- `POST /api/auth/sessions/revoke-all` (added in 7c): bumps `session_version` in DB and writes `sv_revoked:{userId}:{oldSv}` deny-list key (30-day TTL). Re-issues caller's session cookie with `newSv`. Returns `{ session_version: newSv }`. Rate: 10/60s.
- `GET /api/auth/delete-reauth` (added in 7.5): requires auth. Initiates OIDC re-auth with `prompt=login, max_age=0`. State cookie carries `{ deleteIntent: true, userId }`. After callback + optional 2FA, issues `statera_delete_intent` cookie (Path=/api/account, Max-Age=900). Rate: 10/60s.
- `DELETE /api/account` (added in 7.5): requires auth + valid `statera_delete_intent` cookie (must match session userId). Returns `{ deleted: true, task_id: <encrypted-token> }`. The session cookie is cleared on success. Error codes: `DELETE_INTENT_GONE` (410) = cookie absent/expired/mismatched; `ACCOUNT_INACTIVE` (403) = already deleted; `deletion_failed` (500) = sync purge failed. Rate: 10/60s.
- `GET /api/account/deletion-status/:taskToken` (added in 7.5): no auth required; encrypted token proves authorization. Returns `{ status: "complete" | "pending" | "failed", task_id }`. For sync fallback tokens, always returns `complete`. Rate: 10/60s (path-keyed).
- `GET /api/account/data-export` (added in 10c-1): requires auth. Synchronous JSON right-to-access export of all user-owned data. Returns `{ ok: true, data: <export>, error: null, meta: { counts, excluded } }`. `<export>` = `{ generated_at, user, profile|null, categories[], merchants[], transactions[], budgets[], debt_accounts[], savings_goals[], memorized_transactions[], template_suggestion_feedback[], security_events[], product_events[] }`. Scope mirrors the account-deletion purge minus the documented exclusions (see `lib/data-export-lib.ts` file-top deviations block, echoed at runtime in `meta.excluded` / `DATA_EXPORT_EXCLUSIONS`): `users.external_id` + `totp_secret`/`totp_backup_codes_json`/`session_version` (auth-infra), `transactions.name_key`/`import_row_hash`, and the `dashboard_snapshots`/`account_action_tokens` tables + `security_events` tombstone rows. `template_suggestion_feedback` is a PRESENT section (always `[]` — feature not in Hono), NOT an exclusion. `memorized_transactions.norm` IS exported (derived-data transparency). KWD amounts are 3-decimal strings via `formatKd`; timestamps `+00:00`; dates `YYYY-MM-DD`. `meta.counts` is per-array counts; `meta.excluded` is data-independent. Error: `{ code: "user_not_found" }` HTTP 401 (unreachable behind requireAuth). Rate: 5/3600s. **Frontend consumer added in 10c-3:** `accountApi.dataExport()` (raw `fetch` → blob download; 429 surfaces a rate-limit toast) — exposed both standalone and inside the pre-deletion dialog in ProfilePage's "Data & privacy" section.
- **Module 9 follow-up (GDPR) / TODO(GDPR-data-export): CLOSED 2026-07-05 (10c-1)** — `GET /api/account/data-export` implemented (see the contract entry above). Returns a JSON snapshot of all user-owned data whose scope mirrors the deletion purge, minus documented auth-infra/derived-data exclusions. Backend-only; the "Download my data" UI is bundled with the account-deletion re-expose under TODO(account-deletion-ui) — neither ships alone, and both must land before account deletion is exposed in the UI.
- **TODO(account-deletion-ui): CLOSED 2026-07-06 (10c-3)** — account-deletion re-expose UI shipped (frontend-only). ProfilePage "Data & privacy" → delete dialog (offers data export first) → `<a href="/api/auth/delete-reauth">` → OIDC re-auth (+ optional 2FA via `?intent=delete`) → standalone `/delete-account/confirm` page (outside ProtectedRoute) with type-to-confirm `DELETE` → `accountApi.deleteAccount()` (DELETE /api/account) → `resetAuthState()` + redirect `/login?deleted=1`. The bundled "Download my data" UI (TODO(GDPR-data-export), backend closed 10c-1) shipped in the same section via `accountApi.dataExport()`. See the 10c-3 entry in Migration status. NOT yet on `main` (branch-only until the 10c merge).
- **TODO(key-rotation-runbook):** Write the Hono-era encryption key rotation procedure at docs/runbooks/key-rotation.md (currently a stub). Covers: generate new key (`openssl rand -hex 32`), deploy with both `ENCRYPTION_KEY` + `ENCRYPTION_KEY_PREVIOUS` set, re-encrypt all `users.totp_secret` (`enc1:` prefixed) rows using `apps/api/src/lib/crypto.ts` logic, verify no rows use old key, remove `ENCRYPTION_KEY_PREVIOUS`, redeploy.
- **TODO(migration-journal-guard):** Add a CI check (or pre-commit hook) that fails if any `.sql` file under `apps/api/src/db/migrations/` is not listed in `meta/_journal.json`. Prevents the is_tombstone class of silent migration drift where a committed SQL file is never applied to production.
- **Health endpoints at root, not under `/api/*` (8e):** `GET /healthz`, `GET /readyz`, `GET /health` are served at the root of staterafinance.app, not under `/api/`. The Caddyfile reverse-proxies all three from root, the Hono application defines them at root. This is an intentional infrastructure-vs-application boundary: `/api/*` is for application routes (require auth, return business data), `/healthz` and `/readyz` are for ops tooling (no auth, return only liveness/readiness). UptimeRobot, Kubernetes-style probes, and any future monitoring should probe the root-level paths. Do not move these under `/api/` without coordination — moving them breaks the convention and requires updating both the Caddyfile and Hono routes simultaneously.
- **`transactionsApi.topPatterns` has no UI consumer as of design-5.5 (recorded 2026-07-16, ruling 5.5-1).** Its sole live caller, the `TransactionsHero` component, was deleted in design-5.5. The frontend method (`apps/web/src/lib/api.ts`) and its mounted backend route are RETAINED — now exercised only by the contract-capture harness (`apps/web/src/contract/capture.ts`) — because removal is api-surface scope (touches `api.ts` + `capture.ts` + the committed contract fixture + a regenerate), not the dead-*view*-code scope design-5.5 was chartered for. Recorded so a future api-surface audit inherits this orphan explicitly rather than rediscovering it. See TODO(module-9-network-mocking).
